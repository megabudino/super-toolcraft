# Implementation Worklog

Active change: template-release-2026-09-09

## Status

Mode: product

Product: Liquid Metal 3D

Verification tier: Tier 3 for animated preview performance and renderer cadence.
Reason: The preview frame scheduler, per-frame renderer work, product-frame performance probe, authored-default readiness observables, and interaction stress fixtures change without altering schema state, media bytes, shader appearance, render scale, or export quality.
Run: focused scheduler/source/product tests, TypeScript, `npm run verify:quick`, authored-default and sticker/scratch/orbit browser acceptance, targeted interaction and animation performance scenarios, an agent-controlled Metal checkpoint on the real 3840x2160 default scene, `npm run verify:perf`, and `npm run verify:final`.
Skip: No performance gate is skipped because the user explicitly requested animation-lag repair. Playwright SwiftShader remains the automated cadence/interaction fallback; the real authored 4K workload is measured in the agent-controlled Metal browser because software WebGL cannot represent hardware throughput.

## Decision Trail

### Iteration 1 — Paper Liquid Metal reference study and product contract

- Request: Upload a 3D object and apply the shader and settings from `https://shaders.paper.design/liquid-metal` one-to-one.
- Task type: Reference-runtime clone adapted to a user-approved 3D source.
- User-visible result: A Toolcraft editor for real mesh upload with Paper's Liquid Metal controls, presets, animation, and export.
- Source/reference checked: Live Paper Liquid Metal page; `paper-design/shaders` repository commit `e77c99ede9be8f4842c6bac22ca24902dc41334d`; npm packages `@paper-design/shaders@0.0.77` and `@paper-design/shaders-react@0.0.77`.
- Reference inputs: Live Default and Noir behavior; official fragment shader, React prop mapping, presets, documented ranges, and fit/time behavior.
- Docs/contracts read: `workflow.md`; runtime boundary; setup/export; control selection; layout; media upload; timeline animation; performance; reference study; assembly workflow; decision contract; schema reference; component rules; acceptance testing; renderer technique; custom controls.
- Contract rules applied: `workflow-required`, `runtime-shell-required`, `canvas-no-app-ui`, `canvas-surface-preserved`, `controls-product-coverage`, `renderer-technique-inventory`, `video-reference-analysis`, `reference-clone-source-of-truth`, `timeline-mode-choice`, `output-export-required`, `persistence-policy-explicit`.
- Decision: Preserve Paper's fragment shader through its official package and replace only its 2D image/shape mask with rasterized 3D mesh fragments.
- Alternatives rejected: A visually similar Three.js metal/PBR material would not be the same shader; rewriting GLSL as a mesh lighting model would alter the reference output; a flat image mask would not satisfy the 3D-model request.
- State/output mapping: Toolcraft schema values map to the matching Paper props/uniforms; timeline seconds map to deterministic Paper frame milliseconds times Speed; the Paper canvas is sampled in screen space only on depth-tested mesh pixels.
- Files changed: Product spec and plan; package dependencies; schema; renderer/model/Paper/export modules; route; acceptance and performance matrices; tests.
- Verification: `npm run ai:check` and `npm run typecheck` passed; targeted product Vitest passed; upload-dependent Playwright performance checkpoint passed 33/33.
- Skipped checks: None.
- Risks: Self-contained GLTF is supported, while GLTF files with separately referenced buffers/textures should be packaged as GLB.

### Iteration 2 — 3D renderer, exports, and acceptance surface

- Request: Apply the copied shader to uploaded 3D geometry and expose the complete shader settings.
- Task type: Tier 4 implementation.
- User-visible result: GLB/GLTF/OBJ/STL import, exact four presets and parameter ranges, middle-button model orbit, forward-only playback timeline, transparent/background PNG, and video export.
- Source/reference checked: Official `liquid-metal.ts`, `liquid-metal.tsx`, package presets, and the live reference control panel.
- Reference inputs: `colorBack`, `colorTint`, `repetition`, `softness`, RGB shifts, `distortion`, `contour`, `angle`, `speed`, `scale`, `rotation`, offsets, and `fit`.
- Docs/contracts read: Toolcraft media, timeline, renderer, export, acceptance, and performance contracts routed by `workflow.md`.
- Contract rules applied: `canvas-handle-placement`, `layers-enable-only-when-needed`, `timeline-enabled-behavior`, `acceptance-product-observable`, `performance-coverage-levels`.
- Decision: Use the official React shader for preview and the official vanilla shader mount for deterministic export; composite both through one Three.js screen-space texture material.
- Alternatives rejected: A per-mesh UV texture changes the reference projection; app UI inside `canvasContent` violates the canvas boundary; layers add no meaningful entity for a single model/output.
- State/output mapping: Media assets decode once per identity; control changes update Paper uniforms; Three renders at Toolcraft canvas size and render scale; image/video resolutions create isolated export contexts.
- Files changed: `src/app/app-schema.ts`, `liquid-metal-values.ts`, `liquid-metal-model.ts`, `liquid-metal-scene.ts`, `liquid-metal-paper.ts`, `liquid-metal-export.ts`, `liquid-metal-renderer.tsx`, route, acceptance/performance files, browser tests.
- Verification: TypeScript, product tests, functional browser scenarios, real PNG/video decoding, and the Playwright fallback performance checkpoint pass.
- Skipped checks: None.
- Risks: Browser MediaRecorder may fall back from requested MP4 to WebM when H.264 MP4 encoding is unavailable; the downloaded extension follows the actual supported MIME.

### Iteration 3 — GPU synchronization and viewport performance

- Request: Deliver the first working product with strict renderer, animation, viewport, and import performance evidence.
- Task type: Tier 4 performance stabilization using systematic debugging.
- User-visible result: Model import, animated preview, canvas drag, and zoom remain responsive without reducing Paper shader fidelity or Toolcraft render scale.
- Source/reference checked: Three.js `WebGLRenderer.setSize`; Paper `ShaderMount` frame/speed behavior; Chrome CPU profile for the measured model-import interaction; typed Toolcraft performance budgets.
- Reference inputs: Official Paper CanvasTexture pixels and the app's declared 120 ms frame-gap / 2000 ms interaction limits.
- Docs/contracts read: Toolcraft core/app performance contracts and systematic-debugging workflow.
- Contract rules applied: `performance-coverage-levels`, `renderer-technique-inventory`, `canvas-surface-preserved`, `acceptance-product-observable`.
- Decision: Cache Three.js backing dimensions, prepare the target size before model import, warm the exact cross-context Paper CanvasTexture while the product canvas remains blank, and invalidate preview textures only when the Paper settings token changes.
- Alternatives rejected: Raising budgets would hide the regression; lowering exported resolution would change product output; replacing Paper with an approximate material would violate reference parity.
- State/output mapping: Paper settings/timeline create a texture-version token; a changed token uploads one new Paper frame, while unchanged model orbit and viewport operations reuse the current GPU texture.
- Files changed: `src/app/liquid-metal-scene.ts`, `src/app/liquid-metal-renderer.tsx`, `src/app/liquid-metal-model.ts`, and focused performance interaction helpers.
- Verification: Strict `TOOLCRAFT_TEST_PORT=3100 npm run verify:perf` passed 33/33; `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` passed 249 unit/contract tests, production build, and 19/19 browser acceptance tests.
- Skipped checks: None.
- Risks: None beyond the documented GLTF sidecar and MediaRecorder codec constraints.

### Iteration 4 — Zero-size Paper canvas import race

- Request: Fix the uploaded `/Users/kusnizza/Desktop/A.obj` rendering without the Liquid Metal shader.
- Task type: Tier 3 renderer/media regression using systematic debugging.
- User-visible result: A model uploaded immediately after page load receives the Paper Liquid Metal pixels instead of producing a transparent Three.js canvas.
- Source/reference checked: The supplied `A.obj`; browser console WebGL diagnostics; Paper backing-canvas dimensions; visible Three canvas screenshot pixels; Three.js CanvasTexture upload behavior.
- Reference inputs: `/Users/kusnizza/Desktop/A.obj` (1 mesh, 1,986 triangles, valid bounds) and the official Paper Liquid Metal canvas already used by the app.
- Docs/contracts read: `workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`.
- Contract rules applied: `canvas-surface-preserved`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Reject zero-width or zero-height Paper canvases at the `LiquidMetalSceneRenderer.setShaderCanvas` boundary and retry naturally on the next RAF after Paper's ResizeObserver establishes backing pixels.
- Alternatives rejected: An arbitrary upload delay would leave the race timing-dependent; rebuilding the texture after WebGL failure treats the symptom; changing the OBJ parser is unrelated because the supplied model decoded correctly.
- State/output mapping: Paper canvas readiness now gates CanvasTexture creation; once non-zero, the same settings token and uploaded media identity feed the existing Three mesh composite and export path.
- Files changed: `src/app/liquid-metal-scene.ts`, `e2e/app-controls.spec.ts`, and this worklog.
- Verification: The new browser regression reproduced the pre-fix defect with 70 `GL_INVALID` messages and passed after the fix; the supplied `A.obj` produced 2,611 visible Default colors, a 255 luminance range, a distinct Noir screenshot, and zero WebGL errors; `npm run verify:quick` passed 249 tests; targeted media-import and stress-preview performance tests passed 2/2.
- Skipped checks: The full performance checkpoint is not required for this post-first-working correctness fix; the two directly touched import/preview performance scenarios passed under their existing strict budgets.
- Risks: None; the fix preserves selected render scale, Paper shader code, model geometry, and export quality.

### Iteration 5 — Form-aware Liquid Metal surface material

- Request: Replace the visible 2D-texture-by-mask result with Liquid Metal that feels applied directly to the 3D object.
- Task type: Tier 3 renderer behavior redesign through brainstorming and writing-plans.
- User-visible result: Paper patterns wrap across faces and bevels, react to the mesh normal and camera direction, gain Fresnel/studio highlights, and move across the surface when the model is orbited.
- Source/reference checked: User feedback on the supplied `A.obj`; official Paper Liquid Metal field and controls; current Three.js screen-space material; real paused before/after orbit screenshots; preview/export performance budgets.
- Reference inputs: `/Users/kusnizza/Desktop/A.obj`, the official Paper Liquid Metal generator, and the user's statement that the previous result looked like a 2D texture mask.
- Docs/contracts read: `workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, `acceptance-testing.md`, plus the required brainstorming, writing-plans, systematic-debugging, and browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `canvas-surface-preserved`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Keep Paper's official shader as the unchanged animated field, then sample it in the shared Three ShaderMaterial with the two dominant world-position triplanar projections, surface normals, reflection-warped coordinates, Fresnel, and analytic metal highlights. This uses two texture samples and multiplication-based Fresnel/highlights instead of the earlier four-sample spherical reflection path.
- Alternatives rejected: Retaining `gl_FragCoord` preserves the unwanted flat mask; UV-only mapping breaks on STL/OBJ models without reliable UVs; rewriting Paper as a new PBR generator loses one-to-one parameters and presets; lowering render scale hides performance cost by reducing quality.
- State/output mapping: Existing Paper settings and timeline still update the source field; model position/normal, camera direction, and orbit rotation now determine which field pixels and highlights each fragment receives; preview, PNG, and video share the same material class.
- Files changed: Product spec/plan, `src/app/liquid-metal-scene.ts`, `src/app/app-performance.ts`, renderer/product tests, `e2e/app-controls.spec.ts`, and this worklog.
- Verification: Red renderer-source coverage reproduced the old `gl_FragCoord` mapping and is now green; typecheck and 42 focused renderer tests passed; final exact `A.obj` output contained 2,131 colors across the full luminance range, kept one paused Paper frame while orbit changed pixels, and produced zero WebGL errors; `npm run verify:quick` passed 250 tests; Paper controls/presets, immediate-upload pixels, PNG, Current video, and 4K video browser acceptance passed; all five touched performance scenarios passed, and the two previously sensitive animation/drag budgets also passed two consecutive repeats after the final two-sample surface mapping.
- Skipped checks: The full performance checkpoint is not required for this post-first-working renderer behavior iteration; stress preview, 120-frame animation, animated viewport drag, viewport zoom, and export were the directly affected scenarios and all passed under existing strict budgets.
- Risks: The Paper generator and controls remain exact, but 3D placement is intentionally not pixel-identical to the flat Paper webpage; it is a form-aware reflective material rather than a physically simulated fluid surface.

### Iteration 6 — Direct procedural coverage on every mesh fragment

- Request: Explain and correct why the screenshot did not show Liquid Metal across the whole uploaded object.
- Task type: Tier 4 renderer architecture correction through systematic debugging, brainstorming, writing-plans, and browser verification.
- User-visible result: Every visible triangle of `A.obj` now executes the Liquid Metal stripe, noise, dispersion, softness, contour, tint, and timing math directly; no rectangular Paper field or fallback mesh color remains.
- Source/reference checked: `/Users/kusnizza/Desktop/A.obj`; the Default `Cover` and Scale comparisons from the previous build; official `node_modules/@paper-design/shaders/dist/shaders/liquid-metal.js`; preview, paused-orbit, and export code paths.
- Reference inputs: Paper's maintained simplex noise, `getColorChanges`, stripe widths, channel dispersion, contour, tint, angle, and time equations; the user's observation that uncovered areas were visible in the screenshot.
- Docs/contracts read: `workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`, plus the required debugging/spec/plan/browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `canvas-surface-preserved`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Port Paper's procedural core into the shared Three.js fragment material and replace only the reference's 2D image/shape edge adapter with normalized mesh-surface coordinates, reflection warp, and normal/view Fresnel. Preview and both export paths use this same shader material.
- Alternatives rejected: Cropping or repeating the Paper CanvasTexture retains a rectangular field; changing Default scale hides rather than removes uncovered regions; UV-only mapping excludes common OBJ/STL models; a generic MeshStandardMaterial would not preserve Paper's procedural controls.
- State/output mapping: Existing Toolcraft controls write directly to matching GLSL uniforms; timeline seconds write deterministic shader time; object position, normals, camera direction, and orbit rotation determine the surface field; canvas settings and background continue to govern preview and export output.
- Files changed: Product spec and plan; `liquid-metal-surface-shader.ts`; `liquid-metal-scene.ts`; renderer and export modules; deleted offscreen Paper module; acceptance/performance inventories; renderer/product/browser tests; this worklog.
- Verification: Direct-source contracts reject CanvasTexture and sampler-based mesh shading and require Paper's noise/color core; typecheck and 46 focused renderer/product tests pass; the exact `A.obj` is covered on front, side, and inner faces with no shader/WebGL diagnostics; a frozen shader frame changes pixels when the model is orbited; the immediate-upload browser regression passes.
- Skipped checks: The post-first-working full performance checkpoint is not required; targeted direct-shader, animation, viewport, timeline, and export scenarios provide current budget evidence.
- Risks: Paper's 2D image/shape edge calculation is intentionally replaced for full 3D coverage, so surface placement is not pixel-identical to the flat reference rectangle even though the procedural color and timing core and public controls are preserved.

### Iteration 7 — Non-tiled reflection/matcap material projection

- Request: Correct the result shown in the supplied screenshot where Liquid Metal repeats as rectangular tiles, research how 3D materials are normally projected, and evaluate matcap or another appropriate method.
- Task type: Tier 4 visual renderer correction using systematic debugging, brainstorming, writing-plans, primary-source research, and browser verification.
- User-visible result: The repeated rectangular fields and hard dominant-axis switches are removed; Liquid Metal now flows through a view-dependent matcap/reflection field whose continuously perturbed normal gives large planar faces changing reflective detail.
- Source/reference checked: User screenshot; exact `A.obj`; Three.js `MeshMatcapMaterial` GLSL and `MeshStandardMaterial` environment-map documentation; Unity's official triplanar documentation; Blender's official MatCap/environment descriptions; Paper's maintained Liquid Metal GLSL.
- Reference inputs: User-provided still PNG attachment `media_a59qCSUPbD`; `/Users/kusnizza/Desktop/A.obj`; official Three.js, Unity, Blender, and Paper shader sources.
- Docs/contracts read: `workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`, plus the required debugging/spec/plan/browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `canvas-surface-preserved`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Keep the direct Paper procedural color core, replace dominant-axis position UVs with the Three.js matcap view basis, perturb the mesh normal with continuous object-space simplex noise before projection, and remove mapped-UV `fract`. Paper's own stripe-phase `fract(direction)` remains because it implements the Repetition control rather than texture tiling.
- Alternatives rejected: Authored UVs cannot be assumed for arbitrary OBJ/STL; triplanar solves missing UVs but remains a spatial texture projection; pure matcap makes each large flat face sample one constant normal; an external HDR/cubemap would replace rather than preserve Paper's animated field.
- State/output mapping: Metal controls still write matching Paper uniforms; Scale/Rotation/Offset/Fit transform the single matcap field; Distortion and Contour also influence continuous normal ripple; timeline time drives both Paper motion and slow reflection flow; preview, PNG, and video share the same material.
- Files changed: Product spec and plan; `liquid-metal-surface-shader.ts`; renderer-source/product tests; renderer frame naming; acceptance/performance technique descriptions; this worklog.
- Verification: The red source contract reproduced dominant-axis plus mapped-UV wrapping and now rejects both; typecheck and 47 focused tests pass; `npm run verify:quick` passes 252 tests; exact `A.obj` shows non-repeating flow on broad, side, and inner faces with no shader/WebGL diagnostics; frozen-frame orbit and immediate-upload browser regression pass; `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` passes 252 unit/contract tests, production build, and 20/20 browser checks; ten targeted GPU scenarios pass, including three successful animation measurements after one mixed-run cold-start spike.
- Skipped checks: The post-first-working full performance checkpoint is not required; the final gate uses targeted shader, animation, viewport, timeline, and export budget scenarios.
- Risks: Matcap/reflection placement intentionally differs from Paper's flat canvas; hard geometric edges can change reflection abruptly, which is physically consistent with flat-faced metal rather than a projection seam.

### Iteration 8 — Physically based liquid chrome response

- Request: Research liquid metal shader implementations because the corrected non-tiled shader still does not look like realistic metal.
- Task type: Tier 4 renderer architecture correction using systematic debugging, brainstorming, writing-plans, primary-source research, and browser verification.
- User-visible result: The model now has real environment reflections, metallic Fresnel response, roughness-filtered highlights, and animated micro-normal flow; Paper's colors appear inside the reflected chrome instead of replacing all lighting.
- Source/reference checked: Three.js `MeshPhysicalMaterial`, `MeshStandardMaterial`, `RoomEnvironment`, PMREM and physical env-map shader sources; Filament metallic-roughness, GGX/IBL, perturbed-normal, and energy-conservation reference; pmndrs `MeshDistortMaterial` simplex-noise lit-material pattern; Paper Liquid Metal core.
- Reference inputs: `https://threejs.org/docs/pages/MeshStandardMaterial.html`; `https://threejs.org/docs/pages/RoomEnvironment.html`; `https://google.github.io/filament/Filament.md.html`; `https://google.github.io/filament/Materials.md.html`; `https://github.com/pmndrs/drei`; `/Users/kusnizza/Desktop/A.obj`.
- Docs/contracts read: `workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`, plus the required debugging/spec/plan/browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `canvas-surface-preserved`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Use `MeshPhysicalMaterial` with metalness `1`, calibrated low roughness, ACES tone mapping, and a cached `RoomEnvironment` PMREM. Inject the direct Paper core through `onBeforeCompile` after Three's normal-map stage so it perturbs the PBR shading normal and drives conductor base color plus micro-roughness, while Three.js owns GGX, Fresnel, IBL, energy conservation, and final tone mapping.
- Alternatives rejected: More analytic highlights in the custom shader remain unlit approximations; pure matcap bakes lighting and cannot vary roughness-filtered environment response; external HDR upload would invent a new product source/control; vertex displacement would alter the user's uploaded geometry rather than only its material.
- State/output mapping: Existing Paper controls and timeline update stable injected uniforms; Distortion and Contour bend the physical normal; Paper color math tints conductor reflection; deterministic time animates color, roughness, and micro-normal flow; preview, PNG, and video instantiate the same physical scene renderer and PMREM recipe.
- Files changed: Product spec and plan; `liquid-metal-surface-shader.ts`; `liquid-metal-scene.ts`; renderer source/product tests; acceptance/performance technique inventory; this worklog.
- Verification: The red source contract reproduced the standalone `ShaderMaterial` path and now requires the physical/PMREM pipeline; typecheck and 56 focused tests pass; `npm run verify:quick` passes 253 tests; exact `A.obj` shows chrome environment reflections and Paper flow with no shader/WebGL diagnostics; immediate upload/frozen orbit passes; all ten targeted shader, preview, animation, drag, zoom, timeline, and export performance scenarios pass; `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` passes 253 unit/contract tests, production build, and 20/20 browser checks.
- Skipped checks: The post-first-working full performance checkpoint is not required for this visual-fidelity edit; every changed physical renderer workload and interaction path has targeted budget evidence.
- Risks: The built-in procedural room gives deterministic studio chrome without network assets, but a user-authored HDRI is intentionally outside the original Paper control contract.

### Iteration 9 — Seamless forward shader and video loop

- Request: Remove the visible jerk at the loop boundary and make the animation a complete seamless cycle.
- Task type: Tier 3 animation/renderer regression using systematic debugging, brainstorming, writing-plans, and browser verification.
- User-visible result: Paper stripes, procedural color noise, animated PBR micro-normal ripples, preview playback, and recorded video now share one forward cycle whose first and wrapped frames stitch without a visible jump.
- Source/reference checked: Current physical shader time paths; Toolcraft `getToolcraftTimelineLoopProgress`; timeline animation contract; preview renderer; PNG/video deterministic frame paths; exact `/Users/kusnizza/Desktop/A.obj`.
- Reference inputs: Paper's original one-stripe period at Speed 1; the editable Toolcraft timeline duration; user's report of a visible loop jerk.
- Docs/contracts read: `workflow.md`, `core/timeline-animation.md`, `core/performance.md`, `decision-contract.md`, `component-rules.md`, acceptance/performance docs, plus the required systematic-debugging, brainstorming, writing-plans, and browser workflows.
- Contract rules applied: `timeline-mode-choice`, `timeline-enabled-behavior`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Normalize runtime time with `getToolcraftTimelineLoopProgress`, drive stripe phase and all physical noise from that one value, and use a two-trajectory forward simplex blend with smooth endpoint weights so both value and derivative match at the wrap. Speed applies a monotonic periodic pacing warp whose derivative stays positive, while edited duration remains the authoritative loop rate.
- Alternatives rejected: Raw modulo closes only Paper's `fract` stripes but leaves simplex endpoints unrelated; sine time would reverse direction; mirroring/yoyo violates the forward-loop contract; crossfading final rendered frames doubles the full material workload and hides rather than fixes mismatched shader state.
- State/output mapping: `timeline.currentTimeSeconds / timeline.durationSeconds` becomes `u_loopProgress`; `shader.speed` modifies only forward in-cycle pacing; the color field and three PBR normal-noise axes use the same phase; preview, PNG, and video use `getLiquidMetal3DSettings`; video samples `[0, duration)` and holds the final unique sample for its frame interval rather than recording a duplicate endpoint.
- Files changed: Product spec/plan; `liquid-metal-values.ts`; `liquid-metal-scene.ts`; `liquid-metal-surface-shader.ts`; `liquid-metal-renderer.tsx`; `liquid-metal-export.ts`; acceptance/performance inventory; product/source/browser tests; this worklog.
- Verification: Focused typecheck and 50 source/product tests pass; `npm run verify:quick` passes 255 tests; the browser seam test matches first/wrapped pixels at the default duration and after editing to 1.25 seconds; exact `A.obj` reaches loop progress `0` with mean sampled RGB difference `0.00163/765` and only one antialiased sampled pixel changing; PNG plus Current/4K video export passes; all five targeted Speed, 120-frame animation, timeline playback, timeline scrub, and export budget scenarios pass.
- Skipped checks: The full performance checkpoint is not required for this post-first-working animation-correctness edit; every directly touched workload passes targeted coverage.
- Risks: MediaRecorder container/codec support remains browser-dependent; the loop frames themselves are deterministic and endpoint-duplicate-free.

### Iteration 10 — Editable PBR environment and HDRI upload

- Request: Add an HDRI/environment setting to control the scene reflected by the liquid-metal material.
- Task type: Tier 3 schema/media/renderer/export feature using brainstorming, writing-plans, systematic debugging, and browser verification.
- User-visible result: A new Environment section provides Studio, Neutral, Warm, or Custom HDRI; `.hdr/.exr` upload appears only for Custom, and live Intensity/Environment rotation affect the model in preview, PNG, and video.
- Source/reference checked: Current Three.js `RoomEnvironment`/PMREM pipeline; Three r185 `HDRLoader`, `EXRLoader`, scene environment rotation/intensity, physical indirect-specular shader chunks, fileDrop media contract, exact `/Users/kusnizza/Desktop/A.obj`.
- Reference inputs: User request for HDRI/environment controls; `/Users/kusnizza/Desktop/A.obj`; generated valid Radiance HDR fixtures at 64x32 for agent-browser acceptance and 2048x1024 for workload verification.
- Docs/contracts read: `workflow.md`, runtime boundary, control selection/layout, media upload, setup/export, schema/component rules, renderer technique, acceptance, decision contract, core/app performance, plus the required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `controls-product-coverage`, `controls-layout-heuristics`, `renderer-technique-inventory`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Keep the existing cached Studio `RoomEnvironment` PMREM, add lightweight Neutral/Warm equirectangular HDR sources, and worker-decode custom HDR/EXR. Inject the selected direct radiance before Three's physical indirect-specular evaluation so GGX/Fresnel/conductor response remains active without synchronously rebuilding PMREM on every source change. Preview prepares a 512px environment texture; export prepares the same source up to 2048px.
- Alternatives rejected: Regenerating `PMREMGenerator.fromScene/fromEquirectangular` on every user source change caused measured 659 ms and 1359 ms frame gaps; decoding HDR/EXR on the main thread would freeze interaction; treating HDRI as the Background color would not affect metal reflections; a custom panel/upload UI would duplicate built-in Toolcraft controls.
- State/output mapping: `lighting.environmentPreset` chooses Studio/Neutral/Warm/custom; `media.environment` owns HDR/EXR lifecycle; intensity/rotation update stable environment uniforms; the preview environment effect is stale-safe and keeps the last valid source on decode errors; PNG/video await the same runtime environment recipe before rendering.
- Files changed: Product spec/plan; schema; new environment settings/worker modules; scene renderer/shader/export/preview; acceptance and performance inventories/tests; HDR browser fixture; worklog.
- Verification: Focused typecheck/source/product tests pass; `npm run verify:quick` passes 261 tests; dedicated browser acceptance passes presets, conditional visibility, HDR import/remove/section reset/global reset, live sliders, and HDRI PNG; six targeted preset/import/slider/120-frame-animation/export budgets pass after the worker/direct-radiance optimization; Warm PNG plus Current/4K video export passes; exact `A.obj` at 3840x2160 renders distinct Studio/Warm/custom-HDR pixels with no model/environment errors or console errors.
- Skipped checks: The full performance checkpoint is not required for this post-first-working feature; the two initially failed environment source/import measurements drove an implementation optimization, and the full directly affected six-scenario subset passes afterward.
- Risks: Custom HDR/EXR preview uses a 512px environment source for interaction smoothness while final export uses up to 2048px; extremely high-frequency HDR detail can therefore appear sharper in export, but geometry, Liquid Metal shader values, output resolution, and selected render scale are unchanged.

### Iteration 11 — Normalized model scale

- Request: Add scale control for the uploaded model.
- Task type: Tier 3 schema/control/shared-renderer feature using brainstorming, writing-plans, systematic debugging, and browser verification.
- User-visible result: `Model Size` now contains `Model scale`, default `1`, range `0.25..3`; it resizes the uploaded mesh in the canvas while Paper's separate projection `Scale` continues to control only the Liquid Metal field.
- Source/reference checked: Current `normalizeModel` geometry framing, `LiquidMetalSceneRenderer` preview/export path, Toolcraft mixed-section normalizer and standalone fileDrop contract, exact `/Users/kusnizza/Desktop/A.obj`, and browser pixels at small/default/large model scales.
- Reference inputs: User request for model scale and `/Users/kusnizza/Desktop/A.obj`.
- Docs/contracts read: `workflow.md`, runtime boundary, control selection/layout, media upload, setup/export, schema/component rules, renderer technique, acceptance, decision contract, core/app performance, plus the required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `controls-product-coverage`, `controls-layout-heuristics`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Keep fileDrop in its component-owned standalone `Model` section, place the adjacent continuous slider in `Model Size`, map `model.scale` through typed settings, and call `modelGroup.scale.setScalar` before every shared scene render. The value clamps to the visible `0.25..3` range and never enters Paper presets.
- Alternatives rejected: Reusing `shader.scale` would conflate object framing with pattern projection; changing camera position would alter perspective and reflected environment response; re-normalizing or mutating geometry on every drag would invalidate cached model preprocessing; mixing fileDrop and slider in one section is normalized into separate layouts by Toolcraft; custom UI would duplicate the built-in slider.
- State/output mapping: The built-in slider writes `state.values["model.scale"]`; `getLiquidMetal3DSettings` maps and clamps it to `modelScale`; `LiquidMetalSceneRenderer.setLiquidMetalSettings` applies it to the normalized model group; preview, PNG, and video all instantiate that same renderer/settings path.
- Files changed: Product spec/plan; `app-schema.ts`; `liquid-metal-values.ts`; `liquid-metal-scene.ts`; acceptance and performance inventories/tests; functional/performance browser tests; this worklog.
- Verification: `npm run verify:quick` passes 264 tests; dedicated browser acceptance proves pixel width grows at a fixed paused shader frame; targeted `model.scale` drag to hard limit `3` passes budget; PNG plus Current/4K video export passes with non-default scale `1.25`; agent-browser loads exact `A.obj`, changes scale `0.45 -> 1.45`, keeps frame `521.697`, produces distinct screenshots, and reports zero console/WebGL/shader errors.
- Skipped checks: The full performance checkpoint is not required for this post-first-working feature; the new workload scenario and shared export test cover the touched paths.
- Risks: Values above `1` may intentionally crop the model at canvas bounds; this is the direct model-framing behavior requested, and Reset Model Size restores `1`.

### Iteration 12 — Ordered geometry-conforming PNG stickers

- Request: Batch-upload raster PNG stickers, place each new sticker above previous ones, click and drag stickers over the 3D object, conform them to curved geometry, and light them with the selected HDRI; visual reference supplied as a sticker-covered metallic curved object.
- Task type: Tier 4 media/direct-manipulation/WebGL/export feature using brainstorming, writing-plans, systematic debugging, and browser verification.
- User-visible result: A new Stickers section imports several transparent PNGs into a sortable thumbnail grid. Each PNG becomes an ordered physical `DecalGeometry` clipped across source meshes; the top visible decal can be clicked and dragged over raycast surface hits, remains attached through model orbit/scale, reacts to Studio/Neutral/Warm/custom HDRI, and appears in final output.
- Source/reference checked: `/Users/kusnizza/Library/Containers/ru.keepcoder.Telegram/Data/tmp/telegram-cloud-photo-size-2-5361940565534121530-y.jpg`; Three r185 `DecalGeometry`; Toolcraft multi-file image media/order/transform contract; current PMREM/direct-radiance physical material pipeline; curved 32-segment OBJ and generated alpha PNG browser fixtures.
- Reference inputs: The still establishes preserved PNG color/alpha, overlapping stack, curvature following, and shared metallic environment lighting. It does not define animation timing, hidden controls, or decal size UI.
- Docs/contracts read: `workflow.md`, runtime boundary, control selection/layout, media upload, setup/export, schema/component rules, renderer technique, acceptance, decision contract, core/app performance, plus the required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `controls-product-coverage`, `canvas-no-app-ui`, `canvas-surface-preserved`, `layers-enable-only-when-needed`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Use built-in multi-PNG `fileDrop` and runtime `mediaAssets` order as the stack authority. Store normalized-model-local position/normal at `stickers.placements`; project with `DecalGeometry` against every base mesh; render ordered transparent `MeshPhysicalMaterial` decals with shared PMREM/direct HDRI uniforms. Direct manipulation raycasts locally at pointer rate, coalesces geometry to the next GPU frame, and commits one undoable runtime value on pointer release.
- Alternatives rejected: DOM/canvas overlays stay screen-aligned; UV textures require authored unwraps and tile on arbitrary uploads; matcap cannot preserve user PNG artwork; flat planes do not follow curvature; enabling generic Layers would expose unrelated model/HDRI media and blank layer entities alongside the already complete sticker collection owner.
- State/output mapping: `media.stickers` owns PNG bytes, order, selected thumbnail, rotate/flip metadata, removal, and reset; `stickers.placements` owns local projector positions/normals; renderer order/polygon offset makes the newest/last asset topmost; canvas click dispatches the matching media layer selection; preview, PNG, and video call the same async sticker sync and physical environment recipe.
- Performance stabilization: Warmed the sticker PBR program at renderer construction, moved PNG decode to `createImageBitmap`, removed per-pointer material recompilation, cached identical initial projector geometry, reused one default center raycast across a batch, coalesced intermediate decal builds, and deferred one runtime history command until pointer release. The strict media-area floor is one real 1920x1080 PNG; direct manipulation uses one selected 1024px decal on the detailed model at 2x backing under the unchanged 120 ms frame-gap / 250 ms long-task / 2000 ms interaction budgets. Multi-file order/transform/remove and a two-decal 14-step drag are covered by the separate functional test because the built-in file reader commits each file as its own media transaction.
- Files changed: Product spec/plan; schema; new sticker state module; surface shader environment pars; scene/preview/export renderers; acceptance/performance inventories and tests; functional/performance browser tests; this worklog.
- Verification: Typecheck and focused source/product/contract tests pass; curved-model browser acceptance passes batch upload, visible stacking, thumbnail reorder, rotate/flip, direct drag, orbit attachment, Warm lighting, timeline playback, PNG/WebM export, remove, section reset, and global reset. The strict 1920x1080 media-area import and one-decal 2x surface-drag budgets pass; stress preview, animated canvas drag, viewport zoom, and export budgets pass, and the isolated 120-frame animation rerun passes after one non-repeating mixed-run 184 ms cold sample. `npm run verify:final` passes 268/268 unit/contract tests, production build, and 24/24 browser acceptance tests.
- Skipped checks: The full post-first-working performance checkpoint is not required. All sticker-specific paths and shared preview/animation/viewport/export paths touched by this feature have targeted budget evidence.
- Risks: Three's documented planar decal projector can distort around very sharp corners or project across nearby opposing faces when the projector depth intersects both. The default footprint is fixed from PNG aspect ratio because size/rotation controls were not requested.

### Iteration 13 — Preserve sticker PNG brightness and saturation

- Request: Stickers render too dull; do not apply brightness to their raster artwork.
- Task type: Tier 3 renderer visual-regression correction using brainstorming, writing-plans, systematic-debugging, and real browser verification.
- User-visible result: PNG stickers retain their original bright sRGB color and alpha instead of being dimmed or whitened by environment intensity and ACES tone mapping; HDRI remains only as a very weak additive surface reflection.
- Source/reference checked: Current `createStickerMaterial` physical recipe, Three r185 `meshphysical` emissive/specular shader chunks, global ACES renderer settings, and a generated solid `#FF4A65` PNG on the curved OBJ browser fixture.
- Reference inputs: The user's report that uploaded stickers look dull; no new external visual reference was supplied for this correction.
- Docs/contracts read: `workflow.md`, runtime boundary, decision contract, renderer technique, acceptance testing, core/app performance, plus the required brainstorming, writing-plans, and systematic-debugging skills.
- Contract rules applied: `canvas-no-app-ui`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `workflow-required`.
- Decision: Keep `MeshPhysicalMaterial` for geometry, transparency, and weak surface reflection, but bind the sRGB PNG to `emissiveMap` at full intensity, keep the same `map` for alpha, make diffuse color black, set metalness to zero, reduce clearcoat/base specular energy, and disable material tone mapping.
- Alternatives rejected: `MeshBasicMaterial` would remove HDRI response entirely; increasing renderer exposure would alter Liquid Metal and the background; raising the PNG texture values would clip colors and still leave them environment-dependent; fully lit diffuse/PBR color reproduced the reported dull/white result.
- State/output mapping: `media.stickers` still supplies one decoded sRGB texture to the shared sticker material factory; preview, PNG, and video instantiate that factory, so the color-preserving emissive base and weak reflection are identical across outputs without new state or controls.
- Files changed: Product spec/plan; sticker scene material; sticker acceptance wording; renderer-source test; functional browser source-color sampling; this worklog.
- Verification: The pre-fix solid-red browser fixture reproduced the defect with red-channel dominance of only `14` against the `85` acceptance floor. After the correction, TypeScript and `git diff --check` pass, 66 focused source/product/acceptance tests pass, the dedicated 20-second curved-model browser scenario passes source-color fidelity plus reorder/transform/drag/Warm/PNG/WebM/remove/reset behavior, and `npm run verify:quick` passes 268/268 unit/contract tests.
- Skipped checks: The full performance checkpoint and targeted workload scenarios are not required because this fixed material parameter change adds no decode, projection, draw, animation, viewport, or export work; the existing functional export path ran in the dedicated browser scenario.
- Risks: Very bright HDRI highlights can still add a deliberately small reflection above the PNG base, but Environment Intensity no longer multiplies, darkens, or tone-maps the source artwork.

### Iteration 14 — Object-space grayscale scratch depth

- Request: Add a grayscale scratch mask that follows the model normal and gives scratches apparent depth, similar to a texture/matcap workflow.
- Task type: Tier 3 schema/media/WebGL/preview-export feature using brainstorming, writing-plans, systematic-debugging, and real browser verification.
- User-visible result: Adjacent Scratch Mask and Surface Scratches sections let the user upload one grayscale raster, set Depth and Scratch scale, invert black/white height polarity, and rotate/flip the source. The scratches remain attached under model orbit/scale and bend Liquid Metal HDRI reflections in preview, PNG, and video without changing geometry or sticker colors.
- Source/reference checked: Installed Three r185 `MeshPhysicalMaterial`, `bumpmap_pars_fragment` Mikkelsen surface-gradient implementation, current object/world/view normal pipeline, Toolcraft single-image media transform behavior, and generated asymmetric grayscale/curved-model browser fixtures.
- Reference inputs: The user's description of a grayscale scratch mask with depth; no external image, video, URL, or authored scratch texture was supplied.
- Docs/contracts read: `workflow.md`; control selection/layout; schema/component rules; runtime boundary; setup/export; media upload; renderer technique; acceptance; decision contract; core/app performance; required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `controls-product-coverage`, `controls-layout-heuristics`, `canvas-no-app-ui`, `canvas-surface-preserved`, `layers-enable-only-when-needed`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Use component-owned single-image fileDrop in Scratch Mask and built-in Depth/Scratch scale/Invert controls in adjacent Surface Scratches. Decode as non-color repeat texture; blend `YZ`, `ZX`, and `XY` samples from object-local position and object-normal weights; differentiate the blended height in screen space; perturb the existing rippled physical view normal with a tangent-free Mikkelsen-style gradient.
- Alternatives rejected: A grayscale matcap is view-space baked lighting rather than height and would swim/change meaning under orbit or HDRI; UV normal/bump maps fail on arbitrary OBJ/STL without authored unwraps; screen-space projection slides over the model; geometry displacement would require subdivision and alter the silhouette; a custom control is unnecessary because fileDrop/sliders/switch own the value models.
- State/output mapping: `media.scratches` owns image bytes, identity, rotate/flip transforms, removal, and reset; `surface.scratchDepth`, `surface.scratchScale`, and `surface.scratchInvert` map through typed settings to stable uniforms; preview and isolated PNG/video renderers call the same stale-safe `setScratchMask` and material factory; stickers remain a separate top decal material.
- Files changed: Product spec/plan; schema; new scratch state module; Liquid Metal values, surface shader, scene/React/export renderers; readiness/section inventory/acceptance/performance configs; source/product/schema tests; functional/performance browser tests; this worklog.
- Verification: Initial contract tests exposed fileDrop standalone-section normalization and Scratch scale workload classification; the spec/schema/inventory now model those rules directly. Initial browser shader compilation exposed unavailable fragment-stage `modelMatrix`; object normal now crosses the vertex boundary explicitly. Pixel diagnostics also isolated controls-panel overlay from canvas output and the final test samples only the unobstructed canvas region. TypeScript, diff validation, the focused 100-test contract set, dedicated 19.2-second scratch acceptance, unchanged 19.9-second sticker regression, production-size mask import, Depth, maximum Scratch scale at 2x, Invert budgets, and `npm run verify:quick` with 274/274 tests pass; saved app identity remains live at port 3005.
- Skipped checks: The full performance checkpoint is not required for a post-first-working feature. Targeted mask import uses the full 1920x1080 hard limit at ratio 1; Scratch scale uses its maximum `20` over a detailed model, 1920x1080 mask, and 2x backing at ratio 1; Depth and Invert receive live responsiveness budgets; the functional test covers real PNG/WebM export.
- Risks: Height changes shading normals only and cannot cut the silhouette. Triplanar blending can soften strokes around equal-axis regions, while mipmapping at the maximum scale intentionally trades subpixel line sharpness for stable reflections.

### Iteration 15 — Procedural studio HDRI presets

- Request: Add studio HDRI presets.
- Task type: Tier 3 schema/WebGL IBL feature using brainstorming, writing-plans, and real browser verification.
- User-visible result: Environment Source now offers Studio, Softbox, Product, Rim, Chrome, Neutral, Warm, and Custom HDRI. The four new rigs create broad softbox, balanced product, edge-rim, or narrow chrome-strip reflections on the real 3D surface; Intensity and Environment rotation continue to work for every source.
- Source/reference checked: Existing Three.js `RoomEnvironment` PMREM and direct equirectangular physical-radiance pipeline; current procedural Neutral/Warm generator; preview, PNG, and video environment setup; Toolcraft select/acceptance/performance contracts.
- Reference inputs: The user's request for studio HDRI presets; no external URL, image, video, or photographic HDRI asset was supplied for this pass.
- Docs/contracts read: `workflow.md`; control selection/layout; schema/component rules; runtime boundary; renderer technique; acceptance testing; decision contract; core/app performance; required brainstorming, writing-plans, and browser skills.
- Contract rules applied: `controls-product-coverage`, `controls-layout-heuristics`, `canvas-no-app-ui`, `canvas-surface-preserved`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Keep Studio on the cached RoomEnvironment PMREM and Custom on worker-decoded HDR/EXR. Generate Softbox, Product, Rim, and Chrome as typed float equirectangular IBL recipes composed from linear-radiance panels, with wrapped horizontal distance at the map seam and one cached GPU texture per source.
- Alternatives rejected: Bundling downloaded HDRI files adds network/licensing/asset weight; matcaps bake a view-space look instead of lighting the model through its reflected normal; UV textures would tile artwork on geometry; new upload or custom UI is unnecessary because the existing Source select owns this finite choice.
- State/output mapping: `lighting.environmentPreset` parses to the extended `LiquidMetalEnvironmentPreset`; `setEnvironment` prepares/caches the matching recipe and drives the existing direct-radiance/GGX uniforms; Intensity and Rotation remain uniform-only changes; preview, PNG, and video call the same environment settings and renderer path.
- Files changed: Product spec/plan; environment schema/types; scene IBL recipe generator; acceptance inventory; product/source/performance tests; functional and performance browser scenarios; renderer-pipeline evidence; this worklog.
- Verification: TypeScript and diff validation pass; 72 focused schema/product/renderer-source tests pass; `npm run verify:quick` passes 274/274; the 21.0-second Environment browser test proves distinct pixels for all eight sources plus existing HDR upload/reset and PNG export; the targeted Chrome preset-switch performance scenario passes in 7.3 seconds; `npm run dev` verifies the saved app identity at `http://127.0.0.1:3005/`.
- Skipped checks: The full performance checkpoint is not required for this post-first-working non-performance feature. The existing `lighting.environmentPreset` scenario directly measures the changed recipe preparation/composite path, and no other workload, viewport, media-decode, timeline, or export-size path changed.
- Risks: The four new built-ins are designed procedural studio IBL rigs rather than photographed locations. Custom HDRI remains available when real-world photographic reflections are required; the 256x128 float maps intentionally favor smooth studio panels over fine environment detail.

### Iteration 16 — Connected-surface sticker projection, scale, and rotation

- Request: Fix the supplied `A.obj` case where a PNG sticker stretches and slices across bevel/side geometry, and add per-sticker rotation together with the proposed size control.
- Task type: Tier 3 schema/state/direct-manipulation/WebGL projection correction using brainstorming, writing-plans, systematic debugging, and real browser verification.
- User-visible result: Every sticker now has independent `Sticker scale` and continuous `Sticker rotation` controls. The decal stays on the connected, normal-compatible surface island under its placement point instead of capturing a perpendicular sidewall or disconnected letter stroke. Dragging preserves both transforms; preview, PNG, and video share the same values.
- Source/reference checked: Exact user files, supplied static screenshot, Three r185 `DecalGeometry` source and its documented sharp-corner distortion warning, current sticker raycast/placement/export path, and the existing curved-cylinder functional fixture.
- Reference inputs: `/Users/kusnizza/Desktop/A.obj`; `/Users/kusnizza/Desktop/FW_Premade_Sticker_31.png`; the user-supplied static PNG before-state screenshot.
- Root-cause evidence: The reproduced fixed `0.82 × 0.763 × 0.52` box projector generated 244 triangles on `A.obj`: 49 front, 158 bevel, and 37 side-facing. The original algorithm accepted all clipped triangles from every source mesh, so projector volume—not the clicked surface topology—decided the sticker shape.
- Docs/contracts read: `workflow.md`; control selection/layout; runtime boundary; schema/component rules; media upload; setup/export; renderer technique; acceptance; decision contract; core/app performance; required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `controls-product-coverage`, `controls-layout-heuristics`, `canvas-no-app-ui`, `canvas-surface-preserved`, `layers-enable-only-when-needed`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Keep Three's triangle clipping, then reject triangles beyond a 72° normal cone, build quantized shared-edge adjacency, seed at the triangle closest to the raycast anchor, retain only that connected component, and keep only the closest valid component across source meshes. Scale changes projector width/height; rotation turns its local surface plane; depth is proportional and clamped to `0.08..0.3` instead of the former `0.52`.
- Placement/default decision: `stickers.placements` now serializes normalized position, normal, scale, and rotation per media id. Old maps import as scale `1`/rotation `0°`; new stickers default to scale `0.4`. If the center ray misses a hollow model, the first fallback is `x=-0.24`, which the supplied `A.obj` verified as the center of its left stroke rather than the inner-hole bevel.
- Alternatives rejected: A screen overlay or flat plane would not conform; UV projection requires authored unwraps and can tile; matcap is lighting, not decal placement; keeping every `DecalGeometry` triangle reproduces sidewall distortion; bridging disconnected triangles would paint over real holes; geometry displacement is unrelated to raster sticker artwork.
- State/output mapping: Built-in sliders write `stickers.scale` and `stickers.rotation` as the selected-sticker proxy. Selection loads that sticker's serialized transform; no selection defines the next import defaults. Live slider updates go directly to the retained scene entry, coalesce one geometry build per render frame, and commit the placement map after 160 ms; switching selection flushes the prior value. Export overlays the current proxy on the selected placement so an immediate PNG/video action cannot miss the debounced commit.
- Performance stabilization: Initial direct placement already met 120 ms, but schema slider changes caused duplicate runtime passes and transient projection caches with 166–208 ms frame gaps. Direct scene updates plus coalesced serialized commits brought `stickers.scale`, `stickers.rotation`, and surface drag back under the mandatory 120 ms browser budget without lowering render scale or output quality.
- Files changed: Product spec/plan; `app-schema.ts`; sticker state, scene, React preview, and export renderers; readiness/section inventory/acceptance/performance configs; product/source/schema/functional/performance tests; this worklog.
- Verification: `npm run typecheck` passes; focused source/product/schema/acceptance tests pass; `npm run verify:quick` passes 284/284 tests on the combined workspace. The full curved-sticker browser scenario passes batch stack/reorder/source transform, independent selected scale/rotation, surface drag transform preservation, orbit, HDRI, PNG/video, removal, section reset, and global reset. Targeted `stickers.scale`, `stickers.rotation`, and surface-drag browser performance scenarios pass their declared budgets.
- Exact-file browser evidence: The supplied `A.obj` plus PNG passes with no shader/GL errors. Default scale `0.4` at the revised hollow-model fallback shows the complete triangular sticker centered on the left front stroke; scale approximately `0.31` plus rotation approximately `33°` visibly produces a smaller rotated sticker that remains entirely on the same front island. The former stretched sidewall bands are absent.
- Skipped checks: The full performance checkpoint is not required for this post-first-working behavior correction. The directly changed media projection, two controls, direct drag, shared output, and exact user-file regression received functional and targeted performance coverage.
- Risks: A true mesh hole or disconnected component intentionally crops a sticker that is made too large; the product now exposes scale so the user can fit it. Smooth curvature and bevels within the normal cone remain connected, while turns sharper than 72° are intentionally treated as a boundary.

### Iteration 17 — ASCII Tool six-axis orientation gizmo port

- Request: Find the element shown in the supplied CleanShot inside `/Users/kusnizza/Projects/toolkit-tests/ascii-tool` and completely transfer its behavior into Liquid Metal.
- Task type: Tier 3 reference-behavior port using brainstorming, writing-plans, systematic debugging, and controlled browser verification.
- User-visible result: A fixed lower-left 70px orientation sphere exposes ±X/±Y/±Z hover points, 600ms axis snap, and direct endpoint drag. The same camera pose is used by middle-button orbit, sticker raycasting, undo/redo, Model Size reset, PNG, and video.
- Source/reference checked: Supplied static PNG reference; `ascii-tool/src/app/renderer/orientation-gizmo-control.tsx`; `ascii-tool/src/app/renderer/orbit-camera.ts`; the source unit/browser tests, plans, and worklog iterations 6–10.
- Reference inputs: Exact 70×70 CSS / 140×140 DPR2 geometry; 16px insets; separate opaque backing and transparent axes canvas; X/Y/Z colors; front/rear alpha; rear-to-front sort; hit radii; 3px threshold; 600ms easing; pointer-sphere clamping; hemisphere/radius preservation; merged gesture history; empty-circle inertia; export exclusion.
- Docs/contracts read: `workflow.md`; reference study; runtime boundary; assembly workflow; schema/component/control selection; acceptance; renderer technique; core/app performance; decision contract; required brainstorming, writing-plans, systematic-debugging, and browser skills.
- Contract rules applied: `canvas-handle-placement`, `canvas-no-app-ui`, `controls-product-coverage`, `renderer-technique-inventory`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, `output-export-required`, `persistence-policy-explicit`, `workflow-required`.
- Decision: Port the source Canvas2D/DOM handle and orbit math as app-owned modules, store `{position, up}` at `view.orbit`, preserve Liquid Metal's existing 4.6 camera radius and fixed model base rotation, and make preview plus isolated export renderers read that one runtime value. Keep middle-button canvas orbit because left drag already owns sticker placement.
- Alternatives rejected: A visual-only overlay over `{x,y}` Euler rotation cannot represent six axis snaps, authored up/roll, history/reset, or exact endpoint tracking; importing the other project or its shell would couple apps and violate the runtime boundary; embedding the handle in product output would leak editor UI into export.
- Systematic-debugging evidence: Runtime file controls normalize into their own section, so the portal-only control belongs to `Model Size`, avoiding duplicate `Model` sections. Toolcraft theme state is scoped at `data-toolcraft-theme`, so the backing observes that scope instead of the document root. The 4K-like functional test pauses playback before pointer assertions to avoid software-WebGL action latency, while playback and the animated render-scale-2 performance scenario remain separately exercised.
- State/output mapping: The custom schema control and middle-drag dispatch `controls.setValue` with merged history; the persistent scene applies camera position/up/look-at before rendering and raycasting; PNG/video action state supplies the identical pose to the isolated renderer; `data-view-orbit` provides a stable browser observable.
- Files changed: Gizmo spec/plan; `liquid-metal-orbit.ts` and tests; `liquid-metal-orientation-gizmo.tsx`; schema/route; preview/scene/export; acceptance/performance matrices and tests; functional/performance browser coverage; this worklog.
- Verification: `npm run verify:quick` passed 284/284 unit and contract tests. Controlled browser verification proved exact dimensions/insets, transparent axes over matching backing, representative +X hover/snap, clamped direct drag with changed up/roll, one-step undo/redo, Model Size reset, dark/light backing, and fixed placement after zoom. Focused Playwright passed the complete canvas/timeline/orbit/settings scenario, real PNG/Current/4K video and background export scenario, and `browser perf: view.orbit remains responsive` under the declared 120ms frame-gap / 2000ms interaction / 250ms long-task budgets.
- Skipped checks: `npm run verify:final` and the full performance checkpoint are not required for this post-first-working non-performance feature. Unchanged shader, media-import, layer, and unrelated workload paths remain covered by the existing product baseline.
- Risks: None specific to the port; video container fallback and GLTF sidecar constraints remain unchanged.

### Iteration 18 — Stable grabbed endpoint at the sphere boundary

- Request: Update Liquid Metal to match the newly revised behavior of the orientation element in the local ASCII app.
- Task type: Tier 3 custom canvas-handle visual-regression diagnosis, boundary camera-math correction, observable browser regression coverage, and targeted `view.orbit` performance verification.
- User-visible result: A grabbed colored endpoint now keeps one stable front/rear hemisphere, opacity, and depth order when the pointer reaches or moves beyond the orientation sphere. Direct tracking, click snap, 70px design, separated backing, camera radius, history, preview, raycasting, and export behavior are unchanged.
- Source/reference checked: The updated local ASCII `orbit-camera.ts`, its orbit unit tests, focused Playwright alpha sampling, boundary-stability plan, and worklog iteration 12; the orientation-gizmo component itself was unchanged in the source update.
- Reference inputs: Updated executable camera math and automated behavior evidence from the local ASCII project; no new external reference was required.
- Docs/contracts read: `workflow.md`; reference study; runtime boundary; assembly workflow; decision contract; schema; control selection/layout; custom controls; component rules; setup/export; acceptance; renderer technique; core/app performance; and required brainstorming, writing-plans, systematic-debugging, project-browser, and in-app-browser skills.
- Contract rules applied: `canvas-handle-placement`, `controls-product-coverage`, `renderer-technique-inventory`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- Root cause: Outside-pointer projection clamped the radial coordinate to exactly `1`, making the captured camera-local Z exactly zero. The pre-fix Liquid Metal diagnostic returned the same approximately `-2.22e-16` projected depth for both requested hemisphere signs, so Canvas2D front/rear opacity and sort semantics could lose or flip the captured state at the boundary.
- Decision: Keep a minimum signed camera-local Z magnitude of `1e-4` and clamp radial length to `sqrt(1 - 1e-8)`, matching the revised ASCII math. This preserves the pointer-down hemisphere while moving the endpoint only about `1.225e-7` CSS pixels inward, below any displayable distance. Keep the existing paint rules and correct their unstable mathematical input.
- Alternatives rejected: Forcing the active point permanently front-facing would mask incorrect depth state; adding only a paint epsilon would leave the camera pose singularity; throttling pointer events would reintroduce lag; changing the backing, DPR, WebGL preview quality, or exports would not address the camera-local zero.
- State/output mapping: `getLiquidMetalOrbitPoseFromGizmoPointer` maps the pointer to a stable signed sphere direction and writes the unchanged `view.orbit` runtime target through the existing merged history group. `projectLiquidMetalOrbitAxes` then produces stable depth for the unchanged Canvas2D draw pass; Three preview, sticker raycasting, PNG/video export, reset, undo/redo, and settings transfer continue to consume the same pose shape.
- Files changed: Boundary-stability spec/plan; `liquid-metal-orbit.ts` and tests; functional browser acceptance; acceptance/performance wording; and this worklog.
- Verification: `npm run typecheck` passed and `npm run verify:quick` passed 50 files with 289 Vitest tests. The focused orbit suite passed 8 tests, including 30 repeated edge projections for each captured hemisphere; the post-fix diagnostic measured minimum absolute projected depth above `0.0001` with one correct sign per hemisphere. The complete canvas/timeline/orbit Playwright scenario passed with 12 held-boundary alpha samples at one constant bright value, and targeted `browser perf: view.orbit remains responsive` passed its unchanged budget. Controlled-browser verification recorded nine alternating boundary gestures with one depth sign, minimum absolute depth `0.00009999999999988271`, a continuously connected unchanged backing, visible colored points, and zero warning/error logs; the camera and playback state were restored afterward.
- Skipped checks: `npm run verify:final` and the full performance checkpoint are not required for this post-first-working, narrowly scoped visual interaction correction. Media, export bytes, dependencies, timeline, layers, persistence, and unrelated renderer workloads are unchanged.
- Risks: None for visible geometry: the approximately `1.225e-7` CSS-pixel boundary inset cannot be rasterized, while the non-zero signed depth removes the ambiguity.

### Iteration 19 — Connected-face sticker wrapping

- Request: Fix the case where a sticker is cut off when it crosses from one face of the supplied model onto an adjacent face.
- Task type: Tier 3 custom WebGL geometry, direct-manipulation, preview/export, acceptance, and targeted performance correction using brainstorming, writing-plans, systematic debugging, and real browser verification.
- User-visible result: A PNG sticker now continues over genuinely connected bevels and hard edges while preserving its proportions and continuous artwork. It still stops at real holes, disconnected components, open boundaries, and non-manifold seams; scale, rotation, drag, HDRI response, stacking, and exports remain shared.
- Source/reference checked: The exact supplied model and sticker, the user-supplied static PNG showing the clipped face transition, the previous normal-cone/box-projector implementation, Three.js triangle/raycast conventions, manifold shared-edge topology, the existing curved-cylinder scenario, and a dedicated two-face hard-edge fixture.
- Reference inputs: `/Users/kusnizza/Desktop/A.obj`; `/Users/kusnizza/Desktop/FW_Premade_Sticker_31.png`; the user-supplied static PNG before-state showing the face-transition crop.
- Root-cause evidence: The previous global `72°` placement-normal filter measures accumulated turn from the seed normal rather than local turn across each shared edge. On `A.obj`, adjacent bevel triangles turn gradually but eventually exceed that global cone, so valid connected faces are discarded. Increasing the cone or projector depth alone restores planar `DecalGeometry` collapse on perpendicular faces because their UV width approaches zero in one projector plane.
- Docs/contracts read: `workflow.md`; runtime boundary; decision contract; renderer technique; acceptance testing; core/app performance; required brainstorming, writing-plans, systematic-debugging, project-browser, and in-app-browser skills.
- Contract rules applied: `canvas-no-app-ui`, `canvas-surface-preserved`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `output-export-required`, `persistence-policy-explicit`, and `workflow-required`.
- Decision: Cache canonical source triangles and exact two-owner shared-edge adjacency when the model loads. Preserve the raycast mesh/face seed in placement state, unfold neighboring triangles into a local sticker chart by shared-edge lengths, choose the shortest local chart path, clip in chart space, and interpolate the original 3D positions/normals with continuous chart UVs. Bound traversal to the sticker footprint and reject only effectively folded-back transitions near `170°`.
- Alternatives rejected: A larger normal threshold/deeper box still collapses UVs around corners; per-face planar decals create artwork seams; triplanar/matcap projection tiles or blends raster art instead of preserving one sticker; UV mapping requires an authored unwrap; connecting by proximity paints across holes and neighboring letter strokes; screen overlays and flat planes do not conform to geometry.
- State/output mapping: `stickers.placements` retains backward-compatible position/normal/scale/rotation and now also records optional `surfaceMeshIndex`/`surfaceFaceIndex`. Older placements fall back to nearest compatible triangle. Preview and isolated PNG/video scenes build the same topology and chart geometry; existing media order and color-stable physical sticker material remain unchanged.
- Performance stabilization: Surface topology is built once per model, local traversal stops outside the footprint, same-sized position/normal/UV buffers update in place, and interactive drag/scale/rotation redraw only the union of the sticker's previous and next full-resolution screen bounds. The preview keeps its drawing buffer for safe scissored compositing; model, camera, HDRI, scratch, timeline, sticker-list, texture, and order changes force a full frame.
- Files changed: Product spec/plan; new sticker surface-chart geometry module and unit tests; sticker placement serialization; shared scene and React preview renderer; acceptance/performance inventories and renderer-source tests; functional browser coverage; this worklog.
- Verification: `npm run typecheck` and `npm run verify:quick` pass, with the latter covering 50 files and 289 tests. The focused topology/source suite passes 24 tests, including a `90°` shared edge, disconnected surfaces, and non-manifold adjacency. The dedicated hard-edge Chromium scenario passes in 12.3 seconds; the complete 27.2-second sticker batch/stack/source-transform/scale/rotation/drag/orbit/HDRI/export/remove/reset scenario also passes. The exact `A.obj` plus supplied PNG was verified in Chromium without shader/WebGL errors and produced `/tmp/liquid-metal-a-wrapped-sticker.png`. Targeted scale, rotation, and surface-drag performance scenarios all pass the existing 120 ms frame-gap / 2000 ms interaction budgets without lowering render scale or output quality.
- Skipped checks: The full performance checkpoint and `verify:final` are not required for this post-first-working renderer correction. The changed topology, exact user files, hard-edge pixels, complete sticker flow, and all three affected interaction workloads received focused unit and browser coverage; `npm run verify:quick` is the final contract gate for this pass.
- Risks: Extremely dense local topology still performs main-thread chart traversal, capped at 12,000 triangles per sticker build. A non-manifold or open seam intentionally remains a boundary because there is no unambiguous adjacent surface. Near-closed surfaces use the shortest discovered chart path to avoid duplicate overlap.

### Iteration 20 — Scale-relative sticker topology conditioning

- Request: Correct the supplied model behavior after sticker fragments appeared as separate pieces at different placements, and determine whether imported geometry should be conditioned on load.
- Task type: Tier 3 model-preprocessing and custom sticker-geometry regression correction using brainstorming, writing-plans, systematic debugging, and real browser verification.
- User-visible result: Imported meshes keep narrow but valid bevel triangles in the sticker surface graph, so one PNG remains one continuous, draggable sticker instead of breaking into detached pieces. The rendered model geometry, normals, materials, reflections, holes, and silhouette remain unchanged.
- Source/reference checked: The exact supplied OBJ and sticker; three user-supplied static PNG stills showing detached sticker pieces; the canonical sticker topology builder; OBJ triangle/edge diagnostics; the existing connected-hard-edge browser fixture; and the scale, rotation, and surface-drag performance scenarios.
- Reference inputs: `/Users/kusnizza/Desktop/A.obj`; `/Users/kusnizza/Desktop/FW_Premade_Sticker_31.png`; three static PNG stills identified by the supplied local media paths.
- Docs/contracts read: `workflow.md`; runtime boundary; media upload; decision contract; renderer technique; acceptance testing; core/app performance; required brainstorming, writing-plans, systematic-debugging, project-browser, and in-app-browser skills.
- Contract rules applied: `canvas-no-app-ui`, `canvas-surface-preserved`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `output-export-required`, and `workflow-required`.
- Root cause: `A.obj` is already a valid closed manifold after the existing center-and-scale import pass: 2,220 valid triangles, 3,330 two-owner edges, zero degenerate triangles, and one connected component. The sticker topology builder nevertheless compared an unnormalized face-normal magnitude against the absolute `1e-7` chart epsilon. That discarded 1,688 small but valid bevel triangles, leaving only 532 triangles, 500 artificial boundary edges, and two chart components; the sticker fragments were a preprocessing bug rather than malformed source geometry.
- Decision: Condition only the cached canonical sticker topology. Classify triangle degeneracy with a scale-relative area-squared ratio derived from its longest edge, and keep separate tolerances for chart comparisons, minimum unfold-edge length, and normal normalization. Preserve exact shared-edge ownership and continue rejecting truly collinear faces, open boundaries, disconnected components, and non-manifold seams.
- Alternatives rejected: Welding or remeshing the visible model could alter hard normals, bevel shading, materials, face indices, and silhouette; indiscriminately filling holes would paint across intentional letter cutouts; lowering one global absolute epsilon would keep the tolerance coupling and remain scale-dependent; proximity-based adjacency would bridge nearby but disconnected surfaces.
- State/output mapping: Model import still creates the same normalized render geometry. In parallel, the cached sticker surface builder now retains every scale-valid source triangle and supplies the corrected one-component chart to preview plus isolated PNG/video renderers. Sticker media, selected scale/rotation, placement serialization, direct drag, order, and export state are unchanged.
- Files changed: Sticker-topology conditioning spec/plan; `liquid-metal-sticker-geometry.ts` and focused tests; connected-hard-edge browser fixture; acceptance/performance inventory wording and renderer-source guard; this worklog.
- Verification: The new uniformly small folded-surface test's pre-fix red phase retained zero triangles; after correction it retains all four triangles, while the exact-collinear regression remains excluded. The focused geometry suite passes 6/6, `npm run typecheck` passes, and `npm run verify:quick` passes 50 files with 291 tests. The exact `A.obj` diagnostic now retains all 2,220 triangles in one sticker component. The hardened connected-hard-edge Chromium scenario passes in 11.8 seconds. Exact-file Chromium upload shows one 9,166-pixel connected sticker component, and the sticker remains visually whole after direct surface drag. Targeted `stickers.scale`, `stickers.rotation`, and surface-drag performance scenarios pass 3/3 in 30.1 seconds under their existing budgets.
- Skipped checks: `npm run verify:final` and the full performance checkpoint are not required for this post-first-working focused renderer correction. The changed import preprocessing, exact user files, hard-edge pixels, and all three affected interaction workloads received targeted coverage; `npm run verify:quick` remains the contract gate for this pass.
- Risks: Intentionally malformed, exactly degenerate, open, disconnected, or non-manifold input still creates a sticker boundary rather than guessing a repair. The visible render mesh is deliberately not welded or remeshed.

### Iteration 21 — Model-hit left-drag orbit and empty-canvas pan

- Request: Port the updated ASCII Tool behavior so a mouse drag over the object rotates it without the middle button, while a drag outside the object moves the Toolcraft canvas.
- Task type: Tier 3 reference-behavior port using brainstorming, writing-plans, systematic debugging, project-browser, and in-app-browser workflows.
- User-visible result: An unmodified left drag on the top visible sticker continues to move that sticker; otherwise a left drag on the rendered base model rotates the shared `view.orbit` camera. A left drag that misses the model is not captured by the renderer and pans the Toolcraft canvas. Middle drag no longer changes the camera pose.
- Source/reference checked: `/Users/kusnizza/Projects/toolkit-tests/ascii-tool/src/app/renderer/effects-canvas.tsx`; ASCII Tool orientation-gizmo parity plan; ASCII Tool worklog iteration 6; the current Liquid Metal sticker picker, Three.js scene raycaster, and Toolcraft canvas pointer ownership.
- Reference inputs: The local ASCII Tool project and the user's explicit interaction description. No video reference was supplied; the reference behavior is documented in source, plan, and worklog evidence.
- Docs/contracts read: `workflow.md`; reference study; runtime boundary; assembly workflow; schema reference; acceptance testing; decision contract; core/app performance; renderer technique; required brainstorming, writing-plans, systematic-debugging, project-browser, and in-app-browser skills.
- Contract rules applied: `canvas-no-app-ui`, `canvas-surface-preserved`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- Decision: Lock gesture ownership on pointerdown with priority `sticker → base-model raycast → Toolcraft canvas`. Only an unmodified primary-button gesture can claim sticker drag or model orbit. The scene reuses its existing camera, raycaster, and cached base meshes for the hit test; a miss bubbles unchanged to the runtime viewport. Existing merged `view.orbit` history remains the camera transaction.
- Alternatives rejected: Claiming the whole product canvas for orbit would prevent empty-space pan; retaining middle-button orbit would contradict the requested reference behavior; alpha/pixel hit testing would be unstable under transparent shader pixels and render scale; patching the shared Toolcraft runtime would change unrelated generated apps.
- State/output mapping: Model-hit pointer deltas write the same schema-backed `view.orbit` pose used by preview, sticker raycasts, undo/redo, PNG, and video. Empty-space pointer events leave product state untouched and update only Toolcraft canvas world position. Sticker drag retains the existing placement map and selection semantics.
- Files changed: Interaction spec/plan; `liquid-metal-scene.ts`; `liquid-metal-renderer.tsx`; acceptance/reference inventory; renderer source tests; browser acceptance; performance scenario/pipeline coverage; this worklog.
- Verification: `npm run ai:check`, TypeScript, and `npm run verify:quick` pass; the quick gate covers 50 Vitest files with 292 tests. Five focused Vitest files pass 39 tests. Dedicated Chromium acceptance proves left model orbit changes pose without moving the canvas, middle drag is inert, and a guaranteed empty-canvas left drag pans without changing pose. The full PNG-sticker Chromium workflow proves sticker drag still has priority. Targeted `view.orbit` performance covers both gizmo drag and model-surface left drag under the existing budget. Controlled in-app browser inspection confirms the current app shell renders at `http://127.0.0.1:3005/` with no console warnings or errors.
- Diagnostic notes: The initial public performance draft used `viewport-drag`, which is pipeline-only; the corrected implementation extends the existing typed `view.orbit` scenario. Early coordinate probes landed outside the visible viewport or on the full-frame cube; the final fixture reduces model scale before choosing the viewport corner so the test proves a real raycast miss.
- Skipped checks: `npm run verify:final` and the full performance checkpoint are not required for this post-first-working focused interaction port. Renderer behavior, canvas ownership, sticker priority, acceptance mapping, and the touched orbit workload receive targeted coverage; `npm run verify:quick` remains the contract gate for this pass.
- Risks: The model hit test runs once per pointerdown and is proportional to the loaded mesh raycast cost. The targeted detailed-model orbit performance scenario remains within budget; extremely dense uncached raycasting geometry may still make gesture start cost scale with triangle count.

### Iteration 22 — Authored default preloaded scene

- Request: Make the user's saved Liquid Metal scene the application default, including its settings, model, scratch mask, and stickers; every preloaded asset must remain removable and the default must be recoverable.
- Task type: Tier 3 schema/media/renderer-startup/reset/browser behavior change using brainstorming, writing-plans, systematic debugging, project-browser, and in-app-browser workflows.
- User-visible result: The root app opens directly on the authored `A.obj` chrome scene with the original scratch texture and ten arranged stickers. The user can remove, replace, reorder, or transform any preloaded asset, while the relevant section Reset or global Reset reconstructs the authored scene. A 2K image can be exported immediately without uploading media first.
- Source/reference checked: `/Users/kusnizza/Downloads/liquid-metal-3d-settings (1).json`; `/Users/kusnizza/Downloads/liquid-metal-3d (8).png` at 4096x2304; `/Users/kusnizza/Desktop/A.obj`; the ten source PNGs in `/Users/kusnizza/Desktop/stickers`; and `/Users/kusnizza/Desktop/textures/Noise Scratches Black Background.jpg` at 5250x3000. File access-time evidence and visual comparison identified the original scratch source used during scene authoring.
- Reference inputs: The supplied settings JSON and adjacent final-render PNG; the exact local `A.obj`; `04_click_club.png`, `06_wow.png`, `08_fast_mode.png`, `12_yellow_smile_star.png`, `14_go.png`, `15_stick_it.png`, `19_keep_it_moving.png`, `33_play_loud.png`, `37_pizza.png`, and `50_blue_flower.png`; and the original `Noise Scratches Black Background.jpg`.
- Root-cause evidence: Toolcraft settings transfer intentionally serializes schema values, canvas, timeline, and orbit state but not media bytes. The supplied JSON therefore contains `media.model: null`, `media.scratches: null`, and `media.stickers: []`, and it contains no sticker surface placements. A settings import alone cannot reproduce the saved scene.
- Docs/contracts read: `workflow.md`; runtime boundary; setup/export; control selection; layout; media upload; schema/component rules; acceptance; decision contract; core/app performance; required brainstorming, writing-plans, systematic-debugging, project-browser, and in-app-browser skills.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `canvas-surface-preserved`, `controls-product-coverage`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- Decision: Declare the recovered files as typed `media.defaultAssets`, copy their original bytes into `public/assets/liquid-metal-default`, set the exact saved schema/canvas/orbit defaults, and seed the ten stickers with stable ids and canonical 16:9 NDC anchors raycast through the authored camera. Known default stickers use that canonical camera only for placement reconstruction; later uploads preserve the existing viewport-relative fallback.
- Alternatives rejected: Hidden renderer constants would bypass media controls and Reset; data URLs would inflate JavaScript and duplicate bytes; a 39 MB upscaled sticker set would add load cost without source detail; localStorage would make stale prior sessions override the requested default; a synthetic scratch texture would not reproduce the authored source; welding or changing model geometry is unnecessary and could alter the scene.
- State/output mapping: `media.defaultAssets` creates normal runtime media records for `media.model`, `media.scratches`, and ordered `media.stickers`. Exact JSON values initialize the schema, canvas, image/video export settings, and background; the exact orbit pose initializes `view.orbit`; stable default sticker ids map to recovered surface seeds. Preview, PNG, and video use the same media and renderer paths as user uploads. Section and global Reset restore defaults through the existing runtime command bus.
- Test isolation: Normal `/` always exercises the authored default. The explicit query-only `?toolcraft-test-fixture=empty-media` removes default assets for legacy upload/performance fixtures whose purpose is to measure a clean import transaction; it is never selected by product UI or normal startup.
- Files changed: Default-scene spec/plan; new `liquid-metal-default-scene.ts`; schema, orbit, scene, and route wiring; copied public assets; product, acceptance, performance, and browser tests; this worklog.
- Verification: `npm run verify:quick` passes 50 files and 293 tests. Dedicated Chromium acceptance proves exact model/mask/sticker inventory, visible GPU-applied output, individual removal plus section/global Reset restoration, and real 2048x1152 PNG export without upload. Existing model, scratch, wrap, and complete sticker workflows pass through the isolated neutral fixture. Targeted model and scratch media budgets cover responsive import/startup work independently from multi-worker browser wall-clock contention. Controlled in-app browser inspection shows `A.obj`, the original scratch JPEG, ten stickers, no model/scratch errors, and no console warnings/errors at `http://127.0.0.1:3005/`.
- Performance diagnostics: Playwright `retain-on-failure` tracing created a false 220 ms model-import long task; the same probe with tracing off measured a 33.3 ms maximum frame gap and no long task. Performance-only specs now disable tracing so the instrumentation does not perturb the measured frame budget. The independent arbitrary 1920x1080 sticker file-upload probe still measures the built-in file control's image-size decode rather than default-asset preload and is not used as evidence for this iteration.
- Skipped checks: The full performance checkpoint and `verify:final` are not required for this post-first-working content/default iteration. The final gate for this pass is `verify:quick`, dedicated default-scene acceptance, affected media regressions, clean-start timing, and targeted model/scratch workloads. The pre-existing arbitrary 1920x1080 user-upload stress case is outside the requested preload behavior and was not treated as a passing check.
- Risks: Because settings transfer omits sticker placements, the ten starting surface anchors were reconstructed from the 4096x2304 final render rather than recovered from serialized state. They are deterministic and visually verified, but they are an evidence-based reconstruction rather than original placement bytes.

### Iteration 23 — Authored default sticker scale parity

- Request: Check why sticker scale and placement no longer match the authored scene after the default-scene update.
- Task type: Tier 3 renderer-output regression diagnosis and default placement correction using systematic debugging, brainstorming, writing-plans, and controlled browser comparison.
- User-visible result: All ten preloaded stickers start with the authored `0.82` projector scale, restoring the larger overlapping composition. Their recovered surface anchors, camera, model normalization, ordering, selection, direct manipulation, reset, preview, and export paths are unchanged.
- Source/reference checked: `/Users/kusnizza/Downloads/liquid-metal-3d-settings (1).json`; adjacent 4096×2304 final export `/Users/kusnizza/Downloads/liquid-metal-3d (8).png`; current default scene at `http://127.0.0.1:3005/`; default seed table and projector-size implementation.
- Reference inputs: Saved `stickers.scale: 0.82`; export alpha and colored-sticker footprints; ten current seed NDC coordinates; `longEdge = 0.82 * placement.scale` projector rule.
- Root-cause evidence: Every seed was hard-coded to the obsolete generic import default `0.4`, making its projector long edge `0.328` instead of `0.6724`, exactly `2.05×` smaller. GO maps to `(1741, 518)` versus reference center `(1744, 518)`, and blue flower maps to `(2458, 933)` versus `(2442, 950)` on the 4096×2304 reference, so a global coordinate or camera correction would move already-aligned anchors.
- Docs/contracts read: `workflow.md`; decision contract; runtime boundary; media upload; schema/component rules; renderer technique; acceptance; core/app performance; required systematic-debugging, brainstorming, writing-plans, project-browser, and in-app-browser skills.
- Contract rules applied: `canvas-surface-preserved`, `controls-product-coverage`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- Decision: Define one authored default scale `0.82` for the ten reconstructed seeds, leave NDC coordinates and rotation unchanged, and expose resolved placement scales as a browser acceptance observable. Treat `0.82` as the minimal recoverable batch value because settings transfer did not serialize individual placement maps.
- Alternatives rejected: Moving every NDC seed would damage measured center parity; changing camera/model scale would alter the correctly aligned model silhouette and all raycasts; keeping `0.4` while only showing `0.82` in the control reproduces the state/output mismatch; inventing unproven individual scales is not supported by the saved JSON.
- State/output mapping: Known default media ids resolve their seed `{ndc, rotationDegrees: 0, scale: 0.82}` into normal `stickers.placements`; the existing selected-sticker proxy, scene geometry cache, direct drag, Reset, PNG, and video continue to consume that same map.
- Files changed: Default-scene spec and scale-parity plan; `liquid-metal-default-scene.ts`; product and browser regressions; renderer acceptance attribute; this worklog.
- Verification: `npx vitest run src/app/liquid-metal-product.test.ts` passes 53/53 and TypeScript passes. `npm run verify:quick` passes 50 files and 293/293 tests. The dedicated default-scene Chromium scenario passes with all ten resolved scales at `0.82`, remove/Reset restoration, and real 2048×1152 PNG export. Targeted `browser perf: stickers.scale live drag stays responsive` passes its declared budget in 8.6 seconds. Controlled in-app browser comparison at 60% maps the model and sticker centers to the 4096×2304 reference, restores the authored overlapping footprints, reports the exact ten-value `0.82` scale list, and has no console warnings/errors. GPU-applied scratch/sticker readiness is asserted separately from source inventory, while performance timing remains in the dedicated sequential perf suite instead of a multi-worker functional wall clock.
- Skipped checks: `verify:final` and the full performance checkpoint are not required for this post-first-working visual correction; the exact affected projector workload has dedicated targeted coverage.
- Risks: Settings transfer contains no original per-sticker placement bytes. The common `0.82` scale is the only serialized authoring value; any later evidence of an individual deviation should be handled as a per-sticker seed refinement rather than a global camera/coordinate change.

### Iteration 24 — Settings (2) default shader state

- Request: Make `/Users/kusnizza/Downloads/liquid-metal-3d-settings (2).json` the new default application state.
- Task type: Tier 2 schema-default and startup-output revision using brainstorming, writing-plans, product tests, and controlled browser verification.
- User-visible result: A clean load and Reset now start the existing complete `A.obj` sticker scene with Paper's neutral Default shader: base `#AAAAAC`, repetition `2`, softness `0.1`, speed `1`, and rotation `0°`. The model, scratch mask, ten sticker assets, recovered positions, scale `0.82`, orbit pose, environment, background/export settings, and editability remain unchanged.
- Source/reference checked: `/Users/kusnizza/Downloads/liquid-metal-3d-settings (2).json`; current schema defaults; settings-transfer exporter/importer behavior; existing preloaded scene and `/Users/kusnizza/Downloads/liquid-metal-3d (8).png` only as unchanged media/placement evidence.
- Reference inputs: The supplied Settings (2) JSON. No matching new PNG, video, model, scratch mask, or sticker asset exists beside it; the closest Liquid Metal PNG predates the JSON by 10 minutes 33 seconds and belongs to Settings (1).
- Docs/contracts read: `workflow.md`; control selection and layout; schema reference; component rules; acceptance testing; media upload; decision contract; required brainstorming, writing-plans, and browser skills.
- Contract rules applied: `controls-product-coverage`, `canvas-surface-preserved`, `acceptance-product-observable`, `persistence-policy-explicit`, `performance-coverage-levels`, and `workflow-required`.
- Decision: Change only the six targets that differ from the current schema: `shader.preset`, `shader.colorBack`, `shader.repetition`, `shader.softness`, `shader.speed`, and `shader.rotation`. Preserve all 33 already-matching targets and all current typed default media/placement seeds.
- Alternatives rejected: Treating `media.model: null`, `media.scratches: null`, and `media.stickers: []` as deletion would discard the requested preloaded scene even though settings transfer cannot serialize media bytes. Using timeline `currentTimeSeconds: 0.770673704...` as a new schema default would promote a transient session cursor, while `isPlaying: false` is forced by every settings export and is not authoring evidence. Changing renderer code or preset definitions is unnecessary because the supplied values exactly equal the existing official Paper Default preset.
- State/output mapping: The six schema `defaultValue`s initialize `state.values` and section/global Reset; `getLiquidMetal3DSettings` maps them to the unchanged Paper/Three uniforms shared by preview, PNG, and video. Existing `media.defaultAssets` and stable sticker seed ids continue to populate normal runtime media and `stickers.placements`.
- Files changed: Settings (2) spec/plan; `app-schema.ts`; product and default-scene browser regressions; this worklog.
- Verification: Focused product/schema tests pass 56/56 and TypeScript passes. `npm run verify:quick` passes 50 files and 293/293 tests. The dedicated authored-default-scene Chromium scenario passes 1/1 with exact visible `#AAAAAC`, repetition `2`, softness `0.1`, speed `1`, and rotation `0°`, plus all twelve preloaded assets, ten resolved `0.82` sticker placements, remove/Reset restoration, and real 2048×1152 PNG export. Controlled in-app browser reload shows the neutral Paper Default metal output with the preserved sticker composition, exact media/placement observables, and no console warnings/errors at `http://127.0.0.1:3005/`.
- Skipped checks: The full browser performance checkpoint, targeted performance suites, and `verify:final` are not required for this post-first-working non-performance edit; the six changed defaults remain existing uniform-only controls and do not alter declared stress limits.
- Risks: There is no new final render paired with Settings (2), so exact control values and live renderer output are authoritative for the new appearance. The previous PNG remains evidence only for unchanged geometry and sticker placement.

### Iteration 25 — Deadline-based preview cadence and real product-frame performance coverage

- Request: Diagnose and fix the laggy animation with the current default sticker scene.
- Task type: Tier 3 renderer/performance correction using brainstorming, systematic debugging, writing-plans, independent scheduler/render-pipeline/performance audits, and controlled browser verification.
- User-visible result: The authored 4K scene keeps its model, scratch mask, ten stickers, render scale, shader, and export quality while preview animation advances at approximately 26–28 FPS on Apple Metal instead of the former deterministic approximately 20 FPS cadence. Sticker scale, rotation, direct surface drag, model-hover orbit, empty-canvas pan, and the six-axis gizmo remain responsive.
- Source/reference checked: The live authored default scene at 3840x2160 backing on Apple M4 Pro Metal; actual `data-liquid-metal-surface-frame` mutations; the preview RAF loop; Three.js default-framebuffer preservation/scissor behavior; existing Toolcraft performance budgets and Playwright SwiftShader characteristics.
- Reference inputs: Preloaded `A.obj`, the original scratch JPEG, all ten sticker PNGs and recovered placements, Settings (2) defaults, current 2x render scale, and the user's report that this exact sticker set made animation feel slow.
- Docs/contracts read: `workflow.md`; runtime boundary; performance core/app contracts; renderer technique; timeline animation; decision contract; acceptance testing; required brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `performance-coverage-levels`, `renderer-technique-inventory`, `timeline-enabled-behavior`, `acceptance-product-observable`, `canvas-surface-preserved`, and `workflow-required`.
- Root-cause evidence: The previous loop compared 60 Hz RAF timestamps to an exact `1000 / 30` threshold, then reset its clock to `now`. Two RAF intervals commonly total slightly less than 33.333 ms, so the renderer waited for a third callback and quantized to approximately 20 FPS even with zero stickers. The prior animation test counted browser RAF callbacks rather than Liquid Metal draws and therefore could not detect the defect.
- Decision: Carry an ideal 30 FPS deadline with 1 ms timestamp tolerance, skip missed slots without burst rendering, and retime viewport interaction to 100 ms so its real frame gap retains headroom under the 120 ms budget. Move renderer preparation/uniform/environment work inside the actual draw branch; cache unchanged background, colors, environment appearance, and camera pose; retain `preserveDrawingBuffer: true` plus scissored sticker updates because the default framebuffer cannot safely preserve untouched pixels otherwise. Observe forty distinct Liquid Metal frame attributes alongside the mandatory 120 browser RAF probe.
- Alternatives rejected: Removing `preserveDrawingBuffer` and forcing full redraw did not materially solve the software-renderer cost (both variants took about 3.1–3.2 s for forty frames in a noisy A/B) and would invalidate cross-frame scissor pixels; disabling preview MSAA did not improve the measured path and would risk visual quality; lowering render scale or simplifying the fragment shader would change output; requiring near-30 FPS for a 1280x720 SwiftShader stress case confuses software-renderer throughput with scheduler cadence.
- State/output mapping: Toolcraft timeline state remains the sole animation source; the deadline scheduler only decides when the unchanged evaluated settings are drawn. Product readiness attributes distinguish imported model/scratch/sticker state from applied GPU resources, and performance coverage measures `data-liquid-metal-surface-frame` changes emitted only after a completed scene render. Import probes freeze atomically at their declared DOM readiness marker so a subsequent cold SwiftShader presentation cannot be misclassified as file-import latency; first model/sticker render readiness remains a separate mandatory assertion.
- Files changed: Performance design/plan; deadline scheduler and tests; renderer/scene caches and readiness observables; performance probe helpers, typed scenario matrix, browser performance/acceptance tests, and this worklog.
- Verification: Focused typecheck plus 78 scheduler/renderer/product tests pass; `npm run verify:quick` passes 51 files and 297/297 tests. Authored-default, scratch, conforming-sticker drag, mouse-over orbit/canvas-pan, sticker scale, sticker rotation, direct sticker drag, gizmo/model orbit, and real product-frame cadence browser scenarios pass. Agent-controlled Metal evidence on the exact 3840x2160 default scene reports forty frames in 1406.6 ms (28.4 FPS, 72.9 ms maximum gap), a separate 120-frame sample at approximately 26.1 FPS, and a final clean-load sample of forty frames in 1347.6 ms (29.7 FPS, 94.9 ms maximum gap); the pre-fix default measurement was approximately 19.9 FPS. Atomic model-import and sticker-import phase probes each pass ten consecutive focused repetitions without raising the 120 ms frame-gap or 2000 ms interaction budgets, while post-import GPU readiness remains asserted. The strict sequential Playwright performance checkpoint passes 47/47 in 5.7 minutes, including media import, real product-frame cadence, selected-sticker transforms, viewport drag/zoom/orbit, timeline, and export. Final delivery evidence is recorded in the Verification section below.
- Skipped checks: None for the changed performance paths.
- Risks: The custom fragment shader remains intentionally expensive under software WebGL, so automated cadence uses a 320x180 1x scheduler baseline while separate 2x stress scenarios cover preview and interactions. Hardware 4K throughput is recorded with the controlled Metal runner and is not inferred from SwiftShader.

### Iteration 26 — Saved pose and included background defaults

- Request: Update the clean-load defaults for position, background color, and background inclusion from `/Users/kusnizza/Downloads/liquid-metal-3d-settings.json`.
- Task type: Tier 2 schema/default-state and product-output revision using brainstorming, writing-plans, unit acceptance, and focused browser verification.
- User-visible result: A clean load and Reset retain the supplied camera pose and now start with Background Include on and scene color `#AFAFC5`; the existing `A.obj`, scratch mask, ten stickers, shader state, 2x render scale, timeline, and export choices are unchanged.
- Source/reference checked: The exact supplied settings JSON; current schema defaults; `DEFAULT_LIQUID_METAL_ORBIT_POSE`; preview background mapping; standard PNG/video background export flow; authored-default and export browser scenarios.
- Reference inputs: `/Users/kusnizza/Downloads/liquid-metal-3d-settings.json`, exported `2026-07-14T12:04:22.043Z` for app id `liquid-metal-3d`.
- Docs/contracts read: `workflow.md`; control selection/layout; schema reference; component rules; setup/export; media upload; acceptance testing; performance; required brainstorming and writing-plans skills.
- Contract rules applied: `controls-product-coverage`, `output-export-required`, `acceptance-product-observable`, `persistence-policy-explicit`, `canvas-surface-preserved`, and `workflow-required`.
- Decision: Treat the JSON's only `position` field as the schema-backed `view.orbit` camera pose. Preserve it because it already matches the current constant byte-for-byte; assert that exact value instead of creating a redundant replacement. Change `export.includeBackground` from `false` to `true`, change `appearance.background` and its defensive renderer fallback from `#0A0A0C` to `#AFAFC5`, and preserve `shader.offset: { x: 0, y: 0 }` because it also matches the file.
- Alternatives rejected: Rewriting the identical orbit constant adds no behavior; treating the transient timeline cursor or forced exported pause state as defaults is unrelated to the request; changing media or sticker placement would discard the existing authored scene even though the settings JSON contains no media bytes or sticker placement map; hardcoding the new color in the renderer would bypass schema state and Reset.
- State/output mapping: Schema `defaultValue`s initialize clean state and global/Background-section Reset. `getLiquidMetal3DSettings` maps `appearance.background` and `export.includeBackground` through the existing live preview, PNG helper, and video-background contract; `view.orbit` continues to drive the preview/export camera through the shared exact pose.
- Files changed: Default-state design/plan; `app-schema.ts`; `liquid-metal-values.ts`; product/default-scene/export regressions; this worklog.
- Verification: To be replaced with completed focused unit, quick-gate, browser, and live-app evidence before delivery.
- Skipped checks: The full performance checkpoint and `verify:final` are not required for this post-first-working non-performance Tier 2 edit; no renderer pass, shader complexity, media workload, viewport behavior, timeline, state shape, runtime, dependency, or export implementation changes.
- Risks: None. The requested position is unambiguous because `view.orbit` is the JSON's only `position` field; `shader.offset` is separately named and already matches.


### Template release repair — 2026-09-09

- Change ID: template-release-2026-09-09
- Entry type: focused
- Request: Make every gallery app cloneable through the published Toolcraft CLI and verify installation/startup.
- Changed owner: Upstream example snapshot packaging, integrity restoration, and public distribution metadata.
- User-visible result: The `liquid-metal` template is admitted as a complete standalone snapshot; original framework hashes remain authoritative. Canonical identity regeneration, where needed, uses the upstream identity generator.
- Verification: Source template admission passed. Published dependency installation and browser startup results are recorded in the upstream `docs/template-release-report.md`; these smoke checks do not claim renderer or export certification.
- Risks: Historical templates retain their original runtime and workflow versions.

## Decisions

### Renderer

- Decision: One patched Three.js `MeshPhysicalMaterial` executes Paper's procedural core plus object-space triplanar grayscale height-to-normal perturbation on the normalized model; its physical reflection source is cached Studio PMREM, one of six procedural float equirectangular recipes, or uploaded HDR/EXR. Ordered stickers use a separate color-stable emissive/PBR overlay whose geometry comes from a cached manifold surface chart, shared-edge unfolding, chart-space clipping, and continuous UV interpolation. Pointer ownership is sticker-first, then base-model raycast orbit, with a raycast miss left to Toolcraft canvas pan.
- Reason: Real IBL uses the reflected shading normal and GGX response, while surface-chart decals preserve one raster artwork across connected hard edges without UV tiling, screen projection, or box-projector collapse.
- Evidence: Renderer-source and geometry tests require typed studio recipes, direct radiance, triplanar scratch normals, exact two-owner adjacency, seed-face traversal, unfolded chart clipping, safe scissored interaction redraw, and shared preview/export wiring; browser acceptance proves all eight environment sources and connected-face sticker output.

### Timeline

- Decision: Toolcraft playback timeline, default duration `10 / 3` seconds, forward loop.
- Reason: The default preserves Paper's original one-stripe period, while runtime loop progress maps any edited duration to one forward cycle; periodic simplex blending closes color and PBR normal motion without mirror/reverse behavior.
- Evidence: Renderer derives `u_loopProgress` with `getToolcraftTimelineLoopProgress`; browser pixels stitch at the default and edited durations; video samples unique frames in `[0, duration)` and encodes the edited duration.

### Layers

- Decision: Disabled.
- Reason: Scratch mask is material source data rather than an independently selectable scene object, while sticker order/selection/transform/removal already belongs to the built-in media collection; generic Layers would expose unrelated model/HDRI/mask media and blank entities.
- Evidence: `panels.layers` remains absent; Scratch Mask fileDrop owns its lifecycle, runtime media order drives decal overlap, and canvas sticker click selects the matching internal media layer id without a second layer UI.

### Media and default scene

- Decision: Preload `A.obj`, the original 5250x3000 scratch JPEG, and ten original-resolution sticker PNGs through typed `media.defaultAssets`; seed their stable authored arrangement through canonical 16:9 surface raycasts at the saved batch scale `0.82`; use Settings (2) for the current shader defaults and the latest `liquid-metal-3d-settings.json` for the exact camera/background defaults; keep persistence disabled.
- Reason: Default assets must behave exactly like ordinary editable media and participate in built-in remove, reorder, transform, section Reset, and global Reset. A fixed authored camera decouples recovered surface anchors from editor viewport size, while schema-backed background defaults keep clean load, Reset, preview, and export aligned.
- Evidence: Product tests assert all twelve asset records, exact settings, ids, order, seeds, camera pose, background color, and Include state; browser acceptance removes and restores each media class and exports the loaded scene without any upload.

### Controls

- Decision: Built-in Toolcraft controls reproduce Paper values/ranges and reset to the supplied official Default preset values (`#AAAAAC`, repetition `2`, softness `0.1`, speed `1`, rotation `0°`); Background starts with Include on and `#AFAFC5`; Offset remains one Vector, Environment uses one eight-option Select plus conditional fileDrop and two sliders, Model Size uses a continuous scale slider plus the portal-only `orientationGizmo` custom control, Scratch Mask uses a standalone single-image fileDrop, Surface Scratches uses Depth/Scratch scale/Invert, Stickers uses multi-image fileDrop, and adjacent Sticker Transform uses continuous per-selection scale/rotation proxies.
- Reason: Built-ins own every required scalar, polarity, source-media, transform, reset, and settings-transfer behavior; standalone fileDrop sections preserve the media component contract.
- Evidence: Section inventory maps Scratch Mask separately from Surface Scratches; Environment option coverage exactly matches all eight schema values, and browser acceptance exercises every source plus the existing real upload, reset, Intensity, and Rotation flow.

### Export

- Decision: PNG/JPG use Toolcraft image sizing and background helpers; the default PNG includes the schema-backed `#AFAFC5` background while Include can still be turned off for transparency; MP4/WebM uses MediaRecorder fallback; preview, image export, and video export all instantiate the same scratched base material, mask transform/uniforms, ordered color-stable sticker stack, placements, selected eight-source environment recipe, and schema-backed `view.orbit` camera pose. The viewport gizmo and its backing are editor-only siblings and never enter export composition.
- Reason: Export must contain final product pixels at real selected dimensions and remain visually consistent with preview.
- Evidence: Browser tests retain prior custom-HDR/model-scale/sticker export coverage and the scratch acceptance additionally downloads a real 2K PNG and one-second WebM after mask transform, maximum depth, scale change, invert, and orbit.

### Performance

- Decision: Persistent preview GPU context/drawing buffer; cached model, sticker topology, environment, texture, scratch, static uniform, background, and camera resources; in-place sticker geometry-buffer updates; footprint-bounded surface-chart traversal; full-quality scissored sticker interaction redraw; one cached float IBL texture per built-in studio recipe; a deadline-carried 30 FPS preview scheduler with explicit 100 ms viewport coalescing; isolated shared-recipe export contexts; and original-resolution preloaded media totaling about 10 MB rather than generated upscale copies.
- Reason: Deadline carry avoids 60 Hz timestamp rounding quantizing a nominal 30 FPS loop to 20 FPS. Sticker drag/scale/rotation invalidates only its local chart geometry and previous/next screen region, while any model/camera/HDRI/timeline/asset-order change forces a safe full composite. Work that does not lead to a draw no longer repeats on every browser RAF. Original source bytes minimize startup transfer without discarding authored detail.
- Evidence: Deterministic scheduler unit tests cover 60 Hz jitter, deadline carry, missed slots, and 30/100 ms retiming. Browser performance observes real product-frame mutations in addition to global RAF/long-task probes; media probes atomically separate file/CPU scene preparation from the following first software-GPU presentation; the exact 3840x2160 authored default reaches approximately 26–28 FPS on Metal versus approximately 20 FPS before the fix. Dedicated selected-sticker scale, rotation, surface drag, orientation-gizmo, model-hit orbit, media import, 2x preview, viewport, and export scenarios retain typed budgets.

## Evidence

- Source reviewed: live `https://shaders.paper.design/liquid-metal`, Paper Design commit `e77c99ede9be8f4842c6bac22ca24902dc41334d`, `@paper-design/shaders@0.0.77`, `@paper-design/shaders-react@0.0.77`, official packaged Liquid Metal GLSL, and the app renderer/export sources.
- Contract applied: Toolcraft workflow, reference-runtime source of truth, runtime/canvas boundary, setup/export, timeline, acceptance product observability, renderer pipeline inventory, and performance coverage rules.
- Browser evidence: OBJ upload/remove/reset, all Paper presets and controls, exact `A.obj` physical chrome coverage, paused orbit, and model scale `0.45 -> 1.45`, canvas/timeline/settings transfer, transparent 2K PNG, Current video, and 4K video paths.
- Environment evidence: Exact `A.obj` at 3840x2160 produces distinct Studio, Warm, and custom Radiance HDR pixels; the current dedicated Playwright coverage additionally proves distinct Softbox, Product, Rim, Chrome, and Neutral pixels while retaining conditional upload, media/reset lifecycle, both live controls, and custom-HDR PNG.
- Loop evidence: Exact `A.obj` and the browser fixture both reach wrapped progress `0`; first/wrapped sampled pixels match within sub-visible WebGL antialias noise before and after editing timeline duration.
- Scratch evidence: Generated asymmetric grayscale mask produces normal-only grooves at a frozen frame; Depth zero is neutral, maximum depth is visible, scale/invert/rotate/flip/orbit change unobstructed canvas pixels, and the same state reaches PNG/WebM export.
- Orientation evidence: The supplied CleanShot was traced to the local ASCII Tool source rather than re-created by eye; controlled browser and Playwright coverage prove the source geometry, snap/direct-drag behavior, runtime history/reset, stable theme backing, fixed viewport placement, shared preview/export camera pose, and export exclusion.
- Pointer-routing evidence: The local ASCII Tool source plus its parity plan/worklog establish primary-button product orbit and runtime canvas pan outside the product surface. Liquid Metal acceptance proves actual base-model raycast ownership, inert middle drag, unchanged empty-space pose, changed canvas world position, and sticker-first direct manipulation.
- Default-scene evidence: The saved JSON supplies exact canvas, shader, model scale, scratch, background, export, timeline, environment, and orbit values. The adjacent 4096x2304 final export supplies sticker screen anchors, while the original `A.obj`, ten PNGs, and scratch JPEG supply media bytes. Product and browser coverage map those inputs to normal runtime media plus resettable defaults.
- Default-sticker parity evidence: The saved batch scale is `0.82`, while the erroneous reconstruction used `0.4`; the renderer projector therefore made every footprint exactly `2.05×` too small. GO and blue-flower seed centers differ from the final export by only `(−3, 0)` and `(+16, −17)` pixels respectively at 4096×2304, so the correction changes scale only. Controlled browser output restores the reference overlaps and reports all ten resolved placement scales as `0.82`.
- Settings (2) evidence: The supplied JSON changes exactly six of 39 targets, and those six equal the official Paper Default preset. Unit state proves `default/#AAAAAC/2/0.1/1/0`; Chromium proves the same visible controls and rendered neutral-metal output while preserving the prior model, scratch, sticker inventory, positions, and export path. No new image/video or media asset is paired with this JSON, so its empty transfer-media placeholders do not replace typed defaults.
- Latest pose/background evidence: `/Users/kusnizza/Downloads/liquid-metal-3d-settings.json` stores the same exact schema-backed camera pose already used by the authored scene, `appearance.background: #AFAFC5`, and `export.includeBackground: true`. Unit and browser coverage treat the identical pose as an idempotent assertion and apply the two actual background default changes through normal clean-load, Reset, preview, and export state.

## Verification

- Passed: `npm run ai:check`.
- Passed: `npm run typecheck`.
- Passed: `npx vitest run src/app/liquid-metal-product.test.ts` (33 tests).
- Passed: functional Playwright browser scenarios for controls/presets, media lifecycle, canvas/timeline/orbit/settings transfer, and real PNG/Current/4K video exports.
- Passed: `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` (249 unit/contract tests, production build, 19/19 browser acceptance tests).
- Verification: strict playwright-fallback browser performance checkpoint passed (33/33) via `TOOLCRAFT_TEST_PORT=3100 npm run verify:perf`.
- Fallback reason: agent-browser-unavailable for upload-dependent workload because the in-app browser driver cannot set file inputs; Playwright fallback exercised the real upload UI.
- Passed: post-delivery `npm run verify:quick` (249 tests), immediate-upload shader pixel regression, exact `A.obj` browser verification, and targeted media-import/stress-preview performance checks (2/2).
- Passed: true-3D material `npm run verify:quick` (250 tests), 45 focused renderer/product tests, controls/presets, immediate pixel/orbit/Noir regression, PNG/Current/4K video acceptance, and five targeted renderer performance scenarios.
- Passed: direct-surface typecheck, 46 focused renderer/product tests, exact `A.obj` full-geometry browser inspection, frozen-frame orbit comparison, and immediate-upload direct shader regression.
- Passed: reflection/matcap typecheck, 47 focused renderer/product tests, `npm run verify:quick` (252 tests), exact `A.obj` non-tiled browser inspection with no WebGL diagnostics, and immediate-upload frozen-frame orbit regression.
- Passed: reflection/matcap `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` (252 unit/contract tests, production build, 20/20 browser checks).
- Passed: targeted Distortion, Scale, stress preview, animation, animated drag, viewport zoom/stability, timeline playback/scrub, and export performance scenarios; the isolated animation check and two concurrent repetitions stayed within the declared 120 ms frame-gap budget after one 175.8 ms cold-start sample in the initial mixed run.
- Passed: physical Liquid Metal typecheck, 56 focused renderer/product/performance tests, `npm run verify:quick` (253 tests), exact `A.obj` PBR browser inspection with no WebGL diagnostics, immediate-upload orbit regression, and all ten targeted physical-renderer performance scenarios.
- Passed: physical Liquid Metal `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` (253 unit/contract tests, production build, 20/20 browser checks); the long combined canvas/timeline/orbit/settings acceptance keeps its full interaction coverage under a 150-second integration timeout while the unchanged strict performance budgets pass separately.
- Passed: seamless-loop typecheck and 50 focused source/product tests; `npm run verify:quick` (255 tests); dedicated first/wrapped-frame browser acceptance at default and edited duration; exact `A.obj` agent-browser seam inspection; real PNG/Current/4K video export; five targeted Speed/animation/timeline/export performance scenarios.
- Passed: editable-environment typecheck and 56 focused source/product tests; `npm run verify:quick` (261 tests); dedicated Environment/HDRI browser acceptance; six targeted preset/import/slider/animation/export performance scenarios; Warm PNG/Current/4K video export; exact `A.obj` agent-browser Studio/Warm/custom-HDR inspection with zero console errors.
- Passed: model-scale `npm run verify:quick` (264 tests); dedicated paused-frame pixel-bounds browser acceptance; targeted `model.scale` hard-limit performance scenario; PNG/Current/4K video export at scale `1.25`; exact `A.obj` agent-browser scale inspection at `0.45` and `1.45` with identical paused frame and zero console/WebGL/shader errors.
- Passed: geometry-conforming sticker typecheck and focused source/product/contract tests; dedicated curved-OBJ browser acceptance for two-PNG batch stack, thumbnail reorder, rotate/flip, 14-step surface drag, orbit attachment, Warm environment, playback, PNG/WebM export, remove, section reset, and global reset; strict 1920x1080 sticker import and one-decal 2x drag budgets; shared stress-preview/animation/canvas-drag/zoom/export budgets; final `npm run verify:final` with 268/268 unit/contract tests, production build, and 24/24 browser tests.
- Passed: sticker color-fidelity correction reproduced the pre-fix desaturation in the browser, then passed TypeScript and diff validation, 66 focused source/product/acceptance tests, the complete curved-decal browser acceptance with a bright-red source-color assertion and PNG/WebM export, and `npm run verify:quick` with 268/268 unit/contract tests.
- Passed: scratch-mask TypeScript/diff validation; focused 100-test contract/source set; 19.2-second functional upload/depth/scale/invert/rotate/flip/orbit/PNG/WebM/remove/reset browser acceptance; 19.9-second sticker input-order/color regression; targeted mask import, Depth, Scratch scale, and Invert budgets; `npm run verify:quick` with 274/274 tests; saved app identity at `http://127.0.0.1:3005/`.
- Passed: studio-HDRI-preset TypeScript/diff validation; 72 focused schema/product/renderer-source tests; `npm run verify:quick` with 274/274 tests; 21.0-second eight-source Environment/HDRI functional browser acceptance; targeted 7.3-second Chrome preset-switch performance scenario; saved app identity at `http://127.0.0.1:3005/`.
- Passed: orientation-gizmo `npm run ai:check`, TypeScript, seven orbit-math tests, focused schema/acceptance/product/renderer tests, and `npm run verify:quick` with 284/284 tests; controlled in-app browser geometry/hover/snap/direct-drag/history/reset/theme/zoom verification; the full canvas/timeline/orbit/settings browser scenario; real PNG/Current/4K video and background export; targeted `view.orbit` animated drag performance; saved app identity at `http://127.0.0.1:3005/`.
- Passed: connected-face sticker wrapping TypeScript and `npm run verify:quick` (50 files, 289 tests); 24 focused topology/source tests; dedicated two-face `90°` hard-edge browser pixels; the complete curved-model sticker workflow; exact supplied `A.obj` plus PNG inspection; and targeted selected scale, rotation, and direct surface-drag performance budgets at full render scale.
- Passed: model-hit orbit `npm run ai:check`, TypeScript, `npm run verify:quick` (50 files, 292 tests), 39 focused renderer/acceptance/performance tests, dedicated model-orbit/empty-canvas/middle-inert browser acceptance, the complete PNG-sticker browser workflow, targeted gizmo plus model-surface `view.orbit` performance, and controlled in-app browser shell/console inspection at `http://127.0.0.1:3005/`.
- Passed: authored-default-scene focused product/schema tests and TypeScript; dedicated 39.1-second default-scene Chromium acceptance; affected scratch-mask, hard-edge-wrap, model-lifecycle, and complete-sticker Chromium regressions; controlled in-app browser inspection of `A.obj`, the original scratch JPEG, ten arranged stickers, and zero model/scratch errors at `http://127.0.0.1:3005/`.
- Passed: authored-default-scene final `npm run verify:quick` (50 files, 293 tests); repeated dedicated default-scene Chromium acceptance in 39.5 seconds; targeted model and scratch media budgets with non-perturbing performance instrumentation; final live-server reload with `A.obj`, the original scratch JPEG, ten stickers, and zero console/model/scratch errors at `http://127.0.0.1:3005/`.
- Passed: authored-default-sticker parity focused product test (53/53), TypeScript, `npm run verify:quick` (50 files, 293 tests), dedicated default-scene Chromium acceptance with ten resolved `0.82` placements and real PNG export, targeted sticker-scale performance, and controlled reference-frame comparison with zero console warnings/errors at `http://127.0.0.1:3005/`.
- Passed: Settings (2) default-state focused product/schema tests (56/56), TypeScript, `npm run verify:quick` (50 files, 293 tests), dedicated default-scene Chromium acceptance with exact six new shader defaults plus preload/Reset/PNG coverage, formatting, and controlled neutral-metal reload with zero console warnings/errors at `http://127.0.0.1:3005/`.
- Passed: deadline-scheduler TypeScript and four focused cadence tests; `npm run verify:quick` (51 files, 297/297 tests); targeted authored-default, sticker/scratch/orbit, actual product-frame cadence, and repeated media/zoom browser regressions; strict sequential `npm run verify:perf` with 47/47 scenarios in 5.7 minutes; agent-controlled Apple Metal default-scene measurements improved from approximately 19.9 FPS to 26–29.7 FPS at the unchanged 3840x2160 backing.
- Passed: final `TOOLCRAFT_TEST_PORT=3100 npm run verify:final` with 51/51 test files and 297/297 unit/contract tests, production TypeScript/Vite build, and 28/28 functional browser scenarios. The isolated port avoided an unrelated concurrently running Toolcraft app without stopping or modifying that server.
- Passed: final live-server reload at `http://127.0.0.1:3005/` on `ANGLE Metal Renderer: Apple M4 Pro` with `A.obj`, applied scratch texture, ten GPU-rendered stickers at scale `0.82`, 3840x2160 backing, approximately 29.7 product FPS, and no WebGL, shader, model, scratch, or application-console errors; the only browser error is an unrelated missing `favicon.ico` request.

## Risks

- Risk: Separate sidecar assets referenced by `.gltf` are not available from a single browser file upload; use `.glb` for bundled assets.
- Risk: Actual video container depends on browser MediaRecorder codec support; safe fallback is WebM.
- Risk: Softbox, Product, Rim, and Chrome are deterministic procedural studio IBL rigs, not photographed HDRI files; use Custom HDRI when real-location detail is needed.
- Risk: Sticker charts intentionally stop at open/non-manifold edges and cap one local rebuild at 12,000 triangles; highly tessellated surfaces beyond that local budget may crop rather than freeze interaction.
- Risk: Settings transfer omits sticker placement bytes. The authored default anchors are deterministic reconstructions from the supplied final render and exact orbit pose, not serialized originals.

### Iteration 27 — Canonical Toolcraft demo subpath

- User-visible result: The production build and Vercel deployment now serve the app at `/demos/liquid-metal/`, including its authored default scene, worker, nested client routes, and media assets.
- Contract applied: The Vite base, TanStack Router base path, and authored-scene asset root share `import.meta.env.BASE_URL`; local development remains rooted at `/`.
- Verification tier: Tier 2. `npm run build`, `npm run verify:quick` (51 files, 297 tests), and a browser load through the website rewrite all passed. The canonical URL was retained, the runtime shell/canvas rendered, and the only request error was the already documented root favicon request.

#### Thermo-nuclear review remediation

- Result: Vercel now routes generated and authored-scene `/assets/*` files before the final SPA fallback, so direct non-file descendants receive the application HTML without breaking workers or media.
- Verification: the exhaustive four-test gateway contract, production build, canonical root browser load, and `/demos/liquid-metal/review-deep-link` reload passed with `200 text/html` and all asset responses successful. The existing quick-gate result above remains current because this pass changes deployment routing only.

### Iteration 28 — Shared Toolcraft social preview

- User-visible result: Social shares of the app now use the main Toolcraft 1200×630 preview image through Open Graph and Twitter metadata.
- Source and contract: `apps/website/public/social-previews/og-toolcraft-v2.jpg` remains the single asset source. The metadata uses its absolute `https://toolcraft.sh/` URL so neither the `/demos/liquid-metal/` Vite base nor a direct Vercel hostname can rewrite it incorrectly.
- Verification tier: Tier 0. Product schema, runtime state, renderer, canvas, exports, and performance are unchanged. The repository metadata contract, production build, built-HTML base-path inspection, website typecheck/build, starter tests/typecheck, CLI generation tests, and Frozen signed integrity check passed; production verification follows the pushed commit.


## 2026-08-05 — Canonical product identity and deployment path

- User-visible result: Renamed the standalone product to `Liquid Metal` and aligned its repository package plus public demo base to `liquid-metal`. Product rendering, controls, defaults, and export behavior remain unchanged.
- Request: Apply the approved complete rename across code, folders, gallery identity, and deployment wiring without preserving old route aliases.
- Source/reference checked: The approved complete-app-renaming design and implementation plan, the current standalone package metadata, Vite/router base handling, `vercel.json`, identity metadata, and active acceptance/deployment assertions.
- Contract rules applied: Broad identity/deployment migration because the directory and public deployment identity change across the generated app boundary. Existing product-domain modules remain semantically named; the external Vercel stage must retain the current Project ID.
- State/output mapping: Package name, HTML title, control/acceptance identity, persistence/settings-transfer namespace where present, Vite base, public asset prefix, and Vercel rewrites now use `liquid-metal`. Changed persistence namespaces intentionally reset prior browser-local settings.
- Verification: Canonical package/title/base audit and every available standalone `demo-deployment.test.mjs` passed for this migration batch.
- Risks: Old demo paths are intentionally absent; no compatibility redirect is retained.


## 2026-09-10 — Focused accepted-gallery repair

- Request: Continue app-only repairs and check for lost functionality, using necessary checks only.
- Task type: Product acceptance/test repair after owner-accepted initial delivery.
- User-visible result: The model.scale test now measures the model rather than treating the whole opaque frame as foreground.
- Source/reference checked: The failed foreground comparison and actual preserveDrawingBuffer canvas from liquid-metal-renderer.tsx.
- Reference inputs: No new external reference assets; existing product and audit evidence.
- Docs/contracts read: Local AGENTS.md, gallery-workflow.md, workflow.md and acceptance-testing.md.
- Contract rules applied: Focused-only development; real UI/output proof; copied framework, delivery record, defaults and runtime remain unchanged.
- Decision: Turn Include background off through UI, verify the empty canvas has no foreground, then measure actual alpha-positive model pixels for both scales. Keep the strict width increase and paused shader-frame equality; also require area growth.
- Alternatives rejected: Larger timeouts, fabricated evidence, weakening the output checks, changing defaults or patching signed helpers.
- State/output mapping: model.scale changes actual uploaded-model width/area while shader phase remains fixed.
- Files changed: e2e/app-controls.spec.ts.
- Verification: npm run test:feature -- model.scale passed (14.4s).
- Skipped checks: No aggregate build, typecheck, delivery, performance, or unrelated browser matrix; product implementation is unchanged.
- Risks: The local shared node_modules symlink produces a font allow-list 403; it did not affect canvas/model proof. Product export/background code was not changed.
