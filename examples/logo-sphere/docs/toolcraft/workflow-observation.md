# Logo Sphere publication verification — 2026-09-08

- Identity-only publication copy: all source modules other than `app-identity.ts` match `toolcraft-apps/logos-grid` byte-for-byte. Original source remains unchanged.
- `npm ci` and the actual-base production build passed (`/demos/logo-sphere/`). Vite emitted only its existing large-chunk warning.
- One bare `npm run verify:delivery` stopped at integrity: `index.html` and `src/app/app-identity.ts`. The original source integrity check passes all 650 files. The blocker is the explicitly requested rename against the original signed metadata, not inherited source corruption. No manifest, signed validator or bootstrap mechanics were modified to hide it; no protected delivery receipt is claimed.
- Focused product tests: 65 passed, 2 failed. Both failures also reproduce in the untouched source: expected Fisheye default 0 versus current 46, and an exact sizing comparison omitting the current `defaultMode: infinite`. Defaults and tests were preserved rather than rolling back the supplied app.
- Local production browser: title and UI load at port 3210; no uncaught browser errors or Vite overlay. Default logo atlas is visible. Pause works; switching Grid to Fibonacci changes real canvas pixels while keeping 63 logos. Finite canvas controls produce 1600×1200 framing; the real Export PNG action downloads a non-empty rendered image used for the cover. Only favicon.ico returns a local 404; bundled application assets load.
- Gallery: all 53 canonical identity/content/route checks pass in the working tree. Local `/gallery/logo-sphere` shows the correct heading and four-paragraph description. JPEG cover is 1600×1200, generated from actual app export.
- Vercel project `prj_nUdZqSnQJN3nJGJeZa6okhymTc3x` is Git-linked to `pixel-point/primeui-v2`, rooted at `examples/logo-sphere`, with the same build/install/output, Node 24, affected-project detection, outside-root access and preview protection as Aura Carousel.
- No measured performance, full functional certification, or fix to inherited stale tests is claimed. This is a deployment of the supplied product, with the above verification limitations disclosed.
