# Implementation Worklog

## Status

Mode: product

Prism Flow is a procedural WebGL2 refraction product with user-owned soft ellipse masks, runtime-owned editable canvas sizing, background, Infinity mode, settings transfer, playback timeline, persistence, image export, and editor navigation.

First product delivery uses complete functional proof. Later delivery uses ownership-derived functional proof. A localized performance complaint can authorize one targeted iteration; a complete performance audit remains an explicit user choice through `npm run verify:perf`.

## Decision Trail

### Iteration 41 — Stable Infinity position and optical backdrop

- Request: "почини у приложения призм инфинит канвас, там при включении происходит скачок сцены и волна теряет цвет будто бленд под ней происходит с другим фоном".
- Task type: Tier 3 functional renderer/canvas correction.
- User-visible result: Infinity keeps the procedural scene centered and preserves its visible wave colors over the selected viewport background.
- Source/reference checked: The running Prism Flow editor, paused finite/Infinity canvas pixels and DOM geometry, scene bounds provider, runtime background policy, shared field shader, and pinned Paper lens composite.
- Reference inputs: `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-fb9d1d14-011f-4cd2-99a4-8e1c5adf6a10.png` and the current product.
- Docs/contracts read: workflow.md; core/runtime-boundary.md, core/setup-export.md, core/media-upload.md, core/performance.md; decision-contract.md, schema-reference.md, component-rules.md, renderer-technique.md, performance.md, acceptance-testing.md; systematic-debugging and writing-plans workflows.
- Contract rules applied: runtime-shell-required, renderer-technique-inventory, output-export-required, acceptance-product-observable, performance-coverage-levels, workflow-required.
- View interaction intent: Existing non-spatial optical field; runtime owns canvas pan and zoom.
- Interaction ownership: The built-in Infinity switch remains the only mode control; no product navigation or duplicate state is introduced.
- Decision: Center the exact product bounds on the world origin. Preserve the current background-aware optical composite, then recover transparent premultiplied output against the selected background only for Infinity preview. Run this after the final optical effect, including the retained Paper lens, so both modes use identical source colors.
- Alternatives rejected: An opaque product rectangle violates Infinity background ownership. Merely increasing RGB without a sufficient alpha clips premultiplied highlights. Applying transparency before the Paper lens changes its white-matte sampling. Runtime offset overrides duplicate the existing world transform.
- State/output mapping: `canvas.mode` selects preview backdrop removal; selected Background remains the color authority. Product scene bounds map the existing scene dimensions to a centered world rectangle. Finite preview and exported artifacts keep their existing opaque/transparent policies.
- Performance intent: ordinary-product-work. This request concerns geometry and color correctness, not rendering speed.
- Performance model: Existing shader-resource, preview-frame, and image-export passes, workload limits, raymarch/lens sample counts, selected backing resolution, and retained resource lifecycle are preserved. Backdrop removal adds bounded per-pixel arithmetic in the final existing shader.
- Verification: Typecheck and all 15 focused product/schema unit tests passed. Both registered Infinity browser scenarios passed, including geometry/color equivalence, pan, persistence, finite restoration, history, and decoded circular PNG bounds. Eight real GPU comparisons covered pale, black, white, and blue backgrounds with Lens off/on; maximum RGB error was two byte values with unchanged geometry and timeline phase. Retina at 70% zoom passed with Lens off/on and the same error bound. One bare `npm run verify:delivery` stopped at code health because the unchanged `src/app/app-acceptance-data.ts` contains 728 lines against its 700-line limit (also 728 in HEAD). No successful delivery receipt is claimed. Separate production build and integrity verification of all 650 protected files passed.
- Risks: Browser premultiplied-alpha quantization permits a small RGB rounding difference. Infinity retains the established explicit procedural dimensions; customized finite dimensions are restored when leaving Infinity.
- Production publishing: The user explicitly requested immediate production publishing after the functional fix and the pre-existing delivery-gate failure were reported. Publish the verified standalone Prism Flow source to its existing Vercel project using an isolated upload with the configured `examples/prism-flow` root. No successful protected delivery receipt is claimed and no gate or limit is changed.
- Production routing correction: The user reported a Vercel 404 from the delivered URL. Direct HTTP reproduction showed `/demos/prism-flow/` returning 404 while `/demos/prism-flow`, `/index.html`, and prefixed JS assets returned 200 on the same immutable deployment. Canonicalize trailing slashes, redirect the dedicated domain root to the app base, and target `/index.html` explicitly for SPA rewrites. This follows Vercel's Vite SPA routing documentation at `https://vercel.com/docs/frameworks/frontend/vite`. Verify real production HTML, assets, WebGL output, and the Infinity toggle through the slash, no-slash, and root entry URLs before reporting the correction complete. Renderer source and its prior functional checks remain unchanged.
- Unbounded-preview steering: The user then asked whether Infinity should remove all frames and supplied `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-4cb5f852-e536-4220-96c4-29dac86c739b.png`. The remaining rectangle is the product's fixed raster extent plus frame-margin fade, not a runtime border. Continue this Tier 3 batch by presenting the product canvas through a portal in `infiniteCanvasContent`, computing viewport samples in the existing world coordinates, and disabling frame clipping/fade only for Infinity preview. Explicit product bounds remain the finite export crop; authored masks remain part of the effect. The viewport layer does not inherit CSS world transforms; its shader consumes runtime pan/zoom to preserve camera continuity. This avoids a synthetic giant scene rectangle and keeps one active renderer. Inspection showed that the runtime already remounts the product scene on mode changes; the renderer must initialize from the committed timeline phase, never a synthetic zero phase. Focused geometry, edge-pixel, camera, restoration, backing, and export checks plus the required delivery gate precede the already-authorized production update.

- Unbounded-preview verification: All 18 focused product/schema/view-coordinate unit tests, TypeScript, Vite production build, and protected integrity (650 files) passed. The registered Infinity PNG scenario passed (decoded finite and circular scene-bounds crops); the registered mode/restoration scenario passed after its pixel-readiness polling was aligned with its existing strict maximum RGB difference of two byte values. The redundant mean-under-0.5 polling threshold had rejected a SwiftShader frame whose pixels still satisfied that maximum. Real Chrome checks covered four selected backgrounds with Lens off/on, all three frame shapes beyond the former rectangle at 50% zoom, DPR 1/2 backing sizes, viewport resize, and translated pixel agreement after pan (maximum difference one). Lens edge continuation passed too. The bare delivery gate again stopped solely at the unchanged 728/700-line acceptance-data module; no successful receipt is claimed.
- Unbounded-preview implementation: The product canvas is portaled into its own measured viewport mount only in Infinity. Field and Lens sample the original centered procedural coordinate system with runtime zoom/pan; shape clipping, edge fade, and Paper's old image-frame coverage are disabled only for this preview. Authored masks and explicit export bounds remain unchanged. The runtime's existing product-scene remount initializes from committed timeline progress to avoid a zero-phase jump. Only essential viewport redraws run during camera interaction; animation progression remains suspended during the gesture.

- Unbounded production result: Published isolated snapshot `prism-unbounded-production-qad3ql24` as `dpl_25ph2TdzFtxKW2F9sy3hWTW7FHij` (`https://prism-flow-phf9oqhal-pixelpoint.vercel.app`). Actual Chrome checks of the primary no-slash/slash/root entries and the website-proxy alias all reached HTTP 200, loaded WebGL, and filled the viewport without errors. Public 50% zoom tests confirmed colored pixels beyond the old frame for Rectangle/Rounded/Circle, DPR 1/2 backing, resize, and exact translated pixels after pan. Canonical URL: `https://prism-flow-eight.vercel.app/demos/prism-flow`.

### Iteration 40 — Prism Flow standalone deployment

- Request: Deploy the current dispersion-v1 app like the existing examples on docs-new and choose a more descriptive name; the proposed Prism Flow name and deployment scheme were approved with "делай".
- Task type: Tier 4 standalone import/deployment with identity-only product metadata changes.
- User-visible result: The app is called Prism Flow, has its own gallery card, and uses the standard /demos/prism-flow route and dedicated Vercel project.
- Source/reference checked: Current working tree at toolcraft-apps/dispersion-v1, its running editor, existing examples, gallery registry, and live Git-linked Vercel settings for Mesh Gradient and Micrographics.
- Reference inputs: Existing dispersion-v1 source and running application; no new visual design.
- Docs/contracts read: workflow.md, core/runtime-boundary.md, assembly-workflow.md, decision-contract.md, acceptance-testing.md; brainstorming, writing-plans, and Vercel deployment workflows.
- Contract rules applied: runtime-shell-required, reference-clone-source-of-truth, persistence-policy-explicit, output-export-required, workflow-required.
- View interaction intent: Unchanged non-spatial optical field; no new camera or canvas interactions.
- Interaction ownership: Existing controls, timeline, canvas navigation, and export ownership remain unchanged.
- Decision: Import the portable working tree without local diagnostics, preserve all renderer/default/runtime bytes, and rename public identity, panel title, persistence/settings identity, and exported filenames. Re-export only the authorized HTML/app identity metadata through the upstream integrity writer; preserve every other protected hash.
- Alternatives rejected: Redesigning or rebuilding controls, overwriting the source folder, upgrading the copied runtime, changing animation/defaults, creating a separate GitHub repository, or merging the website into main.
- State/output mapping: Existing schema values still feed the same retained preview/export pipeline. The new persistence key isolates this published app from older local Dispersion workspaces.
- Performance intent: ordinary-product-work.
- Verification: One bare `npm run verify:delivery` is the protected delivery authority; hosting identity/base-path and website route/gallery checks additionally cover this deployment adaptation. No measured performance audit is requested.
- Risks: The source contains recent uncommitted product updates; they are intentionally imported as the source of truth. Website production publication remains separate from the docs-new branch.

### Iteration 39 — Current imported settings become product defaults

- Request: Make the current settings from `/Users/kusnizza/Downloads/dispersion-studio-settings.json` the defaults and avoid strong checks.
- Task type: Tier 2 schema/default-value update with no control inventory, renderer, timeline, persistence, export-path, or workload-bound change.
- User-visible result: Fresh workspaces and Reset now use the supplied Prism composition, including Refraction 36, Effect area All, Lens dispersion shift 0.11, Saturation 86, and Glow 43.
- Source/reference checked: The supplied Toolcraft settings-transfer JSON, the canonical Dispersion and Lens default tables, schema consumers, and focused product-default tests.
- Reference inputs: `/Users/kusnizza/Downloads/dispersion-studio-settings.json`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/timeline-animation.md`, `core/setup-export.md`, `core/media-upload.md`, and `core/performance.md`; Implementation references `schema-reference.md` and `component-rules.md`; Verification reference `acceptance-testing.md`; plus brainstorming and writing-plans workflows.
- Contract rules applied: `controls-product-coverage`, `timeline-enabled-behavior`, `persistence-policy-explicit`, `output-export-required`, `acceptance-product-observable`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; only existing panel-control defaults change.
- Interaction ownership: Existing panel controls remain the sole editors of these values, and the canvas remains the product-output surface.
- Decision: Update only the six product values that differ from the existing canonical defaults: `dispersion.refraction` 36, `effect.area` `all`, `lens.dispersionShift` 0.11, `dispersion.spectrum` `prism`, `dispersion.intensity` 86, and `dispersion.glow` 43. Keep every already-matching value unchanged. Do not adopt timeline time, playback state, or panel expansion state because they are workspace-session state rather than product defaults.
- Alternatives rejected: Runtime-importing the JSON would bypass Reset semantics; duplicating values in each schema control would create competing authorities; replacing persisted workspace values would be a destructive migration not requested by the user.
- State/output mapping: Empty-state normalization and schema `defaultValue` references resolve through `DISPERSION_DEFAULTS` and `LENS_DISTORTION_DEFAULTS`; Reset, preview, settings transfer, and export continue to consume those canonical values through the existing paths.
- Performance intent: ordinary-product-work.
- Performance model: The existing reachable workload maxima and renderer invalidation paths are unchanged; the new constants add no pass, sample, texture, loop, allocation, or workload dimension.
- Verification: `npm run typecheck` passed. Focused Vitest files `src/app/dispersion/dispersion-product.test.ts` and `src/app/app-schema.test.ts` passed with 15 tests. Per the user's request, bare `npm run verify:delivery`, full browser acceptance, export verification, and performance suites were intentionally skipped.
- Risks: Existing persisted workspaces keep their saved values until Reset or cleared persistence; this preserves user state and is not migrated automatically.

### Iteration 38 — One-off seamless 4K ProRes loop artifact

- Request: Export the supplied Dispersion Studio settings as a seamless video matching the exact sixty-second loop at 4K and 30 fps in ProRes.
- Task type: Operator artifact render from current product state; no app schema, control, runtime, or product-export UI change.
- User-visible result: One 3840×2160 Apple ProRes 422 HQ MOV containing exactly 1800 frames over 60 seconds, rendered from the supplied settings with the configured background and two enabled soft masks.
- Source/reference checked: `/Users/kusnizza/Downloads/dispersion-studio-settings.json`, the current normalized settings reader, retained WebGL renderer, mask uniforms, and timeline phase mapping.
- Reference inputs: `/Users/kusnizza/Downloads/dispersion-studio-settings.json`.
- Docs/contracts read: `docs/toolcraft/workflow.md`, `core/timeline-animation.md`, and `core/setup-export.md`; plus brainstorming and writing-plans workflows.
- Contract rules applied: `timeline-enabled-behavior`, `output-export-required`, `renderer-technique-inventory`, and `workflow-required` for deterministic frame scheduling and artifact fidelity.
- View interaction intent: Remains `non-spatial`; the render uses the existing fixed two-dimensional product frame.
- Interaction ownership: None added. This is an operator-produced artifact from a settings snapshot, not a new panel or canvas operation.
- Decision: Render the exact current WebGL product at progress `frame / 1800`, include frames 0 through 1799 once, verify progress 0 and 1 have the same renderer signature, and encode hardware-assisted Apple ProRes 422 HQ in a MOV container at fixed 30 fps. Keep Background on and use opaque 10-bit 4:2:2 output because the supplied settings request an included background.
- Alternatives rejected: Duplicating frame zero at the end would make the file longer than sixty seconds and introduce a held frame; upscaling a lower-resolution render would not be true 4K; browser MP4/WebM would not satisfy ProRes; ProRes 4444 would add unnecessary alpha and substantially larger output for an opaque composition.
- State/output mapping: The settings snapshot is normalized by `readDispersionSettings`; every frame uses the shared `createDispersionGlResource` renderer with `loopSeconds: 60`, `maskPreview: false`, and the exact scheduled progress. Raw 4K RGBA frames feed the ProRes encoder without an intermediate lossy codec.
- Performance intent: ordinary artifact generation; no product performance measurement or certification.
- Verification: The renderer seam signature matched at progress 0 and 1. `ffprobe` reported codec `prores`, profile `HQ`, tag `apch`, pixel format `yuv422p10le`, 3840×2160, frame rates `30/1`, duration `60.000000`, and exactly 1800 decoded frames.
- Risks: ProRes 422 HQ is opaque and optimized for editing rather than compact delivery; the resulting file is approximately 1.54 GB.

### Iteration 37 — Mask application comparison toggle

- Request: Add another toggle that shows the dispersion result with masks and without masks, without unnecessary checks.
- Task type: Tier 3 focused schema and renderer-output behavior change because one persistent control changes preview and export pixels.
- User-visible result: The Masks section now includes Apply masks. On preserves the soft-union clipping; off shows the same complete dispersion composition without clipping. Show masks remains an independent red preview-only overlay.
- Source/reference checked: The current mask values, built-in Masks section, shared WebGL mask uniforms, fragment composite, preview renderer, export renderer, acceptance rows, and canonical renderer pipeline.
- Reference inputs: Current product implementation only; no external reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, `core/performance.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation references `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification reference `acceptance-testing.md`; plus brainstorming and writing-plans workflows.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `interaction-surface-ownership`, `renderer-technique-inventory`, `output-export-required`, `acceptance-product-observable`, `persistence-policy-explicit`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; this is a two-dimensional output mode, not a camera or canvas manipulation.
- Interaction ownership: Apply masks is panel-owned as a persistent product property. A canvas gesture was rejected because it would hide the active comparison state and duplicate a simple controls-panel setting.
- Decision: Add the built-in `switch` target `masks.enabled`, default it to true to preserve current output, and feed it to one retained shader uniform. The mask count remains available for Show masks even when application is off. Preview and export both consume the selected application state, while export still forces only the red overlay off.
- Alternatives rejected: Reusing Show masks would couple editor visualization to product clipping; clearing the collection would destroy mask geometry instead of providing a comparison; changing the mask count uniform to zero would also prevent the red preview overlay from showing stored masks.
- State/output mapping: `masks.enabled` normalizes through the shared settings reader and maps to `uMaskEnabled`. The fragment composite selects either soft-union coverage or identity coverage before saturation and dithering. `masks.preview` remains a separate preview-only uniform, and the export callback consumes `masks.enabled` from the same state while forcing `maskPreview: false`.
- Performance intent: ordinary-product-work.
- Performance model: One boolean uniform and constant-cost branch join the existing retained preview/export passes. Mask count, pass count, allocations, texture lifecycle, render scale, and the fixed twelve-mask workload boundary remain unchanged.
- Verification: One bare `npm run verify:delivery` remains the protected delivery authority. Per the user's request, this pass runs only TypeScript typecheck and focused mask/schema/performance-structure unit tests; full delivery, full browser, export matrices, and measured performance are skipped.
- Risks: The browser acceptance scenario is registered for later protected delivery, but this deliberately lightweight pass does not execute the full browser/export proof.

### Iteration 36 — Bounded soft ellipse masks

- Request: Implement `/Users/kusnizza/Projects/toolcraft-apps/dispersion-v1/docs/superpowers/plans/2026-09-03-circle-masks.md` and avoid strong or unnecessary checks.
- Task type: Tier 3 renderer/canvas product feature with schema, preview/export, acceptance, and workload-model changes.
- User-visible result: A new Masks section after Frame lets users add or remove up to twelve elliptical masks and edit Center, Width, Height, Rotation, and Blur. Show masks displays a translucent red soft-union overlay over the unmasked wave; normal preview and export keep the wave visible only inside the soft union.
- Source/reference checked: The supplied circle-mask implementation plan, current Toolcraft collectionActions contract, current retained WebGL field shader, preview renderer, image export callback, and canonical renderer pipeline.
- Reference inputs: `/Users/kusnizza/Projects/toolcraft-apps/dispersion-v1/docs/superpowers/plans/2026-09-03-circle-masks.md`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation references `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification reference `acceptance-testing.md`; plus brainstorming, writing-plans, and browser workflows.
- Contract rules applied: `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-technique-inventory`, `acceptance-product-observable`, `output-export-required`, `persistence-policy-explicit`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; masks edit a two-dimensional procedural output and introduce no camera or 3D object pose.
- Interaction ownership: Panel-owned collection actions control cardinality, compound item fields own exact mask geometry, and the Show masks switch owns preview mode. Canvas handles are intentionally deferred so canvas and panel do not duplicate the same operations.
- Decision: Use one built-in compound `collectionActions` control with an algorithmic hard ceiling of twelve masks and one built-in preview switch. Normalize all imported mask records and keep only the first twelve. Convert centers to bottom-left WebGL screen coordinates once, express ellipse diameters relative to the short edge, rotate into local axes, evaluate `1 - smoothstep(1 - blur, 1 + blur, distance)`, and union masks as `1 - product(1 - coverage)`. Zero masks are identity. Apply coverage before saturation; apply the preview-only red overlay after background composition; force preview mode off in export. Keep the existing Frame clip outside the shader.
- Alternatives rejected: Canvas handles would duplicate the panel authority in this delivery; one shader pass per mask would multiply pass count; dynamic GLSL arrays are less portable than a fixed bounded array; using the preview flag during export would leak editor visualization into the artifact; bumping persistence would unnecessarily discard existing workspaces.
- State/output mapping: `masks.items` stores complete mask records and invalidates the retained preview composite; `masks.preview` toggles only the preview branch. Both feed fixed uniform arrays in the shared field material. Preview passes the switch value, export always passes `maskPreview: false`, and settings transfer plus local persistence inherit both schema targets without a version bump.
- Performance intent: ordinary-product-work.
- Performance model: Mask count is a direct `mask-count` workload dimension bounded at twelve for preview and image export. Geometry, blur, and preview mode update retained uniforms; no renderer pass, texture, allocation class, or raymarch step is added. The mask loop has a fixed compile-time ceiling and an early count break.
- Verification: Focused schema/product/acceptance/performance-structure Vitest checks, TypeScript typecheck, Playwright catalog listing, and one local browser smoke check passed. Per the user's request, bare `verify:delivery`, the full browser suite, export matrices, 8K checks, and all measured performance runs were intentionally skipped.
- Risks: The red overlay is verified in live preview and the clean-export branch is structurally covered, but the full decoded export acceptance scenario was not executed in this lightweight pass. Direct canvas mask handles remain a possible later interaction stage.

### Iteration 35 — Imported workspace values become product defaults

- Request: Make the settings from `/Users/kusnizza/Desktop/dispersion-studio-settings.json` the defaults and avoid unnecessary checks.
- Task type: Tier 2 schema/default-value update with no control, renderer, timeline, persistence, or export-path changes.
- User-visible result: Fresh workspaces and Reset now start from the supplied rectangular Custom-spectrum composition, including its neutral background, field geometry, palette anchors, glow, motion shimmer, and disabled Lens parameter values.
- Source/reference checked: The supplied Toolcraft settings-transfer JSON and the current canonical Dispersion and Lens default tables.
- Reference inputs: `/Users/kusnizza/Desktop/dispersion-studio-settings.json`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation references `schema-reference.md` and `component-rules.md`; plus brainstorming and writing-plans workflows.
- Contract rules applied: `controls-product-coverage`, `persistence-policy-explicit`, `output-export-required`, `acceptance-product-observable`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; only existing control defaults change.
- Interaction ownership: Existing panel-owned controls remain the sole editors of these values, while the canvas continues to display their product output.
- Decision: Copy every differing control-backed value from the settings snapshot into the canonical Dispersion or Lens default table. Keep canvas `1920×1080`, render scale `2`, timeline duration `60s`, PNG format, 4K resolution, and included background unchanged because they already match. Do not adopt the snapshot's current timeline time, paused state, or expanded state because those are session state rather than product settings.
- Alternatives rejected: Importing the JSON at runtime would bypass schema Reset behavior; duplicating defaults in each control would create multiple authorities; bumping persistence would discard existing user workspaces unnecessarily.
- State/output mapping: Schema controls continue to read `DISPERSION_DEFAULTS` and `LENS_DISTORTION_DEFAULTS`; empty state and section/global Reset resolve to those tables, and the existing renderer, timeline, persistence, settings transfer, and image export paths consume the resulting values unchanged.
- Performance intent: ordinary-product-work.
- Performance model: Default constants change reachable initial values but add no pass, branch, sample, texture, loop, allocation, workload dimension, or invalidation path.
- Verification: One bare `npm run verify:delivery` remains the protected delivery authority. Per the user's request to avoid extra checks, this pass is limited to the focused product test and typecheck; delivery, browser, and performance suites are not run.
- Risks: Existing persisted workspaces keep their saved values until the user invokes Reset or starts with cleared persistence; this avoids destructive migration.

### Iteration 34 — Porcelain two-tone spectrum preset

- Request: Implement the approved Porcelain Spectrum preset plan and avoid unnecessary verification.
- Task type: Tier 2 schema/product-behavior extension to an existing finite Spectrum select.
- User-visible result: Spectrum now includes Porcelain after Ice, producing cool periwinkle lows, warm ivory highlights, and restrained neutral transitions without changing the default Prism workspace or background.
- Source/reference checked: The supplied ribbed-ceiling photo study and the approved comparison board at `.toolcraft/browser-artifacts/porcelain-preset-board.png`, plus the existing cosine-palette preset and shader data path.
- Reference inputs: `docs/superpowers/plans/2026-09-03-porcelain-spectrum-preset.md` and `.toolcraft/browser-artifacts/porcelain-preset-board.png`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md` and `core/layout.md`; Implementation references `schema-reference.md` and `component-rules.md`; plus the writing-plans workflow.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `acceptance-product-observable`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; Porcelain only changes the existing spectral palette selection.
- Interaction ownership: Spectrum remains panel-owned through the existing select, while the canvas remains the product-pixel output used to judge the palette.
- Decision: Add `porcelain` as a built-in palette preset with `amp [0.31, 0.24, 0.05]`, `base [0.93, 0.98, 1.05]`, chroma `1`, and phases `[π/2, π/2, 3π/2]`. Keep the preset table alphabetic, place the visible option after Ice, preserve Prism as the default, and leave Background unchanged.
- Alternatives rejected: Changing the default background would affect every preset; replacing Prism would alter existing workspaces; adding new controls, uniforms, or a second palette pass is unnecessary because the current spectrum target and fixed palette uniforms already express the requested look.
- State/output mapping: `dispersion.spectrum = porcelain` resolves through the existing settings reader to the fixed preset table, then uses the same palette uniforms and single field pass as every other built-in Spectrum. Persistence, Settings Transfer, timeline playback, preview, and image export inherit the existing target behavior.
- Performance intent: ordinary-product-work.
- Performance model: This adds one finite lookup value and constant preset data only. It adds no renderer pass, shader branch, texture, loop, allocation, workload dimension, or invalidation path.
- Verification: Focused product/schema tests, typecheck, and one live browser smoke-check are used for this user-directed pass. One bare `npm run verify:delivery` remains the protected delivery authority but is intentionally not run because the user requested no extra checks; no performance suite is authorized.
- Risks: The palette was tuned against the approved reference board; exact appearance still depends on the user's chosen intensity, glow, background, and other existing field settings.

### Iteration 33 — Revert the combined rendering and export expansion

- Request: «давай откатим это изменение» after identifying commit `3cc4b5a` as the batch that substantially changed Lens Distortion and WebGL rendering.
- Task type: Reversible Git rollback of a broad renderer/schema/export batch after a localized Lens Distortion performance complaint.
- User-visible result: The application returns to the complete product state from commit `4f8d3ce`; the Lens Distortion renderer, controls, workload model, preview scheduling, and export behavior again match that previously published state. Features introduced only by `3cc4b5a` are removed together because they were committed as one inseparable batch.
- Source/reference checked: Git history, author/committer metadata, the full `3cc4b5a` file inventory, its Lens Distortion/WebGL diff, and the clean `origin/main` state before rollback.
- Reference inputs: Current repository history only; no external visual reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `decision-contract.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `component-rules.md` and `renderer-technique.md`; plus the systematic-debugging and writing-plans workflows.
- Contract rules applied: `renderer-technique-inventory`, `performance-coverage-levels`, `acceptance-product-observable`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the rollback restores the previous retained two-dimensional WebGL product renderer.
- Interaction ownership: Existing panel ownership for Lens Distortion and canvas ownership for viewport navigation are restored unchanged; no new or duplicated interaction surface is introduced.
- Decision: Apply `git revert --no-commit 3cc4b5a`, retain local diagnostic exclusions in `.gitignore`, and publish one explicit reverse commit instead of rewriting shared Git history.
- Alternatives rejected: Force-pushing `main` would rewrite public history; selectively reverting only a few WebGL files would leave the large cross-file schema, renderer, export, acceptance, and performance refactor internally inconsistent.
- State/output mapping: Every tracked product file changed by `3cc4b5a` returns to its `4f8d3ce` content. Lens enablement and Count again feed the prior retained Paper Lens pass, while preview and export use the earlier pipeline and settings model.
- Performance intent: ordinary-product-work. This is the explicitly requested rollback of a known commit, not a measured optimization iteration; no performance measurement or certification is claimed.
- Verification: One bare `npm run verify:delivery` would derive and run the protected proof; per the user's earlier explicit request, it is not executed for this rollback, so only local build and smoke evidence are claimed.
- Risks: The rollback necessarily removes all features bundled into `3cc4b5a`, not only Lens-related edits. Browser workspaces containing values for removed controls will fall back to the older schema behavior.

### Iteration 32 — Restore two-axis Color Balance Pad

- Request: куда делать настрйока гаммы с помощью пад компонента? почему убрали?; да давай восстанавливать
- Task type: Broken control/state/renderer restoration with focused compound-control acceptance.
- User-visible result: A dedicated Color Balance section again exposes the built-in Pad. Its centered position is neutral; horizontal movement grades Cyan to Red and vertical movement grades Blue to Yellow, updating the canvas live.
- Source/reference checked: Current runtime schema and renderer path, the disconnected historical Color Balance inventory entry, current Git history, and the signed built-in Vector Pad contract.
- Reference inputs: The current app and the user's explicit restoration request; no new external visual reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `decision-contract.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification routes `acceptance-testing.md` and `performance.md`; plus systematic-debugging, brainstorming, and writing-plans workflows.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `interaction-surface-ownership`, `acceptance-product-observable`, `renderer-technique-inventory`, `persistence-policy-explicit`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the Pad grades a completed two-dimensional product image and does not introduce a spatial camera or object pose.
- Interaction ownership: Color balance is panel-owned because exact resettable two-axis grading, persistence, settings transfer, and export parity are useful together; the canvas remains an unobstructed product-pixel surface.
- Decision: Restore `dispersion.colorBalance` as an always-visible built-in `vector` with the `colorBalance` visual variant, Cartesian axes, and neutral `{ x: 0, y: 0 }`. Apply fixed multiplicative RGB gains after saturation in the shared field composite: positive X emphasizes red and negative X cyan; positive Y emphasizes yellow and negative Y blue. The shared field texture is then consumed by the optional retained Paper Lens pass, so preview, Lens output, and image export use the same grade.
- Alternatives rejected: Two sliders lose the requested Pad interaction; an additive tint contaminates black and transparent pixels; changing palette anchors alters spectral construction instead of grading the completed image; a canvas gesture duplicates panel ownership and obscures color judgment.
- State/output mapping: The Vector Pad writes `dispersion.colorBalance.{x,y}` in the bounded `[-1, 1]` domain. Settings normalization clamps both axes, the retained WebGL resource updates one `vec2` uniform on control change, and the existing composite pass converts the axes to multiplicative channel stops before edge fade, background composition, optional Lens, and export.
- Performance intent: ordinary-product-work
- Performance model: One constant-size `vec2` uniform and fixed RGB arithmetic are added to the existing retained composite. There are no new samples, loops, textures, passes, allocations, workload dimensions, or measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: The word “gamma” is represented as practical two-axis color balance, matching the requested Pad model; it is intentionally not a scalar transfer-function gamma control.

### Iteration 31 — Adjustable chiaroscuro: the sheet shades its own depth

- Request: Curve alone reads as position/rotation of the wave, not depth; add adjustable light-and-shadow so the bends read as 3D.
- Decision: One Shading slider (dispersion.shading, default 0). Near the sheet — gated by the existing proximity blend factor so the far veil is untouched — the accumulated glow is modulated by an analytic lighting term: the sheet normal from the exact wave + macro-curve slopes against a fixed upper-left light, mapped to mix(1, 0.28 + 1.25·lit, shading·proximity). Convex lit crests brighten, concave sags and away-facing flanks darken; emission, march, palette, and loop math are otherwise untouched, and zero restores the reference emission exactly.
- Alternatives rejected: the silk-mesh surface shading (replaces the emission model); screen-space gradient shading (fake, ignores actual slopes); baking shading into the palette (couples color to depth).
- User-visible result: With Curve + Shading the bent sheet reads as a volume — the hammock's belly shades, the crest toward the light glows — with strength fully adjustable and defaults unchanged.
- Verification: Harness — shading 0 diff 0 against the reference; loop seams 0; tiles across profiles at shading 45/85 reviewed. Typecheck passes; the protected delivery gate remains the final authority.


### Iteration 30 — Cardinal Curve depth gain in world space

- Request: The Curve controls do not visibly change the wave's 3D shape enough (Arch at 69% reads nearly flat).
- Root cause: A world-space surface offset is divided by ray depth when projected to the screen, so the view-tuned 0.85 gain reads roughly three times weaker than the old view-space bend, and the parabolic profiles are near zero exactly where the visible knot sits.
- Decision: Depth now maps progressively and strongly — uCurveAmp = (depth/100)^1.2 x 2.4 — and the parabola/catenary signs are set to the world-space reading measured at real amplitudes (Valley and Cradle sag into a basin, Arch rises). Emission, march, and profiles are otherwise untouched; depth 0 stays reference-exact.
- User-visible result: Valley/Cradle dive into deep hammocks with thin edge-on flanks, Arch lifts a hill, S-curve sweeps across the whole frame — clearly cardinal at mid depths while low depths stay gentle.
- Verification: Harness tiles at depths 35/69/100 for all profiles reviewed; depth 0 diff 0 against the reference; typecheck passes. The protected delivery gate remains the final authority.


### Iteration 29 — Curve bends the sheet in world space; emission untouched

- Request: The silk-mesh prototype's 3D shape is right, but the current wave's behavior and emission must not change — keep the glow exactly as is and only allow bending it in 3D like the prototype showed.
- Decision: The macro-curve moves from the view transform into the march: the profile term now adds to `waveHeight` inside `marchDispersionSheet` (world space), so the surface the rays graze really dips, arches, or S-swoops in depth — the band thins where it turns edge-on and widens where it faces the camera, with natural fog on far sweeps — while accumulation, palette, interference, channel split, sparkle/grain, fog, and compositing remain byte-identical code paths. The prototype's lit-mesh shading (normals, sheen, rim) is explicitly not shipped. The view-space uv bend is removed; targets, controls, defaults, and acceptance stay as in Iteration 28, with control descriptions reworded to say the sheet bends in 3D.
- Alternatives rejected: shipping the silk surface shading (changes the emission model the user wants preserved); keeping both view- and world-space bends behind a toggle (two subtly different bends for one control invites confusion; world space is the requested behavior).
- User-visible result: The same six Curve profiles at the same depths now bend the glowing sheet itself in 3D perspective; at depth 0 the output is the reference frame byte-exactly, and every other control behaves exactly as before.
- Verification: Harness on the assembled live shader — depth 0 diff 0 against the reference frame; wrap seam diff 0 for all profiles; S-curve/valley/cradle tiles reviewed for the 3D thinning/widening read. Typecheck after the change. `npm run test` / the protected delivery gate remains the final authority.
- Risks: At maximum depth the steepest profile segments cross the sheet at sharp angles where the fixed 38-step march samples the band thinly (reads as an edge-on fold; extreme settings can show slight banding on the steep flanks).


### Iteration 28 — Curve: the sheet's trajectory bends into ribbon shapes

- Request: Not sine-like harmonic tweaks — bend the wave itself into different large-scale shapes, like the supplied glossy-ribbon reference (a deep swooping band with a fold).
- Decision: A static view-space macro-curve applied where view shift and tilt already act, before the march: `uv.y += uCurveAmp * dispersionCurveProfile(uv.x)`. The Curve select offers Line (reference), Valley (parabolic sag), Arch, S-curve (`u(u²−1.1)`, the reference image's crossing swoop), Drape (long sine), and Cradle (catenary sag with steep walls); Curve depth scales the bend (0–100 → 0–0.85 view units). The reference waves keep traveling along the bent trajectory, every mode/effect composes with it, and the Lens post-pass distorts the already-bent sheet automatically. The profile is time-free, so march cardinality and the loop seam are untouched.
- Alternatives rejected: crest waveshaping (triangle/saw/plateau harmonics — prototyped, explicitly not what was asked); bending the surface in world space (perspective makes the screen shape unpredictable and dims grazing energy on steep slopes — measured on the world-split experiment earlier); a second march for a folded ribbon (doubles the fixed 38-step cost).
- State/output mapping: `dispersion.curve` (select) → uCurveForm branch, `dispersion.curveDepth` (slider) → uCurveAmp; defaults Line/0 reproduce the reference frame byte-exactly (harness diff 0). Field Distribution inventory, acceptance rows with full option coverage, pipeline drag/change invalidation, verification-impact ids, and the browser spec (slider + select proofs) follow the Tilt/bend pattern.
- User-visible result: The ribbon can sag into a valley, arch over the frame, swoop through an S like the reference image, drape in a long sine, or hang in a steep cradle, at adjustable depth — while waves, spectra, sparkle/grain, and export behave exactly as before; existing scenes are untouched at the default Line.
- Verification: Harness on the assembled live shader — Line@0 vs reference max channel diff 0; wrap seam diff 0 for all six profiles; sign/orientation of each profile measured from the rendered beam rows. Typecheck on the working tree after the change. `npm run test` / the protected functional delivery gate remains the final authority.
- Risks: Very deep curves push the sheet's grazing band toward steeper screen slopes where it naturally thins (reads as perspective foreshortening); the reference image's solid 3D fold with self-occlusion is out of scope for a single height-field march.


### Iteration 27 — Natural projected Grain Surface distribution

- Request: distribution surface как-то нереалистично работает
- Task type: Renderer/canvas visual correction to the existing Grain distribution branch.
- User-visible result: Surface now distributes the undistorted Paper grains smoothly across the projected 3D light sheet instead of creating synthetic crest islands. Grain remains luminous over the wave and no longer replaces or darkens the white caustic; Screen keeps its existing flat source-over appearance.
- Source/reference checked: Fixed browser captures of the same paused Grain frame in Screen and Surface, the current 38-step raymarch accumulator, and the pinned official Paper Grain noise, shape, and color-ramp recipe.
- Reference inputs: The current app and the user's direct visual feedback; no new external reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `decision-contract.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `component-rules.md`, `renderer-technique.md`, and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the correction changes only a projected output mask inside the retained WebGL material.
- Interaction ownership: Distribution stays panel-owned through the existing Screen/Surface select, and the canvas remains the product-pixel output. No duplicate canvas operation is introduced.
- Decision: Keep Paper's output-pixel `grain_uv` and complete grain recipe unchanged. Replace the discontinuous weighted-position height/noise map with an exposure-style projection of accumulated ray/sheet hit energy, `1 - exp(-k * sheetW)`, and use it exactly once as final alpha coverage. Keep Grain Amount as Paper's sole density/color input. Preserve Screen source-over compositing and use non-darkening spectral emission only for Surface.
- Alternatives rejected: Sampling Paper noise in 3D would bend and resize individual grains; adding another value-noise terrain recreates synthetic islands; selecting only positive wave height suppresses equally real troughs; multiplying both Paper Amount and opacity couples placement to color and density; replacing the wave with grain color flattens the caustic.
- State/output mapping: `effect.grain.distribution = screen` remains uniform Area-masked Paper compositing. `surface` converts the existing constant-cost `sheetW` accumulator into continuous projected coverage, while Amount, Scale, Softness, Distortion, Drift, Area, spectrum, timeline phase, preview, and image export retain their current mappings.
- Performance intent: ordinary-product-work
- Performance model: The change replaces constant scalar GLSL math inside the existing fixed 38-step field pass. It adds no pass, texture, loop, allocation, workload dimension, invalidation, or measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Surface intentionally follows projected hit confidence rather than rotating grain dots with local normals; this preserves the requested flat, unbent particles while giving their density a stable 3D attachment.

### Iteration 26 — Denser upper Grain Amount range

- Request: при этих настройках мало шума все равно я хочу чтобы было еще больше и плотнее
- Task type: Renderer/canvas visual correction with focused acceptance.
- User-visible result: Grain Amount 100 now shows substantially more Paper-style particles and visibly greater coverage than the midpoint while preserving each grain's existing size, spectral shape, softness, and Screen/Surface placement behavior.
- Source/reference checked: The user's supplied control-state and rendered-output captures, the pinned official Paper Grain Gradient kernel and shape recipe, and the current Dispersion grain composite.
- Reference inputs: `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-c865079e-c40a-4d97-b965-df173906c930.png` and `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-ef9b444a-3a60-4e08-bf1c-d4df77dbf9d5.png`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `decision-contract.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification routes `acceptance-testing.md` and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `controls-product-coverage`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the change modifies procedural grain coverage and adds no camera or editable spatial object.
- Interaction ownership: Grain Amount remains panel-owned, while the canvas remains the product output used to judge the live pixel result.
- Decision: Keep the current Paper field as the primary layer and evaluate two decorrelated copies using translation-only UV offsets. Preserve the existing field through the middle of Amount, progressively introduce the second field over `0.48..0.82`, and the third over `0.72..1.0`. Combine their opacity with union alpha and blend their spectral colors by contribution, increasing the number of particles rather than only their brightness. Use the same stable, unwarped screen coordinate in Screen and Surface; Surface continues to affect distribution only.
- Alternatives rejected: Raising the existing alpha gain makes the same sparse particles brighter without adding enough grain; changing Scale makes particles smaller rather than denser; world-space offsets would reintroduce grain deformation on the 3D map; a user-controlled sample-count loop would turn Amount into a workload dimension.
- State/output mapping: `effect.grain.amount=0` remains visually neutral. Around 50 the primary Paper field remains dominant. Above the midpoint, fixed secondary and tertiary fields increase pixel coverage, reaching all three fields at 100. Scale, Softness, Distortion, Drift, Area, Distribution, preview, and image export keep their existing mappings.
- Performance intent: ordinary-product-work
- Performance model: The Grain branch now performs three fixed evaluations of the already-retained Paper kernel with no dynamic loop, added texture, pass, allocation, resolution bound, or user-controlled workload dimension. The extra cost applies only when Grain is active and creates no measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: The fixed Grain branch is heavier than one evaluation; measured performance was not requested, so this batch preserves visual authority and uses functional/browser proof only.

### Iteration 25 — Denser Paper Grain with undistorted 3D-map distribution

- Request: ноиз все еще будто мелкий по количеству еще раз посмотри пример на пейпер там прямо его больше. посмотри что у нас не так по параметрам. еще сейчас ноиз который накладывается на 3д карту будто снова 2д и два режима ноиза не сильно отличаются друг от друга; зерно искажаться не должно, но должно работать по 3д карте
- Task type: Renderer/canvas visual correction, schema semantics, and focused acceptance.
- User-visible result: Grain Amount now reaches Paper's full normalized noise range, the default Scale uses Paper's patterned-grain frequency, and Surface is visibly driven by the raymarched wave relief. Individual grains retain the same stable shape and size instead of stretching with 3D perspective.
- Source/reference checked: The official Paper Grain Gradient page, the pinned `@paper-design/shaders` 0.0.80 Grain Gradient fragment and shared vertex source, and the current Dispersion composite shader.
- Reference inputs: `https://shaders.paper.design/grain-gradient` plus the user's explicit no-distortion clarification.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/reference-study.md`, `core/runtime-boundary.md`, `assembly-workflow.md`, `core/performance.md`, `decision-contract.md`, `core/control-selection.md`, and `core/layout.md`; Implementation routes `schema-reference.md`, `decision-contract.md`, `renderer-technique.md`, `performance.md`, and `component-rules.md`; Verification routes `acceptance-testing.md` and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `controls-product-coverage`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the raymarched map controls procedural effect placement and adds no editable camera or spatial object.
- Interaction ownership: Effect, Distribution, Amount, Scale, Softness, Distortion, and Drift remain panel-owned. The canvas remains product output and runtime viewport surface.
- Decision: Map Amount linearly from `0..100` to Paper `noise=0..1`. Map Scale to `0.8..2.4`, keeping Paper's patterned-grain `1.6` multiplier at the control midpoint, and normalize the grain coordinate against a 720 px short-side reference so render scale and device pixel ratio do not change its shape. Alpha-composite Paper's spectral result over the wave instead of hiding it in an additive saturated highlight. Use the same screen-stable `grain_uv` in Screen and Surface. In Surface, feed the existing raymarched sheet confidence, accumulated world position, and height into a low-frequency distribution map for Paper's noise amount and opacity, producing relief-driven density and brightness without changing UV geometry.
- Alternatives rejected: Sampling Paper noise directly in world/surface coordinates would stretch, compress, and rotate grains with perspective, contradicting the user's clarification. Keeping only the previous broad confidence mask leaves Screen and Surface too similar. Increasing only final RGB gain does not restore Paper's missing noise-range density.
- State/output mapping: `effect.grain.amount` now covers Paper's complete noise domain; `effect.grain.scale` controls a stable Paper-like spatial frequency; `effect.grain.distribution=screen` uses uniform selected-Area placement, while `surface` modulates amount and opacity from `sheetW`, accumulated world position, and wave height. Preview and image export consume the same mapping and timeline phase.
- Performance intent: ordinary-product-work
- Performance model: The change adds only fixed scalar math to the existing retained 38-step field pass. It adds no samples, loops, textures, passes, allocations, or workload-bound changes and creates no measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Surface uses a raymarched relief map rather than a geometric UV unwrap by design; this preserves undistorted grains while making their distribution, not their shape, respond to the 3D sheet.

### Iteration 24 — Finish live Lens slider preview

- Request: продолжай, сейчас уже не тормозит, тольк обновление нужно на лету а не при отпускании слайдера, сделай необходимые минимальные изменения, чтобы закончить задачу
- Task type: Renderer/canvas/runtime scheduling correction and focused browser acceptance.
- User-visible result: Lens controls update the exact canvas while the pointer remains held down; the preview no longer waits for slider release or a trailing idle window.
- Source/reference checked: The optimized retained Paper Lens pass and the real Base UI slider pointer sequence used by product browser acceptance.
- Reference inputs: The current app and the user's exact live-update request; no new external reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `core/runtime-boundary.md`; `core/performance.md`; `renderer-technique.md`; `acceptance-testing.md`; and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `acceptance-product-observable`, `renderer-technique-inventory`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the change affects preview scheduling only.
- Interaction ownership: Lens switches and sliders remain panel-owned, and the canvas remains the product-pixel output plus runtime viewport surface.
- Decision: Replace the trailing 600 ms debounce with a 120 ms latest-value-wins live throttle and one-frame leading delay. Start each interval after the previous exact frame completes so slow GPUs coalesce stale values rather than building a queue.
- Alternatives rejected: Rendering every input event can recreate GPU backlog; lowering backing scale, Paper Count, shader samples, or animation quality violates the user's fidelity constraint.
- State/output mapping: Toolcraft state updates immediately. `data-lens-requested-count` records the current slider request and `data-lens-count` records the completed Paper frame; focused browser proof requires convergence before `pointerup`.
- Performance intent: ordinary-product-work
- Performance model: This is functional steering of invalidation timing and creates no new measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: On a very slow GPU, live cadence is bounded by actual full-quality frame completion; the preview skips stale intermediate states but never reduces visual quality.

### Iteration 23 — Remove Border mode

- Request: Remove the Border mode.
- Task type: Renderer/canvas/runtime schema, persistence, acceptance, and browser-proof cleanup.
- User-visible result: Dispersion Studio now exposes one canonical animated 3D light-sheet field. The Wave Mode selector and all Border-only controls are absent; Frame shape, Field variants, Sparkle, Grain, Lens Distortion, timeline playback, and image export remain available.
- Source/reference checked: The current local schema, retained WebGL renderer, persistence model, product acceptance map, and browser proof. No new external reference was required.
- Reference inputs: None.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification route `acceptance-testing.md`; plus brainstorming, writing-plans, and browser workflows.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `renderer-technique-inventory`, `acceptance-product-observable`, `persistence-policy-explicit`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; removing a renderer branch does not introduce a camera or editable spatial object.
- Interaction ownership: The obsolete distribution property and its panel ownership entry are removed. The remaining Frame, Field, effect, Lens, color, motion, background, and export operations keep their existing owners.
- Decision: Delete the distribution target, Border parameters, Wave Mode section, Border shader material, Paper Pulsing Border adapter, WebGL branch, test fixtures, browser proofs, and verification ownership. Advance persistence from version 8 to 9 so a saved Border selection cannot restore an unavailable branch. Keep the shared 38-step light-sheet core as the only field renderer.
- Alternatives rejected: Hiding only the selector would leave unreachable state and GPU code; keeping the old persistence version could restore removed values; mapping Border to another Field variant would silently change saved output.
- State/output mapping: Current settings no longer parse or emit `dispersion.distribution` or `border.*`. Obsolete imported keys are ignored by the current schema. Preview and image export use the same canonical light-sheet material, while all unaffected controls preserve their prior mappings.
- Performance intent: ordinary-product-work
- Performance model: Removing a mutually exclusive material and its conditional controls reduces code and retained GPU resources without changing the canonical field pass, Lens sample bounds, image resolution bounds, or animation cadence. No measured-performance authority is created.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Old settings files may still contain obsolete Border keys; schema import intentionally ignores them rather than translating them to another visual mode.

### Iteration 20 — Exact Inside wave wrapped around the perimeter

- Request: Correct Border because Paper was only a construction reference for following the perimeter; the visible wave must remain the same 3D wave as Inside, move cyclically around the frame, shimmer, and retain controls for spread, distance from center, and the rest of its optical character.
- Task type: renderer/canvas/runtime visual correction across the primary shader, preview/export density, control semantics, acceptance, and browser proof.
- Reproduction: The prior Border capture showed four isolated bright capsules. Source inspection confirmed a separate 24-step march, different camera/wave/palette/glow constants, a nearest-packet mask, and a narrow Paper stroke; it could not match the 38-step Inside field. A first continuous prototype also exposed diagonal route creases on broad influence settings.
- User-visible result: Border now displays one continuous folded spectral 3D wave around the complete Rectangle, Rounded, or Circle perimeter. It morphs with the same field animation as Inside while the whole pattern advances forward around the closed route. The center stays background; broad influence passes smoothly through corners without rays converging toward the center.
- Source/reference checked: The current Inside shader and captured Inside reference frame are visual-character authority. The user-supplied Paper Pulsing Border page and pinned `@paper-design/shaders` 0.0.80 source are used only for rounded signed-distance construction, derivative antialiasing, and smoke helpers.
- Reference inputs: `https://shaders.paper.design/pulsing-border` and the user's clarification that Paper must not replace or restyle the central wave.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `decision-contract.md`; `core/runtime-boundary.md`; `core/performance.md`; `core/timeline-animation.md`; `renderer-technique.md`; `performance.md`; `component-rules.md`; plus the systematic-debugging, brainstorming, writing-plans, and browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `timeline-enabled-behavior`, `controls-product-coverage`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; Border changes the procedural output domain and adds no editable camera or object pose.
- Interaction ownership: Existing panel controls remain the single precise-value surface. Canvas handles remain rejected because they would duplicate Position, Frame margin, Influence area, and route controls while obscuring the wave.
- Decision: Extract the complete Inside optical recipe into one shared GLSL core: the fixed 38-step raymarch, three sine folds, interference, refraction/channel split, distance fog, cosine palette, caustic accumulators, glow, Sparkle, Paper Grain, saturation, and compositing. Inside uses identity coordinates. Border embeds the same open x-z sheet into a periodic virtual tube, with the Paper rounded SDF supplying signed transverse distance and a separate rounded guide supplying a smooth longitudinal coordinate through broad corners. Remove packet selection, gaps, and all Border-only optical constants. Copy every shared uniform value from Inside; add only contour geometry, field scale, continuous repetition, pulse/turbulence envelope, and an integer-circuit route phase. Preview now shades Border at the same density as Inside.
- Alternatives rejected: Retuning the 24-step packet shader would preserve a different wave model; mounting Paper's React renderer would bypass Toolcraft state and export; a hard nearest-contour route creates visible diagonal medial seams on wide influence bands; a center-angle route creates visible rays toward the center; a narrow border mask collapses the broad 3D veil into a flat glowing outline.
- State/output mapping: `dispersion.distribution` selects identity or closed-tube domain over the same shared core. `border.spots` is continuous route repetition, `border.spotSize` sets longitudinal field scale, `border.influence` sets transverse reach, `dispersion.inset` moves the contour relative to the canvas center, and `dispersion.position` offsets the wave along the local normal. Field, spectrum, motion, Sparkle, Grain, background, timeline, and Lens settings keep the same uniform meanings in both modes. Preview and image export share the retained renderer and current timeline phase.
- Performance intent: ordinary-product-work
- Performance model: Both modes now use one fixed 38-step maximum march; Border early-rejects fragments outside its bounded influence envelope and adds no dynamic or nested user-controlled loop. This visual correction creates no measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: The rounded longitudinal guide can be broader than the visible corner radius solely to keep wide influence fields smooth; the Paper SDF remains the actual visible contour. Border's shared march costs more than the rejected 24-step approximation, but exact visual character is the requested authority and performance measurement was not requested.

### Iteration 21 — Responsive Paper Lens application without quality reduction

- Request: давай по оптимизируем перф применения эффекта сейчас довольно таки тормозит. мы не должны терять в качестве и визуале.
- Task type: localized request-authorized performance iteration for applying and adjusting the optional Paper Lens Distortion preview effect.
- Reproduction: A real-browser trace isolated first-use shader compilation on the interaction that enabled Lens, followed by synchronous GPU submission barriers and a heavy first composited frame. Steady Lens cost correctly remains proportional to the official Count value; no renderer-resource rebuild or changing pass count was found.
- User-visible result: The Lens switch paints responsively, the retained Paper program is compiled before first use, and the exact Lens frame follows on the next animation tick even when playback is paused. Lens parameter edits reuse static uniforms and retained targets without synchronous per-preview GPU flushes; while Lens is active, output-control bursts retain the last exact frame and render only their newest full-quality composition after a 600 ms idle debounce. The selected render scale, Paper loop, Count range, frame cadence, colors, blur, blend, and export pixels are not reduced.
- Source/reference checked: The installed `@paper-design/shaders` 0.0.80 Lens GLSL, Three.js 0.185 `compileAsync` and WebGL renderer lifecycle, current Toolcraft renderer pipeline, browser trace, and exact canvas backing inspection.
- Reference inputs: The current app plus the user's exact localized performance request; no new visual reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `decision-contract.md`; `core/runtime-boundary.md`; `core/performance.md`; `component-rules.md`; `renderer-technique.md`; `acceptance-testing.md`; and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `renderer-technique-inventory`, `performance-coverage-levels`, `acceptance-product-observable`, `output-export-required`, `timeline-enabled-behavior`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the iteration changes GPU scheduling and resource lifecycle only.
- Interaction ownership: The Lens switch and sliders remain panel-owned, while the canvas remains the pixel output and runtime viewport surface. The one-tick activation yield does not add another operation surface or deferred slider value model.
- Decision: Precompile the retained official Paper Lens material through Three's parallel compile path; cache its unchanged source/aspect/parameter assignments; compare settings directly against retained numeric uniforms without allocating a 20-field parameter object per frame; coalesce output-control changes with latest-value-wins scheduling whenever Lens is already active while leaving the previous exact frame visible; remove preview `flush()` calls while retaining deterministic fullscreen clears and `preserveDrawingBuffer` for complete pixel observability; keep explicit `finish()` only for deterministic snapshot capture; and yield the first Lens activation frame once so the control state can paint before submitting the exact 35–50-tap frame. Resolve the pending 50-sample render-plan benchmark with real Canvas 2D and WebGL candidates and select the existing WebGL pass.
- Alternatives rejected: Lowering Resolution scale, Count, render cadence, raymarch density, texture size, or output backing would violate the explicit fidelity constraint. Replacing Paper's shader with an approximation would break source parity. Disabling framebuffer preservation breaks complete off-viewport pixel inspection; keeping per-frame GPU barriers preserves avoidable synchronization. Moving WebGL to a worker is a broad renderer rewrite outside this localized iteration.
- State/output mapping: `lens.enabled` and all twenty Paper values keep their existing Toolcraft targets and official uniforms. Enabling Lens schedules the same retained post-process one tick after the switch paints; continuous Count interaction exposes its newest scheduled value immediately while the canvas separately records the last fully rendered Count, so performance proof measures input response and then waits for exact-frame completion before accepting the outcome. `canvas.renderScale` still resolves to exact CSS × DPR × selected scale backing, and still export runs the same full-resolution snapshot path with explicit completion.
- Performance intent: performance-iteration
- Performance request evidence: "давай по оптимизируем перф применения эффекта сейчас довольно таки тормозит. мы не должны терять в качестве и визуале."
- Performance paths: ["performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22dispersion.preview-frame%22%5D%2C%5B%22gpu%22%5D%2C%5B%22lens-samples%22%5D%5D","performance-path:%5B%22interactive-discrete%22%2C%22control-change%22%2C%5B%22dispersion.preview-frame%22%5D%2C%5B%22gpu%22%5D%2C%5B%22lens-samples%22%5D%5D"]
- Performance model: Both selected paths use the reachable development fixture for `lens-samples`; Count remains bounded at the official maximum of 50. The preview pass stays one full-fidelity GPU composite with retained renderer-scoped resources, while program creation moves to the once-only shader-resource lifecycle and interaction frames no longer force immediate command completion.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Browsers without `KHR_parallel_shader_compile` execute Three's compile fallback during idle initialization rather than the enabling click. GPU-bound steady animation still scales with the user-selected official Count; this iteration intentionally does not disguise that cost by reducing pixels, samples, cadence, or visual complexity.

### Iteration 22 — Live full-quality Lens updates during slider drag

- Request: продолжай, сейчас уже не тормозит, тольк обновление нужно на лету а не при отпускании слайдера, сделай необходимые минимальные изменения, чтобы закончить задачу
- Task type: Renderer/canvas/runtime scheduling correction and focused browser acceptance inside the existing Lens delivery.
- User-visible result: Lens controls now update the canvas while the pointer is still held down. The renderer submits the newest exact composition at a bounded live cadence instead of waiting for a 600 ms quiet window after the gesture.
- Source/reference checked: The current optimized renderer, the existing retained official Paper Lens pass, and the real Base UI slider pointer sequence used by product browser acceptance.
- Reference inputs: The current app and the user's exact live-update request; no new external reference.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `core/runtime-boundary.md`; `core/performance.md`; `renderer-technique.md`; `acceptance-testing.md`; and `performance.md`; plus brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `acceptance-product-observable`, `performance-coverage-levels`, `renderer-technique-inventory`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the change affects preview scheduling only.
- Interaction ownership: Lens switches and sliders remain panel-owned, and the canvas remains the product-pixel output plus runtime viewport surface.
- Decision: Replace trailing-only debounce with a 120 ms latest-value-wins live throttle and a one-frame leading delay. The interval starts after the previous exact render finishes, preventing queued stale GPU work while still producing repeated full-quality frames during a continuous drag. Add a focused Count proof that waits for `data-lens-count` before releasing the pointer.
- Alternatives rejected: Restoring render-on-every-input recreates avoidable GPU backlog; preview downscaling, reducing Count, or changing the Paper loop violates the fidelity requirement; worker migration is not a minimal finishing change.
- State/output mapping: Toolcraft slider values remain immediate. `data-lens-requested-count` records the newest requested state and `data-lens-count` records the exact rendered state; during browser proof both converge while `pointerdown` is still active. Export remains synchronous and full resolution.
- Performance intent: ordinary-product-work
- Performance model: This functional steering changes only preview invalidation frequency. Work stays one retained full-quality Paper pass per admitted tick with no new pass, allocation, sample reduction, or workload-bound change; no new measured-performance authority is requested.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Extremely slow GPUs can display fewer live ticks because the next 120 ms slot begins only after the preceding exact frame completes; this is intentional backpressure rather than visual degradation.

## Archived Decision History

#### Archived iteration 19 — Optional official Paper Lens Distortion post-effect

- Request: Inspect `https://shaders.paper.design/lens-distortion`, install and use `@paper-design/shaders-react`, expose the same settings as Paper, and make Lens Distortion additive rather than a replacement for Inside or Border.
- Task type: renderer/canvas/runtime scope dependency-backed renderer, schema, persistence, export-parity, acceptance, and browser-proof extension.
- User-visible result: `Lens Distortion — Sampling`, `Lens Distortion — Warp`, and `Lens Distortion — Texture` expose an independent Effect switch plus Paper's complete 20-parameter lens model. Enabling it filters the already-rendered Inside or Border frame; disabling it preserves the existing output. Warp and Texture stay available as preconfiguration while the effect is off, while sampling controls that cannot have a visible result are conditional.
- Source/reference checked: The live Paper Lens Distortion page; official `paper-design/shaders` repository source; pinned npm packages `@paper-design/shaders` and `@paper-design/shaders-react` 0.0.80; package-authored `lensDistortionFragmentShader`, `lensDistortionMeta.maxSamples`, and `LensDistortionProps` defaults/value model.
- Reference inputs: `https://shaders.paper.design/lens-distortion`, `https://github.com/paper-design/shaders/blob/main/packages/shaders/src/shaders/lens-distortion.ts`, and the user's requirement that the effect compose with both existing wave modes.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/reference-study.md`, `core/runtime-boundary.md`, `assembly-workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/performance.md`, `core/timeline-animation.md`, and `core/setup-export.md`; Implementation routes `schema-reference.md`, `decision-contract.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `interaction-surface-ownership`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, `output-export-required`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; the Paper lens deforms the completed two-dimensional product frame and introduces no editable camera or model pose.
- Interaction ownership: The panel exclusively owns exact Paper values, reset, persistence, settings transfer, and preview/export parity. Canvas handles were rejected because they would obscure the filtered pixels and duplicate a normalized 21-control parameter model that requires precise numeric editing.
- Decision: Import Paper's real GLSL 300 fragment source and maximum sample metadata from the pinned package, strip only the duplicate version directive required by Three's GLSL3 material wrapper, and retain one fullscreen post-process material plus separate source/destination render targets. The existing Inside or Border shader renders first; Paper's multi-tap chromatic sampling, bias, perspective, focus, swirl, noise, bulge/circle mapping, grain, and source pan then filter that texture. Preview filters at the existing branch-specific internal density and linearly resolves into Toolcraft's exact backing; deterministic image export omits preview scaling and filters every artifact pixel. The official defaults are preserved, and Count is clamped to Paper's authored maximum of 50.
- Alternatives rejected: Mounting Paper's React canvas above the product (would bypass Toolcraft state, masking, timeline, and export); rewriting the visual from screenshots (would lose Paper's actual sample weighting and warp model); replacing Inside or Border (explicitly conflicts with the additive request); performing source and destination work in one texture (undefined WebGL feedback); duplicating canvas size/export controls inside the lens sections (runtime already owns them).
- State/output mapping: `lens.enabled` gates one post-process after `dispersion.distribution` has selected Inside or Border. `lens.spread`, `bias`, `angle`, `perspective`, `count`, `dispersion`, `dispersionShift`, `dispersionColor`, `focusCenter`, `focusEdges`, `swirl`, `lensBulge`, `lensCircle`, `noise`, `noiseFrequency`, `noiseOffset`, `grainMixer`, `grainOverlay`, `imageX`, and `imageY` map one-to-one to the official uniforms. All values persist through the Toolcraft schema (version 8), reset to Paper defaults, participate in settings transfer, and feed the shared preview/export renderer.
- Performance intent: ordinary-product-work
- Performance model: `lens-samples` is an explicit 2–50 maximum-bound workload dimension because Count changes the official loop's active taps. The retained preview/export pass costs are modeled as linear in lens samples; assessment correctly exposes a pending protected kernel benchmark requirement. Per contract, ordinary functional delivery defers that benchmark and creates no measured-performance authority.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Count 50 intentionally increases fragment sampling cost; the pending kernel comparison remains visible until a separately authorized performance iteration. Paper's shader composites transparent source pixels over white internally, matching the package source. The adapter fails fast if a future package version removes the expected GLSL 300 marker, which is why package versions are pinned.

#### Archived iteration 1 — Adjustable animated dispersion studio

- Request: Create an application for adjustable animated dispersion inside a resizable canvas, support rectangles, circles, and canvas corner rounding, study the attached motion reference in detail, preserve its changing iridescent flow, add central and edge-glass dispersion types plus useful alternatives, and expose inset, height, and related optical settings.
- Task type: First product delivery covering reference study, schema, controls, custom raster renderer, editable canvas, timeline, persistence, image export, acceptance, and browser behavior.
- User-visible result: Users can choose Rectangle, Rounded, or Circle output masks; select Central, Edge Glass, Halo, Diagonal, or Ripple fields; tune position, edge inset, height, refraction, spread, softness, bend, five spectra, intensity, glow, and deterministic motion; scrub or play one seamless loop; resize the finite canvas; and export the selected frame as PNG or JPG.
- Source/reference checked: `/Users/kusnizza/Desktop/CleanShot 2026-08-11 at 09.25.06.mp4`; complete 18.4-second 4096×1904 H.264 recording; one-second extraction and 4×5 contact sheet under `.toolcraft/browser-artifacts/reference-video`; detailed frames 01, 06, 12, and 18.
- Reference inputs: The user-provided CleanShot recording and the explicit request for central, edge-glass, circular, rectangular, rounded-canvas, inset, height, and additional dispersion behavior.
- Video Reference Study: The reference keeps a stable light-gray frame while one continuous caustic ridge evolves from a shallow step through a crest and crossing into a rising diagonal. Cyan/blue, magenta/red, yellow/orange, and white layers separate and recombine around that ridge; broad low-opacity bloom persists on both sides. The implementation maps these frame-to-frame deltas to Central geometry, independent spectral offsets, white glow, and forward-only periodic timeline acceptance.
- Docs/contracts read: `docs/toolcraft/workflow.md`; plan routes `core/reference-study.md`, `core/runtime-boundary.md`, `assembly-workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/performance.md`, `core/timeline-animation.md`, `core/setup-export.md`, and `core/media-upload.md`; implementation routes `decision-contract.md`, `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `canvas-surface-preserved`, `infinity-canvas-scene-bounds`, `interaction-surface-ownership`, `layers-enable-only-when-needed`, `timeline-mode-choice`, `timeline-enabled-behavior`, `controls-product-coverage`, `output-export-required`, `controls-section-inventory-required`, `controls-component-layout-invariants`, `renderer-technique-inventory`, `renderer-view-interaction`, `video-reference-analysis`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: `non-spatial`; the product is a two-dimensional optical field with no 3D model, camera orbit, or orientation target.
- Interaction ownership: The panel owns frame mask properties, exact field placement/inset values, and optical properties. Canvas alternatives were rejected because they would duplicate the same values, reduce precision, and overlay chrome on the output. The canvas remains product pixels plus runtime pan/zoom.
- Decision: Use a constant-count analytic Canvas 2D compositor shared by preview and export. Build periodic line, perimeter, and ellipse paths; layer broad blurred spectral lobes, narrower channel offsets, and one white caustic; clip the complete output to Rectangle, Rounded, or Circle. Use the top Toolcraft playback timeline with an eight-second product-derived forward-only loop.
- Alternatives rejected: CSS-only filters cannot keep independent channel geometry and deterministic export. SVG filter graphs add complex blur-bound handling. WebGL adds shader compilation, GPU resource ownership, and export readback for a small fixed path set. Canvas handles were rejected in favor of precise built-in panel controls. Layers and media upload were rejected because the product has one procedural output and no source collection.
- State/output mapping: Schema targets feed one normalized `DispersionSettings` model. `frame.*` selects the mask; `dispersion.*` selects geometry and optical composite parameters; `motion.*` changes periodic coefficients; timeline progress selects the deterministic frame; `export.includeBackground` and `appearance.background` drive preview/export background semantics; runtime canvas frame and render scale determine live backing; runtime image settings determine artifact encoding and dimensions.
- Performance intent: ordinary-product-work
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: Canvas 2D blur antialiasing can differ slightly across browser raster backends, so semantic structure rather than byte identity is acceptance authority. JPG cannot preserve transparent rounded/circular corners. Very large 8K blur/export work remains batch-only and is modeled by the image-long-edge workload dimension.

#### Archived iteration 2 — Article-grounded shader refraction correction

- Request: Correct the renderer because its blend, colors, blur, and overall dispersion do not resemble the supplied reference; then study Maxime Heckel's refraction/dispersion article and base the effect on its examples.
- Task type: Major renderer and reference-facing motion correction within the first coherent product delivery.
- Reproduction: A real-browser default Central capture at phase 0 showed a nearly white screen-only composite with broad pastel ribbons and a uniform sinusoidal path. Reference frames 01, 06, and 12 show a much darker asymmetric glass volume, narrow saturated RGB edges, a compact white caustic, and localized step/crest/crossing geometry.
- Source/reference checked: The full user video and extracted storyboard; Maxime Heckel's “Refraction, dispersion and other shader light effects” article and its linked refraction, chromatic aberration, multisampling, RYGCBV, saturation, lighting, Fresnel, and front/back-pass examples.
- Root cause: Every authored optical layer used `screen`, so broad colored strokes lost shadow and saturation against the near-white background. The line path distributed sine harmonics across the full width instead of concentrating deformation in one moving fold. More fundamentally, the implementation painted colored blur instead of sampling a scene through a refractive normal field, so it had no IOR separation, optical volume, or view-dependent light response.
- User-visible result: Central dispersion becomes a layered refractive glass fold: broad dark neutral volume, muted chromatic haze, compact saturated channel separation, a thin white caustic, and restrained bloom. The seamless loop morphs through localized step, crest, crossing, trough, and diagonal phases. Edge Glass, Halo, Diagonal, Ripple, all masks, controls, and still export retain the same ownership and state model.
- Decision: Replace the live renderer with a retained WebGL2 engine derived from the article. It renders a neutral procedural scene into a bounded framebuffer, computes a signed-distance glass normal, applies `refract` with six RYGCBV IOR offsets and fixed multisampling, then adds saturation, transmitted dark volume, Blinn–Phong specular, and Fresnel. Keep preview and export as the two product-visible passes, add one retained shader-resource pass, and preserve a Canvas 2D fallback only for WebGL-unavailable environments. Reuse the mounted WebGL target for deterministic runtime-owned still export instead of allocating an export canvas.
- Alternatives rejected: Raising saturation alone cannot restore missing dark volume; adding more screen layers further washes out the light background; a purely uniform background cannot reveal refraction; preset artwork would violate procedural control and export parity. A third-party 3D engine is unnecessary because the product needs one full-screen analytic field, not a mesh scene graph.
- State/output mapping: Height controls volume thickness and normal height; Refraction controls base IOR/displacement; Spread controls IOR separation; Softness controls multisample aperture and volume falloff; Bend controls fold curvature; Intensity controls reconstructed spectral saturation; Glow controls specular/Fresnel energy; Motion values move the periodic fold and micro-normal phase. Schema targets, persistence slices, timeline mode, layers, and artifact ownership remain unchanged.
- Performance intent: ordinary-product-work. The shader uses fixed bounded sample cardinality and one bounded procedural-scene framebuffer; this visual complaint does not authorize measured performance.
- Verification: Four browser phase captures are compared against the extracted storyboard, focused acceptance covers background/render-scale evidence, and the protected functional delivery lifecycle remains the final authority.
- Risks: Canvas blur kernels remain backend-dependent; reference parity is structural and perceptual rather than byte-identical. Normal-composited shadows must remain restrained so transparent PNG output does not appear as an opaque band when Background is off.

#### Archived iteration 3 — Clean article mesh-optics baseline

- Request: Stop attempting to recreate the original video and first make dispersion realistic, clean, and correct according to Maxime Heckel's article; the current blend, colors, and blur are rejected as dirty and optically wrong.
- Task type: broad renderer/schema scope replacement of the renderer model, control semantics, optical acceptance, and reference authority inside the same first-delivery batch.
- Source/reference checked: Maxime Heckel's complete “Refraction, dispersion, and other shader light effects” article, including scene FBO, mesh eye/normal vectors, refraction, multisampling, RYGCBV conversion, saturation, Blinn–Phong lighting, Fresnel, and backside/frontside FBO ordering. The CleanShot video is explicitly deferred for this iteration.
- Reproduction: The current shader calls `refract(...)` but then paints the dominant image with `spectralBand`, palette interpolation, `upperLobe`, `lowerLobe`, `lobeTint`, analytic `ridgeHeight`/`fieldDistance`, manual shadow subtraction, and a synthetic core/bloom. Those terms overpower transmitted scene light and cause the reported dirty blend.
- User-visible result: A rounded glass mesh refracts a crisp neutral test scene with angle-dependent displacement, smooth six-channel spectral separation, internal back-face light, focused specular response, and Fresnel edges. Presets select closed mesh forms and optical-glass IOR curves instead of painted 2D fields and arbitrary color palettes.
- Decision: Replace the raw full-screen height-field compositor with a retained Three.js/WebGL2 mesh renderer. Render the source scene to one FBO, the refractive back faces to a second FBO, and the front faces to the visible target. Use sixteen fixed article samples and exact RYGCBV reconstruction; add no direct spectrum, lobe, caustic, shadow, or blur layer. Animate only periodic mesh/light transforms.
- Interaction ownership: Panel controls continue to own exact mask, geometry presentation, optical values, and motion values; the canvas remains pointer-transparent output plus runtime viewport navigation. Fixed camera presentation is reference-backed by the article demo and is not directly edited.
- State/output mapping: Existing persisted spectrum IDs are relabeled as Article, Crown glass, Flint glass, Diamond, and Achromatic IOR presets. Refraction controls screen-space displacement; Spread controls IOR separation; Softness controls multisample aperture; Height/Inset/Position/Bend control mesh presentation; Intensity controls luminance saturation; Glow controls diffuse/specular/Fresnel energy; motion controls change periodic object and light transforms.
- Alternatives rejected: Tuning the painted lobes would preserve the root cause. A uniform background cannot reveal refraction. Post-process chromatic aberration would split the whole frame instead of only the refractive mesh. A single front-face pass misses the article's refracted backside highlights.
- Performance intent: ordinary-product-work. The mesh complexity, two reusable FBOs, and sixteen shader samples are bounded; this visual correctness complaint creates no measured-performance authority.
- Verification: Focused browser proof checks source-scene sharpness, inside-mesh displacement, localized wavelength separation, achromatic reduction, parameter response, mask transparency, and preview/export parity. One bare `npm run verify:delivery` remains the final functional authority.
- Risks: The article technique is an artistically convincing real-time approximation rather than a spectral path tracer. Screen-space refraction clamps at the framebuffer edge, and transparent export retains only the glass silhouette while still sampling the hidden test scene.

#### Archived iteration 4 — Reference implementation port: octolane.com raymarched light sheet

- Request: Stop approximating with authored shapes; study the live reference implementation on octolane.com, port how its beam and animation are actually built, and apply it to this app first, tuning later. The Vercel and Maxime Heckel articles remain supporting context from the same request thread.
- Task type: broad renderer/schema scope renderer-model replacement inside the same product; schema copy, e2e background tolerance, and performance prose realigned.
- Source/reference checked: The live octolane.com bundle inspected in the user's browser. Extracted verbatim: the full-screen vertex shader, the complete fragment shader (38-step raymarch toward a three-sine displaced surface, trigonometric interference pattern, inverse-square blend, distance fog, distance-driven cosine palette `1+cos(d*3+vec3(0,1,2))`, dithering, luminance-keyed premultiplied alpha with optional opaque backdrop), the library defaults (waveSpeed .5, waveHeight 2, wave1 3/.04, wave2 .4/-.1, wave3 -.7/-.7/.1, folding 20, stepBase .11, glowSpread 4), the hero instance override (glowIntensity .011), and the animation driver (iTime accumulates real time; audio-reactive scaling ignored as inapplicable).
- User-visible result: The default canvas reproduces the reference: a horizontal volumetric light sheet with a wavy knot, white-hot caustic segments fanning into spectral bands, and the smoky veil the sheet's dim glow casts over the light background. Five field modes reframe the same sheet (view shift, zoom, tilt, wave scaling); spectra select cosine-palette phases (Prism = reference palette, Mono = achromatic); the loop is exactly seamless.
- Decision: Port the fragment shader verbatim and confine app control mapping to marked extensions: view Y-shift/tilt, palette phase/frequency/chroma, post saturation, optional edge fade. Drive iTime from timeline progress over one exact common period (timeElapsed span 40π) with wave speed quantized to quarters so every sine completes whole cycles per loop; the seed offsets the loop phase. Opaque/preview compositing uses the same luminance weighting as the reference page so exported stills match the site look; uBg passes raw sRGB so glow-free pixels reproduce the exact background hex.
- Alternatives rejected: Continuing the authored spectral-sheet shader (superseded by the user's explicit reference-implementation request); texture/sprite beams (break deterministic export); copying the audio-reactive driver (no audio source in this product).
- State/output mapping: Drift quantizes wave speed in loop-safe quarters; Wobble scales wave amplitude; Secondary motion scales the fine ripple; Light travel mixes pixel sparkle into the march; Phase offsets loop time. Position/Tilt move and shear the view; Thickness maps to glow falloff; Refraction scales the two large wave amplitudes; Spread scales palette frequency; Sample spread scales the march step; Saturation and Glow scale post saturation and glow energy; Frame margin fades glow toward frame edges (zero = reference full bleed). Defaults reproduce the live hero exactly. Targets, persistence, pass ids, timeline mode, and export ownership are unchanged.
- Performance intent: ordinary-product-work. One full-screen pass with a fixed 38-iteration loop, no auxiliary framebuffers, same three canonical pass ids; no measured-performance authority created.
- Verification: Harness renders at multiple loop phases and all modes/spectra were compared against the user's storyboard; the reference-fidelity spec thresholds (p05, max, dark/bright/chroma ratios) were measured green against the default frame before integration. The near-edge exact-pixel background assertion became a bounded-tolerance assertion because the reference sheet emits faint glow across the whole frame; the fixed export-size, bounds, and transparency proofs are unchanged. Protected functional delivery remains the final authority.
- Risks: The raymarched look intentionally covers the full frame with faint emission, so flat-background exactness only holds where the edge fade zeroes the glow; JPG still cannot preserve transparent masked corners; palette presets other than Prism are first-pass tunings awaiting user direction.

#### Archived iteration 5 — Playback pace correction

- Request: The animation plays far too fast and abruptly after the reference port.
- Root cause: Iteration 4 mapped a doubled full wave period (timeElapsed 40π ≈ 126 reference-seconds of motion) onto the default eight-second timeline loop, compressing the reference's slow drift ~15×.
- Decision: Quantize wave speed to halves so the minimal exact loop is 20π, and set the default timeline duration to 120 seconds — the reference implementation advances the same phase at 0.5/s, so the default pace now matches the live site within 5%. The loop seam stays exact for every wave-speed step (each sine completes whole cycles). Persistence version bumps to 2 so previously stored eight-second workspaces do not mask the corrected default.
- User-visible result: The default loop drifts as slowly and smoothly as the reference site; duration edits still retune the pace freely and the forward-only seam proof is unchanged.
- Verification: Typecheck plus harness seam/phase renders; the protected functional delivery gate remains the final authority.

#### Archived iteration 6 — Targeted playback performance correction

- Request: The app stutters badly during playback while the reference site runs smoothly.
- Root cause: The reference site marches its 38-step ray loop over a 1:1-CSS canvas (~0.9 megapixels) and lets the display upscale it, while the preview here marched every contracted backing pixel — CSS × devicePixelRatio × Resolution-scale — which is up to eight times the reference fill on a retina display, at sixty frames per second.
- Decision: Two exact-output changes. First, the ray loop breaks as soon as total distance passes 36.5, where the depth-fog factor is identically zero — sky rays stop after a few coherent steps instead of burning all 38, with bit-identical output. Second, reference-parity march density: the preview marches the field into a retained internal target at CSS × Resolution-scale pixels (physical-pixel density at scale 2 on a 2× display) and blits it into the exact contracted backing with a no-blend copy pass. The backing allocation the render-scale proofs measure is unchanged; the Resolution-scale slider now selects march density exactly as the reference selects its canvas density. Export still marches every pixel of the selected artifact backing.
- User-visible result: Playback is smooth like the reference site; preview softness at Resolution scale 1 matches the site's own upscaled look, and scale 2 marches at full physical-pixel density.
- Verification: Typecheck and code health pass; harness parity renders unchanged; the protected functional delivery gate remains the final authority. The performance complaint is recorded here as the authority for this one targeted iteration.

#### Archived iteration 7 — Cleaner field and tunable color pattern

- Request: Reduce the smoky "dirt" in the volume, blend the glow more cleanly into the background (per the supplied prism example), and make the color pattern user-tunable.
- Root cause of the dirt: the reference shader's trigonometric interference pattern modulates the volume with hard smoothstep(0.5, 0.7) patches, and the luminance key cut faint glow off sharply against the backdrop.
- Decision: Three marked control extensions on the ported shader. Sample spread now also smooths the interference pattern (widened smoothstep plus a lift toward a uniform veil) with march density and a glow compensation balanced so the default stays reference-equal, zero restores the reference texture, and one hundred is a clean gradient. Light travel now rotates the cosine-palette phase (uPaletteShift), giving direct control over which hues lead the spectral pattern; the pixel-sparkle noise mapping is removed (the reference ships with zero). The luminance key is eased (half smoothstep) so faint glow melts into the backdrop.
- User-visible result: The default field is clean like the supplied example while the sheet keeps its reference structure; Sample spread scans from textured reference smoke to pure gradient; Light travel repaints the hue ordering; Spectrum, Spread, and Saturation continue to select the palette, band frequency, and chroma.
- Verification: Harness renders across softness 0/44/100 and palette shifts compared against the example; reference-fidelity thresholds re-measured green on the default frame; typecheck and code health pass. The protected functional delivery gate remains the final authority.

#### Archived iteration 8 — Speed control decoupled from timeline duration

- Request: Keep pause, but stop steering the pace through the animation duration: the field is always a loop that the user can speed up, slow down, and pause.
- Decision: The timeline stays the transport (play, pause, scrub, deterministic export frame). The Drift slider becomes Speed and selects an integer number of whole 20\u03c0 wave cycles per timeline loop (log-stepped, 1-12; render phase = (progress + Phase) \u00d7 cycles), so every setting keeps the wrap seam exact and pace no longer depends on duration edits. The wave speed itself pins to the reference 0.5. The default duration becomes 240 seconds with two cycles per loop — the reference site's real-time pace — and the persistence version bumps to 3 so stored shorter durations do not mask the new default.
- User-visible result: Speed drags the loop smoothly from half the reference pace to several times faster while pause, scrubbing, Phase posing, and export behave as before; duration edits remain available but are no longer the pace tool.
- Verification: Typecheck and code health pass; the seam proof holds for every Speed step because the cycle count is integral. The protected functional delivery gate remains the final authority.

#### Archived iteration 9 — Lounge-slow bottom of the Speed range

- Request: Keep the timeline (scrubbing and pause stay); the lowest Speed is still too fast — the bottom of the range should be a true slow ambient drift.
- Decision: The default loop duration becomes 600 seconds and Speed maps log-stepped whole cycles 1-40 around a default of five cycles per loop. Default Speed therefore stays at the reference site's real-time pace, the slider minimum stretches one 20\u03c0 cycle across the whole ten-minute loop (about five times slower than the site), and the maximum plays roughly six times faster. The persistence version bumps to 4 so stored shorter durations do not mask the new pace floor.
- User-visible result: Speed now reaches a genuine lounge drift at its minimum without touching duration; pause, scrubbing, Phase posing, seam exactness, and export are unchanged.
- Verification: Typecheck and code health pass; whole-cycle mapping keeps the wrap seam exact at every Speed step. The protected functional delivery gate remains the final authority.

#### Archived iteration 10 — Duration-proof calm pace

- Request: The animation thrashes again after duration changes; make it simply a calm lounge drift.
- Root cause: Speed selected an absolute cycle count per loop, so the real pace inverted with the stored timeline duration — a shorter persisted or edited duration multiplied the pace instead of keeping it.
- Decision: Speed now selects a target real-time pace (seconds per 20\u03c0 wave cycle, log-mapped 900s down to 40s with the default near 250s — a calm drift at about half the reference site's rate), and the renderer derives the whole number of cycles closest to that pace from the current loop duration, passed alongside each frame. Pace therefore stays put whatever the duration is, the wrap seam stays exact, and scrubbing and export stay deterministic.
- User-visible result: A calm ambient loop by default; Speed spans a quarter-hour crawl to a few times the reference pace; editing or restoring any timeline duration no longer changes the feel.
- Verification: Typecheck and code health pass. The protected functional delivery gate remains the final authority.

#### Archived iteration 11 — Gentle spectrum preset variations

- Request: The presets are monotonous in color and change almost nothing; make tonally distinct presets while keeping the current look available. Steering in the same batch: an unrequested mirrored field-split/diffusion extension read as a kaleidoscope effect and was removed, and a first diverse-preset pass built on saturated duotone families was rejected — stay close to the copied reference algorithm and modify it, keeping only the approved Ice tuning. A supplied dark blurred reference image then asked for one additional palette in its muted earth tones.
- Task type: Product behavior iteration on the spectrum presets and their palette mapping inside the ported reference shader, plus removal of the unrequested field-split/diffusion controls with Position and Tilt restored exactly as ported.
- Root cause: Every chromatic preset shared the reference cosine palette's structure — near-equally spaced phases — so presets differed only by rotation, which the Light travel control already owns.
- User-visible result: Spectrum offers six options that all read as the reference sheet with different casts: Prism (reference exact), Aurora (mirrored channel order under a cool gain — aqua ridge, violet haze), Sunset (reference order under a warm golden gain with the blue phase pulled closer), Ice (pale glacial blues, slightly desaturated), Dusk (from the supplied dark reference image — burnt-orange highs, dark umber troughs, steel-blue lows), and Mono (achromatic). Position and Tilt behave exactly as in the ported reference again.
- Source/reference checked: The ported octolane.com palette line `1 + cos(d * freq + phase)`; iteration 4's recorded risk that palette presets other than Prism were first-pass tunings awaiting user direction; standalone shader-harness renders of every candidate on the default frame; the user-supplied dark blurred reference image (rust-orange, umber, steel-blue on near-black) for the Dusk palette.
- Docs/contracts read: `docs/toolcraft/workflow.md`; renderer route plan `core/runtime-boundary.md` and `core/performance.md`; implementation `renderer-technique.md` and `performance.md`; verification `acceptance-testing.md`.
- Contract rules applied: `acceptance-product-observable`, `controls-product-coverage`, `renderer-technique-inventory`, and `workflow-required`.
- Decision: Extend the marked palette control extension with per-channel midpoint and amplitude uniforms (`uPaletteBase`, `uPaletteAmp`), both defaulting to 1 so the formula stays the reference `1 + cos(...)` exactly; presets set amp equal to base as a plain per-channel gain over the unchanged shape, and only Ice narrows amplitude below base for its paler sheet. Remove the field-split/diffusion fold, uniforms, and controls; restore the Position and Tilt mappings verbatim. Dusk uses the same gentle mechanism with dim narrowed amplitudes at chroma 0.95 so the sheet runs the reference image's rust-orange to steel-blue range with dark umber troughs, tuned against the reference on a dark background.
- Alternatives rejected: Phase-structure duotone and equal-spacing wheel families (rejected by the user as abandoning the reference character); a per-preset hue rotation (duplicates Light travel); keeping field split behind a zero default (the capability was not requested).
- State/output mapping: `dispersion.spectrum` selects one preset mapped to `uPalettePhase`, `uPaletteBase`, `uPaletteAmp`, and `uChroma` in the single field pass; Spread still scales palette frequency, Light travel rotates the phase, Saturation post-scales chroma, and Position/Tilt again drive the view shift and shear. Targets, persistence slices, pass ids, timeline mode, and export ownership are unchanged; previously stored spectrum values remain valid.
- Performance intent: ordinary-product-work. Two vec3 uniforms in the same fixed-cost single pass; the field-split/diffusion uniforms and fold are removed; no measured-performance authority created.
- Verification: Standalone shader-harness renders of all five presets on the default frame were compared for family coherence and distinct casts; typecheck, the local docs check, and code health pass on the delivered tree. One bare `npm run verify:delivery` remains the final protected authority.
- Risks: Aurora and Sunset intentionally stay close to Prism — the same optical structure under a different temperature — so preset switches read as cast changes, not new patterns; Ice keeps narrowed amplitudes and slight desaturation, so its bands are paler than the reference by design.

#### Archived iteration 12 — Custom spectrum with user-adjustable colors

- Request: Add a Custom mode to the Spectrum control so the palette colors can be adjusted directly; a follow-up in the same batch raised the anchor count to four.
- Task type: Product behavior extension adding a user-parameterized palette option to the existing spectrum select inside the ported reference shader.
- Root cause: The palette was reachable only through the fixed preset table; no user-controlled colors existed.
- User-visible result: Spectrum gains a Custom option; choosing it reveals four color controls (Color 1 through Color 4) whose values become the exact colors of the palette cycle at its start and each quarter point. All other spectra render exactly as before, and the custom defaults start in the Dusk reference's earth tones so switching to Custom lands on a familiar look.
- Source/reference checked: The ported palette line and the existing preset uniform extension (`uPalettePhase`, `uPaletteBase`, `uPaletteAmp`); the schema's conditional-applicability and inline-layout patterns already used by Corner radius and Background.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `docs/toolcraft/schema-reference.md` and `core/control-selection.md` for control authoring; the acceptance label and inline-layout rule sources for label and layout constraints.
- Contract rules applied: `acceptance-product-observable`, `controls-product-coverage`, `renderer-technique-inventory`, and `workflow-required`.
- Decision: Fit the four anchor colors with the palette's cosine form via a per-channel four-point Fourier fit (mean, first harmonic, and a signed second-harmonic Nyquist amplitude in one extra uniform that every built-in preset keeps at zero), so each anchor is reproduced exactly at its cycle quarter; hex channels map so mid gray is the reference-neutral multiplier of one. A `max(palette, 0)` guard clamps undershoot between anchors and is a no-op for every built-in preset and the reference formula. The three colors persist as validated hex values and invalidate the preview like other field targets.
- Alternatives rejected: A two-color duotone blend (cannot represent the reference-style traveling hue separation); direct sliders over the nine palette coefficients (engineer-facing, not designer-facing); a separate custom palette branch in the shader (duplicates the palette path and risks drifting from the reference).
- State/output mapping: `dispersion.customColorA/B/C/D` persist hex anchors and their controls appear only while `dispersion.spectrum` is `custom`; updateUniforms converts them into the existing palette uniforms each frame; Spread, Light travel, and Saturation keep their meanings over the fitted palette. Targets, persistence slices, pass ids, timeline mode, and export ownership are unchanged; previously stored spectrum values remain valid.
- Performance intent: ordinary-product-work. The fit is a constant-cost CPU computation during uniform updates; the shader adds one `max()` to the fixed single pass; no measured-performance authority created.
- Verification: A numeric check confirms the fitted palette equals each chosen color exactly at its cycle anchor, and standalone harness renders cover the default and altered anchor sets on a dark background; typecheck, the local docs check, and code health pass on the delivered tree. One bare `npm run verify:delivery` remains the final protected authority.
- Risks: Extreme anchor sets (near-blacks against a white) clamp at zero between anchors, slightly flattening the dip the pure fit would carry; anchors are palette multipliers, so a uniformly dark set dims the sheet rather than recoloring the background; the Nyquist term makes four-anchor palettes oscillate twice per cycle, which is the intended cost of exact four-color reproduction.

#### Archived iteration 13 — Chromatic split and march sparkle

- Request: Analyze the app against the reference and make the beam more interesting; from four harness prototypes the user chose the chromatic caustic split and the march sparkle.
- Task type: Product behavior extension adding two zero-default optical controls to the ported reference shader.
- Root cause: Dispersion lived only in the distance-driven palette — the sheet itself was a single surface with a monolithic white core — and the reference's own per-pixel march jitter (`uNoiseAmount`) was wired into the shader but permanently forced to zero by the app.
- User-visible result: Two new Dispersion Field sliders, both defaulting to zero so the reference look is untouched. Chromatic split separates the red and blue sheets vertically, fanning the caustic into rainbow fringes that read as true channel dispersion; Sparkle re-enables the reference's march jitter, graining the veil into diamond-dust shimmer that animates with playback.
- Source/reference checked: The ported fragment shader's step and accumulation terms; iteration 4's port notes recording `uNoiseAmount` as part of the reference library defaults; harness prototype renders of split, sparkle, ghost-sheet, and fringe variants compared side by side on a dark background.
- Docs/contracts read: `docs/toolcraft/workflow.md`; `renderer-technique.md` and `performance.md` for the marked-extension pattern; the acceptance section-cohesion rule sources for the ten-control section cap and dense-section semantic groups.
- Contract rules applied: `acceptance-product-observable`, `controls-product-coverage`, `renderer-technique-inventory`, and `workflow-required`.
- Decision: Compute two extra distances to the same wave surface offset by ±`uChannelSplit` and blend per-channel caustic weights from them; at zero all three distances collapse onto the reference's single surface and the accumulation is exactly the reference term. Sparkle maps a 0–100 slider onto the existing `uNoiseAmount` jitter (0–0.12). No new passes; both targets join the slider invalidation list.
- Alternatives rejected: Post-process chromatic aberration (splits the whole frame including the backdrop rather than the sheet); an anamorphic-streak second pass (new pass id and pipeline churn for a cosmetic gain); texture-based grain (breaks deterministic export).
- State/output mapping: `dispersion.chromaSplit` and `dispersion.sparkle` persist as 0–100 sliders in the Dispersion Field section (now ten controls, at the section cap, all in the field-geometry semantic group); updateUniforms maps them to `uChannelSplit` (0–0.12 world units) and `uNoiseAmount` (0–0.12). All other targets, persistence slices, pass ids, timeline mode, and export ownership are unchanged.
- Performance intent: ordinary-product-work. Two divisions and two mixes per march step inside the same fixed 38-iteration single pass; no measured-performance authority created.
- Verification: Harness renders confirm the zero-default frame is identical to the pre-change baseline and show the intended fringing and shimmer at mid slider values; typecheck, the local docs check, and code health pass on the delivered tree. One bare `npm run verify:delivery` remains the final protected authority.
- Risks: High split values read as three distinct sheets rather than fringing (the intended top of the range); sparkle is per-pixel screen-space jitter, so exported stills carry visible grain and animation shimmers exactly like the reference's own term.

#### Archived iteration 14 — Sparkle grain bound to the sheet

- Request: Привязать грейн-эффект (Sparkle) к самой линии вместо текущего наложения зерна поверх всего кадра.
- Task type: Product behavior iteration on the Sparkle control's optical model inside the ported reference shader; no schema targets, passes, or section structure change.
- Root cause: Iteration 13 mapped Sparkle onto the reference's per-pixel march jitter, which is seeded by screen coordinates and perturbs the march everywhere, so the grain read as a static screen-space noise layer over the veil and backdrop rather than a property of the caustic line.
- User-visible result: Sparkle now grains the light sheet itself. Specks live on the wave surface — they hug the bright caustic (and its chromatic-split fringes), ride the wave motion during playback, scale with perspective, and fade out where the sheet's light fades, leaving the veil and backdrop clean. Zero still renders the exact reference frame; the default frame is byte-identical to the previous baseline.
- Reference inputs: None (harness renders of the current app baseline only).
- Source/reference checked: The ported march loop's step and accumulation terms; iteration 13's recorded risk that sparkle was per-pixel screen-space jitter; harness A/B renders of the old jitter versus the new surface-bound grain across sparkle levels, both backdrops, chromatic split, all five field modes, Mono spectrum, and two loop phases; a GPU pixel diff proving the sparkle-0 default frame is unchanged.
- Docs/contracts read: `docs/toolcraft/workflow.md`; renderer route Plan `core/runtime-boundary.md` and `core/performance.md`; Implementation `renderer-technique.md` and `performance.md`; Verification `acceptance-testing.md`.
- Contract rules applied: `acceptance-product-observable`, `controls-product-coverage`, `renderer-technique-inventory`, and `workflow-required`.
- Decision: Remove the jitter term from the march (the step formula is now identically the reference's sparkle-0 march for every slider value, keeping geometry stable), accumulate caustic-weighted surface coordinates during the march (the same inverse-square falloff the accumulation uses, squared, gathered past the camera's near bubble), and after the march multiply the accumulated light by a two-octave value-noise speck field sampled at the weighted mean surface point with the local wave height riding in the seed. The grain is masked by the accumulated light's own luminance (smoothstep 0.14–0.65), so it bites the bright line and leaves dim veil untouched, and it is applied before the luminance key so transparent-export alpha carries the same grain. Sparkle maps 0–100 onto a 0–0.9 multiplicative speck depth. Wave-height seeding keeps the loop seam exact because every seed component is loop-periodic.
- Alternatives rejected: Keeping the march jitter with a proximity gate (the jitter's visible effect concentrates away from the sheet, so gating it near the line leaves almost nothing); masking by closest-approach distance or accumulated caustic weight alone (both classify the camera bubble and the smoky veil as line, regraining the veil); screen-space grain masked by luminance (stays visibly glued to the screen instead of the moving line); per-step noise inside the accumulation (independent samples average out across the march and wash the grain away).
- State/output mapping: `dispersion.sparkle` keeps its target, slider range, section placement, semantic group, invalidation entry, and acceptance row; updateUniforms maps it onto the reused `uNoiseAmount` uniform as grain depth. All other targets, persistence slices, pass ids, timeline mode, and export ownership are unchanged; stored sparkle values remain valid and simply render the new grain.
- Performance intent: ordinary-product-work. The march keeps its fixed 38-iteration cardinality; the per-step addition is one bounded accumulate, and the grain block is a constant post-march cost (eight hash evaluations) behind a uniform branch. No pipeline, envelope, path, or fixture change; no measured-performance authority created.
- Verification: renderer/canvas/runtime scope targeted development checks — harness renders (old vs new at matched sparkle levels, both backdrops, split, every field mode, Mono, two loop phases) confirm the grain hugs the line and the veil stays clean; a GPU readPixels diff proves the sparkle-0 default frame is byte-identical to the pre-change shader; typecheck, the local docs check, and code health pass on the delivered tree. One bare `npm run verify:delivery` remains the final protected authority.
- Risks: At high Sparkle the grain darkens parts of the white-hot core by design (film-grain bite); the luminance mask means grain also touches the brightest smoky wisps immediately around the line in Edge mode, where the sheet's own glow is bright; existing saved sparkle values now render surface-bound grain instead of screen jitter, which is the requested behavior change.

#### Archived iteration 15 — Inside / Border wave distribution

- Request: Preserve the current wave inside the canvas and add a second mode in which the wave travels around the canvas border like Paper Design's Pulsing Border, while retaining the current optical/motion settings and adding control over the affected area.
- Task type: broad renderer/schema scope renderer/schema/reference extension covering conditional controls, a new full-frame shader branch, export parity, acceptance, and browser proof.
- Source/reference checked: Paper Design's live Pulsing Border page and the official `paper-design/shaders` source. The inspected model uses a rounded-rectangle signed-distance contour, angular moving spots, adjustable thickness/softness/intensity/bloom/spot size/pulse/smoke, deterministic animation, and per-side inset margins.
- User-visible result: A new `Wave mode` switches between unchanged `Inside` output and `Border`. Border follows Rectangle, Rounded, or Circle output geometry; travels continuously around its perimeter; uses the current spectrum, custom colors, optical, and motion controls; and exposes Influence area, Spot size, Wave count, Pulse, and Turbulence.
- Decision: Implement Border independently as a retained fullscreen SDF shader in the existing WebGL resource. Use the real output-mask distance for the contour, a physical side-length perimeter coordinate for Rectangle/Rounded and polar arc for Circle, a bounded double-beat pulse, analytic deterministic value noise for turbulence, and premultiplied source-over compositing that preserves chroma on light backgrounds. Reuse preview/export settings, timeline progress, internal scaling, snapshot restoration, and the existing canonical three-pass Toolcraft pipeline.
- Interaction ownership: The panel owns exact distribution and envelope values. Canvas remains product pixels plus runtime viewport navigation; direct border handles would duplicate inset/influence controls and obscure the glow.
- State/output mapping: `dispersion.distribution` selects the Inside or Border material. `border.influence`, `border.spotSize`, `border.spots`, `border.pulse`, and `border.turbulence` drive only Border and are conditionally absent in Inside. Existing frame settings define the real contour; existing field/spectrum/motion settings tune both branches. Runtime clipping remains the final output-mask authority. Persistence version 5 prevents older workspaces from supplying undefined branch values.
- Alternatives rejected: A second overlay canvas risks preview/export drift; importing the Paper React package would introduce a parallel renderer and incompatible state/export ownership; a CSS border/filter cannot follow the requested spectral, animated, mask-aware controls; replacing Inside would violate the request to keep the current mode.
- Performance intent: ordinary-product-work. Border uses fixed fullscreen work and a constant maximum spot loop; mode selection does not add a simultaneous pass. Preview shades its analytic contour at CSS density and upscales into Toolcraft's exact backing, while export shades every artifact pixel. The visual feature request creates no measured-performance authority.
- Verification: Focused state/schema tests, browser comparison of Inside vs Border, Rounded/Circle contour proof, Border-parameter response, timeline seam, and current-frame export; one bare `npm run verify:delivery` at the coherent boundary.
- Risks: Rounded-corner travel uses a continuous side-length route rather than exact analytic arc length, so speed can ease slightly through very large corner radii. Strong turbulence deliberately extends glow beyond the contour, then the canvas mask clips it at the outer edge.

#### Archived iteration 16 — Separate Sparkle and official Paper Grain effects

- Request: Make Sparkle a separate configurable effect, add the Grain Gradient noise from `shaders.paper.design/grain-gradient`, apply either effect to selectable parts of the wave, and use the open-source package directly via `@paper-design/shaders-react` rather than recreating its noise by eye.
- Task type: broad renderer/schema scope dependency, schema, renderer, export-parity, acceptance, and browser-proof extension across both Inside and Border wave distributions.
- User-visible result: A new Effects section selects Sparkle or Grain Gradient and an application area (Whole wave, Core, Glow, Color bands, or Veil). Sparkle stays attached to the wave surface and adds Size and seamless Twinkle. Grain is a flat 2D texture over the completed image, with Amount, Scale, Softness, Distortion, and Drift. Following visual feedback, Grain now uses Paper's sparse luminous color-ramp behavior rather than centered greyscale TV grain; Amount 50 matches the official default preset and 100 doubles that reference contribution.
- Source/reference checked: The live Paper Grain Gradient reference; official `paper-design/shaders` repository and Apache-2.0 license/notice; pinned npm packages `@paper-design/shaders` and `@paper-design/shaders-react` 0.0.80; the package-authored `grainGradientFragmentShader`, `GrainGradientProps`, and embedded randomizer texture.
- Reference inputs: `https://shaders.paper.design/grain-gradient`, the official package shader source, and the current surface-bound Sparkle implementation.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/reference-study.md`, `core/runtime-boundary.md`, `assembly-workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/performance.md`, and `core/timeline-animation.md`; Implementation routes `schema-reference.md`, `decision-contract.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `interaction-surface-ownership`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Remains `non-spatial`; these effects modify the procedural two-dimensional wave and add no editable camera or object pose.
- Interaction ownership: The panel exclusively owns effect selection, exact parameters, reset, persistence, and export parity. A canvas paint/mask gesture was rejected because it would duplicate the same area selection and obscure the animated pixels being judged.
- Decision: Pin the official packages exactly. Extract Paper's `snoise`, texture-backed `randomR`, `valueNoiseR`, and fixed three-octave `fbmR` block from `grainGradientFragmentShader` at module load, validate stable source markers, and adapt only GLSL `texture()` to the existing GLSL1-compatible `texture2D()` spelling. Also extract Paper's complete post-kernel recipe verbatim: positive-only `clamp(rawNoise, 0, 1)`, distortion/noise shape offsets, derivative antialiasing, softness coverage, and the color-ramp mixer. Feed that mixer through four stops from the active Dispersion Spectrum and composite the resulting colored energy, never a centered threshold or `vec3(gray)` layer. Keep Paper's embedded 128×128 randomizer image as one retained Three texture shared by preview and export. Composite Grain after the wave is rendered, in `gl_FragCoord` output-pixel coordinates, so it stays a planar 2D image layer and never bends with raymarch/world/camera perspective. Map Grain Amount so `50 -> Paper noise 0.25` and `100 -> 0.5` for both render modes and export. Do not mount Paper's standalone React canvas because that would bypass Toolcraft state, wave masks, Border mode, and the shared export renderer.
- Alternatives rejected: The first caustic-surface coordinate mapping (rejected by the user because it made Grain look embedded in 3D); the first planar but centered greyscale threshold composite (rejected after visual comparison because it discarded Paper's sparse positive shape and color mixer); reimplementing visually similar noise (would drift from the explicitly requested source); mounting `GrainGradient` as a second overlay canvas (would diverge in export); adding a second renderer pass (unnecessary for a fixed-cost 2D composite); adding new Grain colors (the existing Spectrum owns image color).
- State/output mapping: `effect.mode` selects surface-bound Sparkle or screen-space Grain. `effect.area` selects a luminance/chroma mask from the completed wave image and changes only where the planar layer is visible. Existing `dispersion.sparkle` remains Sparkle Amount for stored-value compatibility; `effect.sparkle.size` and `.twinkle` extend it. `effect.grain.amount`, `.scale`, `.softness`, `.distortion`, and `.drift` map through the official `GrainGradientProps` value model into output-pixel texture generation and flat overlay compositing. Both Inside and Border shaders consume the same controls, timeline phase, retained texture, preview resource, and image-export snapshot. Persistence version 6 supplies defaults for older workspaces.
- Performance intent: ordinary-product-work. Sparkle and Grain are mutually exclusive fixed-cost shader branches. Paper Grain adds one retained 128×128 texture and a fixed three-octave kernel but no pass, workload dimension, variable loop bound, or measured-performance authority.
- Verification: Focused state/schema/kernel tests and browser control/output checks cover both branches, conditional visibility, every area mask, seamless animation, Border compatibility, and current-frame export; one bare `npm run verify:delivery` remains the final protected authority.
- Risks: The adapter intentionally fails fast if a future package version changes the expected source markers, which is why both packages are pinned. Area masks are derived from rendered luminance/chroma, so an achromatic spectrum makes Color bands naturally sparse. Screen-space grain density follows output pixels, matching a 2D image effect but making higher-resolution exports contain proportionally more texture samples. The Paper texture is loaded from the package's embedded data URI; export waits for it when Grain is active.

#### Archived iteration 18 — Paper-authored Rounded perimeter motion

- Request: Fix Rounded because the current Border appears to work toward the center; adapt Paper Design's supplied Pulsing Border algorithm with the existing Dispersion parameters and inspect the official npm implementation.
- Task type: renderer/canvas/runtime scope renderer/canvas visual correction within the existing Border material, preview/export path, and control model.
- User-visible result: Rounded Border now forms a closed 3D light ribbon around all four canvas sides and rounded corners. Distinct volumetric wave packets visibly travel along that ribbon at a Border-specific readable pace; the middle remains background instead of becoming the field's visual destination.
- Source/reference checked: The user-supplied live Paper Pulsing Border preset and pinned `@paper-design/shaders` / `@paper-design/shaders-react` 0.0.80 source, including the official `beat`, rounded-box SDF/mask, texture-backed noise, angular spot, source-over, additive bloom, smoke, and background-composite formulas.
- Reference inputs: `https://shaders.paper.design/pulsing-border#colors=0dc1fd,d915ef,ff3f2ecc&colorBack=000000&roundness=0.25&thickness=0.1&softness=0.75&aspectRatio=auto&intensity=0.2&bloom=0.25&spots=4&spotSize=0.5&pulse=0.25&smoke=0.3&smokeSize=0.6&speed=1&scale=0.6&rotation=0&offsetX=0&offsetY=0&marginLeft=0&marginRight=0&marginTop=0&marginBottom=0`.
- Docs/contracts read: `workflow.md`; reference-study, runtime-boundary, performance, assembly, renderer-technique, schema-reference, component-rules, decision-contract, acceptance-testing, plus the brainstorming, systematic-debugging, writing-plans, and browser workflows.
- Contract rules applied: `reference-clone-source-of-truth`, `renderer-technique-inventory`, `timeline-enabled-behavior`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Remains non-spatial; Rounded is output-frame geometry, not a camera or manipulable 3D object.
- Interaction ownership: Existing panel controls remain the single precise-value surface; this correction adds no canvas handle or mirrored operation.
- Decision: Extract Paper's stable Pulsing Border core directly from the pinned shader at module load and fail fast on missing source markers. Use Paper's exact rounded mask, derivative antialiasing, texture noise, smoke shaping, intensity, and bloom math as the edge envelope. Replace the overlapping Paper color-by-spot sector grid with one physical closed-perimeter route and a total `Wave count` of moving packets. Each packet reveals a bounded 24-step local raymarch whose longitudinal axis is perimeter distance, transverse axis is signed border distance, and Z supplies virtual depth. The local march reproduces the Inside vocabulary with three folded waves, interference, depth fade, Spectrum palette, refraction/channel split, and volumetric glow. Give Border its own Speed-to-circuit mapping (about five seconds per default circuit), while rounding to an integer circuit count for an exact Toolcraft loop seam.
- Alternatives rejected: Keeping the custom physical-perimeter `borderEnergy` plus exponential aura (the reported center-seeking appearance); directly using Paper's nested color × spots loop (16 wide default sectors overlap into a white frame whose movement is illegible); using flat 2D perimeter packets (rejected by the user's explicit 3D requirement); mounting the Paper React component as a second canvas (would bypass Toolcraft state, retained resources, effects, and export); changing the schema to duplicate Paper controls (existing controls already cover the requested parameters).
- State/output mapping: `frame.shape=rounded` and Corner radius define Paper's roundness; Wave count controls total 3D packets, Wave length controls their longitudinal footprint, Pulse modulates local energy, and Turbulence/Influence control Paper's edge smoke. Height, Sample spread, Saturation, Glow, Inset, Speed, motion phase, Spectrum, Refraction, and Chromatic split feed the local 3D ribbon. `dispersion.distribution=inside` remains unchanged. Preview and image export use the same retained border material and official randomizer texture.
- Performance intent: ordinary-product-work. The renderer remains one retained fullscreen Border pass with a fixed 24-step local raymarch and a fixed eight-entry nearest-packet scan; no user-controlled shader loop bound or extra pass is added. This visual complaint does not authorize measured performance; functional/browser proof is sufficient.
- Verification: One bare `npm run verify:delivery` will derive and run the protected proof.
- Risks: The physical route follows the straight-side projection through rounded corners, so very large radii may ease slightly across the corner arc. The local volume is a bounded border-oriented light ribbon, not the complete full-canvas Inside camera scene; this is intentional so the 3D energy remains confined to the edge.

#### Archived iteration 17 — Hybrid 3D-surface Grain distribution

- Request: Add a Grain mode that distributes noise across the 3D surface while keeping it unbent, unlike Sparkle.
- Task type: renderer/canvas/runtime scope schema and renderer behavior extension using the existing field-shader surface accumulator.
- User-visible result: Grain gains `Distribution` with `Screen` and `Surface mask`. Screen preserves the current planar overlay. Surface mask makes Grain appear only according to the moving raymarched wave density, while every individual grain remains a flat screen-space particle with unchanged scale and shape.
- Source/reference checked: The current official Paper Grain adapter and the caustic-weighted `sheetW` accumulator already used to locate Sparkle on the wave.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `acceptance-product-observable`, `renderer-technique-inventory`, `persistence-policy-explicit`, and `workflow-required`.
- Decision: Keep Paper's `grain_uv` exclusively in `gl_FragCoord`, then normalize the existing accumulated caustic weight into a smooth visibility mask and multiply it with the selected Area mask. The new control is conditional on `Effect = Grain Gradient` and `Wave mode = Inside`; Border has no raymarched 3D surface and continues to use Screen behavior. Split the eleven-control Effects entity into balanced Placement and Style workflow sections.
- Alternatives rejected: Sampling Paper noise at the weighted 3D surface coordinate (would warp and scale grains like Sparkle); adding a second render pass or depth texture (duplicates data already present in the march); exposing Surface mask in Border (there is no corresponding 3D surface).
- State/output mapping: `effect.grain.distribution` persists as `screen | surface`, defaults to Screen, invalidates only the retained preview frame, and maps to one field-only uniform. `effect.area` remains an additional luminance/chroma mask. Paper Amount, Scale, Softness, Distortion, Drift, spectrum color, timeline phase, and export path remain unchanged.
- Performance intent: ordinary-product-work. The field shader reuses `sheetW` and adds constant scalar mask math after the existing fixed raymarch; no pass, workload dimension, loop bound, or measured-performance authority is added.
- Verification: Targeted unit proof covers default/fallback parsing, conditional applicability, balanced inventory sections, and shader coordinate/mask invariants. Browser proof covers visibility in Grain + Inside, absence in Sparkle and Border, and a changed pixel result between Screen and Surface mask. One bare `npm run verify:delivery` is the protected functional authority; measured performance is not requested.
- Risks: Surface mask intentionally follows accumulated caustic density rather than reconstructing a geometric UV unwrap, so the grain distribution moves with the visible volume but individual dots do not rotate with local normals; this is the requested no-bend hybrid.

## Decisions

### Renderer

- Decision: Retained WebGL2 with one canonical 38-step light-sheet material, a bounded twelve-record soft-mask uniform loop, one live canvas, one retained Paper randomizer texture, an optional retained official Paper Lens Distortion post-process, one runtime export callback, and canonical `dispersion.preview-frame` / `dispersion.image-export` passes.
- Reason: The supplied article makes refraction, channel-specific IOR sampling, multisampling, Fresnel, and view-dependent lighting the implementation authority; these operations are direct fragment-shader work and cannot be reproduced faithfully by additive Canvas 2D strokes.
- Evidence: Preview and export use the same normalized settings, periodic phase, shader programs, and target-size renderer. Canvas 2D remains only the unavailable-WebGL fallback.

### View Interaction

- Decision: `non-spatial` with no orientation targets.
- Reason: The visible output is a 2D procedural field, not an editable 3D scene or model.
- Evidence: Product readiness records the mode and the renderer exposes no camera or orbit behavior.

### Interaction Ownership

- Decision: Panel owns frame clipping, mask collection cardinality, exact ellipse transform/blur values, mask application and preview modes, field placement/inset, and optical properties; canvas owns only runtime viewport navigation.
- Reason: Exact values, reset, persistence, and export parity outweigh direct manipulation for these non-object properties.
- Evidence: Four typed mask ownership entries name the rejected canvas duplicates and relevant acceptance rows carry their `interactionId`.

### Timeline

- Decision: Toolcraft playback timeline, initial duration sixty seconds, seamless forward-only periodic motion.
- Reason: The requested animation needs user-facing play/pause, scrub, duration, and loop transport; mirror/yoyo motion is not requested.
- Evidence: Transfer metadata records product-derived duration provenance and loop acceptance requires first/last seam equality before and after duration editing.

### Layers

- Decision: Disabled.
- Reason: The app has one procedural output with no independently selectable, reorderable, hideable, or grouped entities.
- Evidence: Schema omits `panels.layers` and no selected-layer targets exist.

### Controls

- Decision: Built-in segmented, select, slider, switch, color, compound collectionActions, and panelActions controls grouped by Frame, Masks, two staged Dispersion Field sections, Effect Placement, Effect Style, three staged Lens Distortion sections, Spectrum, Motion, Background/Setup, and Image Export. Masks owns separate Apply masks and Show masks switches because product clipping and editor visualization are distinct operations.
- Reason: Each requested value has a direct built-in owner; no custom control interaction is needed.
- Evidence: The section inventory names exact entity targets; every product control declares explicit applicability, defaults, and performance role/reason. The twelve-control field and eleven-control effect entities are split into balanced workflow sections, and inactive effect/distribution branches are absent.

### Export

- Decision: Runtime-owned PNG/JPG still export at 2K/4K/8K with the selected Apply masks state respected and the red mask-preview overlay forcibly disabled; video export is not enabled.
- Reason: Image export is the default product artifact. The request asks for animated preview behavior but does not explicitly request a video artifact.
- Evidence: Product readiness declares image `toolcraft-default` and video `not-requested`; schema exposes only Image Export and one typed `export-image` action; the product callback draws one deterministic current timeline frame.

### Performance

- Decision: One fixed-sample WebGL field composite with a bounded soft-mask loop, an optional variable-sample official Paper lens filter with a precompiled retained program and asynchronous preview submission, and one quadratic batch image-export path; explicit workload dimensions are `image-long-edge`, `lens-samples`, and `mask-count`.
- Reason: Field and Grain settings remain fixed-cardinality, while Paper Lens Count directly changes active taps from 2 through the package maximum of 50. Output resolution remains the batch magnitude; retained programs/targets and removal of preview GPU barriers reduce application latency without changing output work or quality.
- Evidence: The canonical pipeline drives assessment, derived paths, the 2K/4K/8K, continuous lens-sample, and 0–12 mask-count fixture adapters, scenario coverage, and exact verification ownership. The request-authorized kernel decision compares Canvas 2D and WebGL at Count 50; protected kernel and targeted delivery receipts remain the only timing authority.

## Evidence

- Source reviewed: Product prompt, circle-mask plan, the complete video reference, extracted storyboard/contact sheet, neutral starter schema, and current local Toolcraft contracts.
- Contract applied: Toolcraft owns the shell and all editor surfaces; product code supplies only schema, canvas pixels, scene bounds, pipeline registration, and deterministic export drawing.

## Verification

Protected receipts own changed files, the derived plan, executed checks, reports, measurements, and pass/fail evidence. Iteration 41 performs the focused checks recorded above and one bare `npm run verify:delivery`; no measured performance iteration or complete performance audit is requested.

## Risks

- Risk: Raster backend blur kernels can shift subpixel antialiasing while preserving the accepted optical structure.
- Risk: Infinity mode uses an explicit 1920×1080 procedural product bound (1080×1080 for Circle) rather than the dormant finite canvas size; returning to finite restores the exact user dimensions.
- Risk: Mask rendering is intentionally bounded at twelve records; imported overflow is truncated and UI addition stops at the same algorithmic ceiling.
