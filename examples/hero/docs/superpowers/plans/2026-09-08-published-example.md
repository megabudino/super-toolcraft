# Published Hero Example Plan

**Goal:** Publish the exact current native app from the process serving port 3101 as an independent Toolcraft example, without Apply.

**Architecture:** Copy the read-only native source from /Users/kusnizza/Projects/recraft-apps/hero; retain the existing src/section renderer and signed Toolcraft host. No iframe, scene transplant, Recraft server, or paid backend is introduced. Reset dispatches controls.reset and all other existing controls, media, persistence and JSON settings transfer remain runtime-owned.

- [x] Verify correct source identity; discard the initial obsolete-source draft entirely.
- [x] Preserve source defaults, signed files and visual components; copy without source dependencies/server state.
- [x] Prove Apply/save/root-asset failures with four RED tests, then remove publication actions and adapt local asset base paths.
- [x] Run focused action, protocol/native renderer tests, typecheck, actual-base production build and protected integrity diagnostics.
- [x] Inspect inherited delivery status; run initial functional delivery gate once if no receipt exists. Do not run measured performance or edit signed validators.
- [x] Verify isolated production preview, edit/reset/media and Fine Details prompt flight; report inherited limitations explicitly.

Verification tier: Tier 4 exported app. No measured performance is authorized. The controller owns separate review, integration, commits and deployment. Inline execution is assigned; no further delegation.
