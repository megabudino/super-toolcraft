# Toolcraft agent worklog

## Product mode

- Mode: product
- Product: Recraft Fine Details
- Purpose: configure the real website Fine Details background through a Toolcraft iframe and deliberately publish settings through Reset or Apply.

## Decision Trail: Fine Details background-only Toolcraft setup

- Request: create a second Toolcraft section beside Hero and Footer; retain only the current light striped/grid background; expose only basic controls; do not implement export.
- Task type: first product delivery, generated app assembly, schema controls, canvas output, persistence, and website action bridge.
- User-visible result: the website Fine Details section now renders only its light background plus repeated grid texture; Toolcraft edits background color, grid size, grid opacity, and section height in the real website iframe.
- Source/reference checked: current `fine-details-section.tsx`, `fine-details-parallax-scene.tsx`, `/images/recraft-fine-details/grid.png`, the supplied website screenshot, and the existing Hero/API Section/Pre-footer Toolcraft bridge implementations.
- Contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, reference study, runtime boundary, assembly workflow, control selection, layout, performance, setup/export, media upload, schema reference, decision contract, component rules, and renderer technique.
- Contract rules applied: `defineToolcraft`/`ToolcraftAppComposition` shell; built-in controls only; product output only in `canvasContent`; editable-output canvas; runtime persistence; fixed-camera iframe reference; request-scoped panel commands; no Layers, Timeline, media, canvas handles, or artifact actions.
- View interaction: `fixed-camera`; evidence is the inspected flat website background and the user's request to remove all spatial/content elements; no orientation target exists.
- Interaction ownership: `website.settings` Reset/Apply commands belong to the sticky panel because they write local website source; rejected duplicate canvas buttons because canvas contains only product output.
- Decision: use the real Next.js section as a pointer-transparent iframe, keep its DOM/CSS renderer authoritative, and send versioned appearance/height settings from Toolcraft.
- Alternatives rejected: duplicating the section in Toolcraft DOM/Canvas/SVG; retaining old text/artwork; adding uploads, layers, timeline, custom handles, or export UI.
- State/output mapping: `appearance.background` sets the section fill; `appearance.gridSize` sets repeated texture size; `appearance.gridOpacity` sets texture opacity; `canvas.size.height` sets the website reference height; Apply persists current values; Reset dispatches canonical Toolcraft reset and persists shared defaults.
- Persistence: Toolcraft workspace uses runtime localStorage; the website uses a development-only atomic PUT into `fine-details-applied-settings.json`; successful Apply/Reset broadcasts to an already-open website tab.
- Export: image mode is `user-removed` with explicit evidence “экспорт мы не делаем вообще”; SVG and video were not requested; no export renderer, settings, acceptance row, or action exists.
- Performance: one constant-cost iframe settings-sync pass, no workload dimensions, no measured performance request, and viewport pan/zoom do not invalidate the bridge.
- Workflow fallback: no dedicated external Toolcraft workflow skill was available, so the complete signed local workflow and routed contract documents were used.
- Verification: the user explicitly requested implementation without extra checks and manual local testing afterward; automated, delivery, browser, build, lint, typecheck, formatting, and measured-performance commands were not run.
- Risks/follow-up: the Next.js site must be running on port 3000 before the iframe can acknowledge Apply/Reset; the user will perform the requested local visual and interaction check.

## Decision Trail: move the AI prompt popup from Hero into Fine Details

- Request: remove the existing AI prompt popup from Hero, transfer it to Fine Details, and move its existing position and shadow controls into the Fine Details Toolcraft app without adding prompt-content controls.
- Task type: later Tier 2 schema/product behavior edit spanning two independent website/Toolcraft ownership boundaries.
- User-visible result: Hero no longer renders or configures the popup; Fine Details renders the unchanged `AiPromptInput` over its background/grid and exposes Position plus Shadow, offset, blur, spread, color, and opacity controls.
- Source/reference checked: the supplied popup screenshot, current Hero `AiPromptInput` rendering/settings, current Hero Toolcraft prompt targets, and the Fine Details website/Toolcraft bridge.
- Contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, control selection, layout, runtime boundary, core performance, schema reference, component rules, renderer technique, and app performance; the current Next.js Server/Client Components guide was also read before the website edit.
- Contract rules applied: built-in Vector/Switch/Slider/ColorOpacity controls; canonical schema defaults; panel-only direct-authored position ownership; product output only in the existing website iframe; no custom handles, Layers, Timeline, media, artifact export, or prompt-content editor.
- View interaction: unchanged `fixed-camera`; the migrated popup is flat DOM output and does not introduce a spatial model, orbit, or orientation target.
- Interaction ownership: `prompt.position` belongs to the panel because the user explicitly requested movement through the pad; a duplicate canvas drag was rejected because it would add a second owner and editing chrome over website output.
- Decision: transfer complete ownership instead of sharing state. Fine Details protocol v2 carries background, grid, height, prompt position, and prompt shadow as one atomic object; Hero protocol v16 is prompt-free and aligned with its current website receiver.
- Alternatives rejected: duplicate prompt rendering in both sections; cross-app shared state; new text/preset/output/reference/submit controls; custom position handles; a second persistence endpoint.
- State/output mapping: `prompt.position` maps normalized X/Y to the Fine Details section; `prompt.shadow.enabled`, `offset`, `blur`, `spread`, and `colorOpacity` create the panel box shadow; Apply persists the complete Fine Details object; Reset restores the shared migrated defaults.
- Performance: the existing constant-cost iframe preview-sync pass now includes six prompt targets; no workload dimensions, rendering loop, measured performance request, or new resource lifecycle was introduced.
- Verification: the user explicitly requested implementation and manual local review without strong checks; tests, lint, typecheck, build, browser suites, formatting, diff verification, delivery verification, and measured performance were not run.
- Risks/follow-up: visual fit across every viewport and the complete Apply/Reset round trip remain for the user's requested local manual review on the already-running servers.

## Decision Trail: add edge-anchored Fine Details typography

- Request: implement the updated Figma `TRY IT` and `YOUR WAY` compositions, anchor them to opposite section edges, and expose their pixel insets, font sizes, and lower composition gap in Toolcraft.
- Task type: later Tier 2 schema/product behavior edit extending the existing website-owned Fine Details iframe.
- User-visible result: Fine Details now renders `TRY IT` 112px from the upper-left edge and the `YOUR WAY` heading/body composition 112px from the lower-right edge; eight px controls edit both anchors, three font sizes, and the internal gap live.
- Source/reference checked: Figma node `6063:943` after the user's 112px update, its 1920 × 1080 screenshot, the existing site font registry, and the Fine Details settings bridge.
- Decision: keep both typography groups as pointer-transparent absolute website DOM and carry their measurements in the same atomic settings object as background, section height, and prompt.
- State/output mapping: `typography.upperLeft.left`, `top`, and `fontSize` control `TRY IT`; `typography.lowerRight.right`, `bottom`, `headingFontSize`, `bodyFontSize`, and `gap` control the lower composition.
- Typography: reuse the registered ABC Gravity variable upright face for `TRY IT`, ABC Gravity Wide Trial Ultra Italic for `YOUR WAY`, and Geist Medium for the body copy.
- Persistence: Fine Details protocol v3 includes typography in live preview, Apply, Reset, tracked applied JSON, and adjacent-tab broadcasts.
- Performance: the existing constant-cost iframe sync receives eight scalar controls while retaining the same grid, typography, and prompt DOM nodes.
- Verification: per the user's workflow, no tests, lint, typecheck, build, browser suite, formatting, commit, or push is run; the running local apps are handed off for manual review.

## Decision Trail: add the Fine Details cursor image trail

- Request: implement the supplied image-trail specification so uploaded images appear sequentially beneath the cursor, expose the complete effect through Toolcraft, and hand the feature off without strong checks.
- Task type: later Tier 2 media, protocol, motion, and website-renderer extension across the existing Toolcraft iframe boundary.
- User-visible result: Toolcraft now has Trail Images, Trail, Trail Motion, and Trail Shadow sections; uploaded mixed-aspect images cycle in order behind the existing typography and prompt as the pointer moves across either the standalone website or the Toolcraft canvas.
- Source/reference checked: the user-authored `2026-08-24-fine-details-image-trail-design.md`, the existing Fine Details v3 bridge, Hero's runtime media bridge/store, the Toolcraft built-in image file-drop lifecycle, and the installed `motion/react` runtime.
- Interaction ownership: Toolcraft's built-in file-drop collection owns image upload, reorder, rotate, flip, remove, and reset. The website owns spawning, spring smoothing, lifetime, fading, prompt hit-testing, and card rendering. The iframe remains pointer-transparent; its wrapper forwards scene-pixel pointer samples at most once per animation frame.
- Protocol: Fine Details advances to v4. Atomic settings messages carry ordered image metadata and all numeric/shadow values; separate runtime media messages carry Blob payloads; separate pointer messages carry active scene coordinates.
- Persistence: Apply strips `trail.images` before the API and tracked JSON write, then restores the current runtime image list only inside the iframe. Adjacent website tabs receive image-free persisted settings. Reset restores numeric defaults and clears the runtime image set.
- Media lifecycle: Blob URLs are created once per resource ref, decoded for dimensions with a bounded 2048px bitmap path, reused across cards, and revoked after a five-second unused grace period. Runtime image transforms are retained in settings and consumed by the renderer.
- Motion behavior: cards spawn by smoothed travel distance, cycle media order, cap at 2–24 retained nodes, shrink by age rank with a 0.1 minimum scale, receive bounded random tilt, and expire through configurable enter/lifetime/exit timing.
- Suppression and gates: no new card spawns over the AI prompt; exit uses a configurable delay and ease-out ramp. The engine is off when disabled, when no images exist, for reduced motion, or after touch input.
- Layering: the pointer-transparent, clipped trail layer is mounted directly after the background grid and before the `z-[1]` typography; the prompt remains at `z-10`.
- Performance: one bounded 24-card DOM pool, cyclic reuse of decoded image URLs, a maximum of six interpolated spawns per frame, and one RAF-coalesced Toolcraft pointer message per frame. Pan and zoom do not invalidate media or settings passes.
- Alternatives rejected: persisting local Blob/image references; enabling pointer events on the iframe; duplicating the effect inside Toolcraft; rendering cards above typography or prompt; unbounded spawning; adding custom upload or per-image controls.
- Verification: the user's established workflow applies—implementation is handed off for local manual review; tests, lint, typecheck, build, browser automation, formatting, delivery verification, commit, and push were not run.

## Decision Trail: remove the trail image cap and bound preview media

- Request: remove the 24/25-image upload limit, keep the trail collection product-unbounded, use Next Image, and avoid transferring oversized originals into the website preview; preview resizing must happen once rather than after every Card size change.
- Decision: Toolcraft retains originals and caches one derivative per resource reference and quarter-turn orientation. The card-height axis is capped at `400px × 2 DPR = 800px`; smaller inputs are not enlarged and oversized inputs are encoded once as WebP at quality 0.86.
- Data flow: media is prepared with concurrency four and posted in batches no larger than four instead of one collection-wide promise/message. Settings retain the complete ordered descriptor list while the existing live-card Length limit remains 2–24.
- Count limits: removed the Trail Images `hardMaxItems` and advisory item wording, both Toolcraft truncations, website settings truncation, and website media-message count rejection. Per-item MIME/blob/identifier validation remains.
- Rendering: the website store now retains the received derivative URL and records its true dimensions; the trail card uses `next/image` with explicit intrinsic dimensions and `unoptimized` for the local `blob:` source. Card geometry and animation are unchanged.
- Verification: focused derivative/count tests and Next Image/Motion source tests pass. A local browser smoke-check imported 25 images in addition to the existing one, observed 26 synchronized descriptors with no maximum-item error, spawned trail cards, and observed a `591×800` derivative-backed Next Image with no console errors. Repository-wide build, lint, and browser suites were not run.
- Existing unrelated gates: package type checking still reports the prior missing `selectionScope` entry in Toolcraft acceptance data; website type checking still reports the prior missing draggable-prompt module/test configuration and Hero renderer/boundary errors. No new type error remained in the changed implementation files.

## Decision Trail: let the trail pass under the prompt and pause on focus

- Request: remove the prompt-area movement restriction, place trail cards above both typography compositions but below the prompt, and stop animation only while focus is inside the prompt.
- Decision: remove prompt rectangle hit-testing from pointer updates. The trail now receives continuous standalone and Toolcraft bridge coordinates across the full section.
- Layering: typography remains at `z-index: 1`, the trail owns `z-index: 5`, and the draggable prompt remains at `z-index: 10`.
- Focus behavior: bubbling `focusin` suppresses new spawns and clears only the spawn anchor; existing cards keep their lifetime/fade timers. `focusout` ignores transfers to another prompt descendant and otherwise resumes through the configured Resume delay and Resume ramp.
- Verification: four focused Node source/component tests pass. Local smoke-check observed cards at prompt coordinates, computed layer order `1 < 5 < 10`, seven existing cards retained at focus and naturally expired to zero without replacements, internal focus transfer remaining suppressed, post-blur spawning returning, and no console errors. Broad checks and commits were not run.

## Decision Trail: draggable Fine Details prompt with animated reset

- Request: let visitors drag the complete prompt panel and return it quickly to its authored position by double-clicking non-interactive panel chrome.
- Ownership: `prompt.position` remains the sole persistent Toolcraft-authored target. The website wrapper owns only a session-local pixel offset; drag, reset, Apply, persistence, history, and exported settings never write that offset.
- Interaction: native website use relies on primary Pointer Events and pointer capture. Inputs, buttons, links, editable descendants, and interactive roles remain excluded from drag start. The panel is clamped to a 16px section inset when it fits, while small sections retain a reachable 48px edge.
- Reset: double-click animates the transient offset to zero over 220ms with a fast ease-out; reduced motion resets immediately and preserves form content.
- Root cause found in real-iframe acceptance: the Toolcraft iframe is intentionally `pointer-events: none`, so direct website pointer handlers cannot receive a real Toolcraft canvas drag. Enabling iframe pointer events would regress CanvasShell pan/zoom and the existing image-trail pointer bridge.
- Protocol: Fine Details advances to v5. The website publishes the live prompt rectangle in shared 1920×section-height coordinates. Toolcraft captures and forwards down/move/up/cancel/double-click only for a hit inside that rectangle, stopping CanvasShell propagation only for the active prompt gesture. Pointer transparency, canvas gestures, and trail hover remain unchanged everywhere else.
- Verification: website helper/source tests pass 6/6; Toolcraft bridge/ownership tests pass 4/4; the exact real-iframe Playwright drag/reset proof passes 1/1 after disabling the autonomous trail through its real Toolcraft switch; Toolcraft typecheck passes; focused website oxlint passes with zero findings. Website typecheck was attempted and stopped only on three concurrent/baseline Fine Details trail and Hero errors outside this feature. The first browser run honestly failed at `0.00:0.00` and led to the bridge diagnosis; a later baseline run exposed autonomous trail pixels and was isolated rather than weakening the assertion.
- Exclusions: no broad build, aggregate browser suite, delivery verification, or measured-performance run was requested or executed. Existing concurrent image-trail, Hero CTA, and other dirty worktree changes were preserved.
- Follow-up interaction refinement: the v5 geometry message now carries the live prompt rectangle plus the actual rendered rectangles of every semantic/focusable interactive descendant in shared scene coordinates. Toolcraft starts prompt gestures only inside the prompt and outside all exclusions, so textarea, input, button, link, select, editable, tabindex, explicit no-drag, and interactive-role surfaces never drag the panel while outer padding, inter-control gaps, and free lower-row space retain drag/reset ownership. Native website behavior uses the same semantic selector.
- Follow-up verification: deliberate RED produced 5/7 website tests and 1/3 Toolcraft bridge tests before the collection implementation. Final focused checks pass: website 7/7; Toolcraft bridge/drag 5/5; exact real-iframe Playwright 1/1, which attempts a drag from every currently rendered interactive descendant, requires `0.00:0.00` after each, then proves padding drag and animated reset. Focused website oxlint reports zero findings and `git diff --check` passes. No broad build, aggregate suite, or measured-performance run was executed.

## Decision Trail: make the current Fine Details scene and 50 Dia images the defaults

- Request: make `fine-details-settings (2).json` the canonical Fine Details configuration, preserve the 50 images currently attached in Dia as the default trail collection, and deliver website images through `next/image`.
- Defaults: the supplied prompt position/shadow, trail border/radius/size/timing/falloff/spacing/tilt, and both typography groups now define Toolcraft, website runtime, and applied JSON defaults. Runtime uploads remain explicit live overrides; persisted website settings intentionally omit binary media and restore the built-in set when `trail.images` is absent.
- Media identity: the ordered collection uses stable ids `fine-details-default-01` through `fine-details-default-50`. Toolcraft registers them as removable/resettable `media.defaultAssets` for `trail.images`; the website resolves the same ids to checked-in public files while preserving intrinsic aspect ratios and transforms.
- Asset pipeline: the website set is 50 WebP files bounded to an 800px maximum axis at quality 82. Toolcraft uses a separate 320px/quality-70 preview set. A generated JSON data manifest serializes those preview assets because the Playwright reporter imports schema code in plain Node, where Vite-only `import.meta.glob` is unavailable.
- Website renderer: checked-in defaults use optimized `next/image` URLs with intrinsic dimensions; only runtime `blob:` uploads remain `unoptimized`. A focused browser request to the Next optimizer returned HTTP 200 with an image response for `fine-details-trail-01.webp`.
- Exact weight: original Dia JPEGs total 33,314,449 bytes (31.771 MiB); website WebPs total 1,235,732 bytes (1.178 MiB); Toolcraft preview WebPs total 206,012 bytes (0.196 MiB); both binary image variants total 1,441,744 bytes (1.375 MiB), a 95.67% reduction from the originals. The build-owned base64 JSON manifest is 276,197 bytes (0.263 MiB) of serialized bundle storage, not a third image variant.
- Verification: focused Toolcraft defaults/derivative tests pass 5/5; focused website settings/Next Image/radius/border tests pass 7/7; focused Toolcraft and website lint report zero findings; Playwright discovery under the plain-Node reporter succeeds; scoped `git diff --check` is required before handoff. Aggregate delivery, broad browser/build/typecheck, and measured performance were not run.
- Known base-uploader risk: a real Toolcraft upload attempt assigns files to the built-in input but does not commit a user media asset while `media.defaultAssets` are present. The temporary browser scenario was removed rather than reported as a false pass. This does not affect the checked-in 50-image default collection, Reset restoration, or website `next/image` delivery, but the generic uploader coordinator needs a separate fix before claiming the new-upload lifecycle green in this configuration.

## Decision Trail: sustained trail motion performance

- Request: давай внесем корректировки по плану
- Task type: later Tier 2 localized sustained-motion performance correction adopted from the attached plan; no protocol, schema, control, persistence, export, or visual-design change.
- User-visible result: Fine Details keeps the same trail behavior and appearance while moving bridge-pointer updates out of React rendering, warming received image decodes sequentially, reducing repeated card allocations and retarget work, requesting display-sized built-in Next Image candidates, and bounding panorama derivatives on both axes.
- Source/reference checked: attached `docs/superpowers/plans/2026-08-24-fine-details-trail-sustained-motion-performance.md` at `recraft-tools/fine-details/docs/superpowers/plans/2026-08-24-fine-details-trail-sustained-motion-performance.md`; current website trail/store implementation; current Toolcraft derivative, preview pipeline, `appPerformance`, and app-owned `deriveToolcraftPerformancePaths` output.
- Reference inputs: None; this correction uses the attached performance plan and current source/runtime contracts rather than a visual or motion reference asset.
- Docs/contracts read: complete `docs/toolcraft/acceptance-testing.md` and complete `docs/toolcraft/performance.md` before proof, plus the attached sustained-motion performance plan.
- Contract rules applied: keep selected output quality and live interaction behavior; preserve the existing protocol/schema/control/visual surface; derive path authority from current app code; run focused functional checks only; do not claim measured performance from source assertions, unit tests, lint, build, or prose.
- View interaction intent: unchanged `fixed-camera`; sustained trail hover forwards scene coordinates to the external website preview and introduces no orbit target or canvas editing handle.
- Interaction ownership: the Toolcraft preview wrapper continues to own RAF-coalesced pointer forwarding, while the website trail owns motion values, card spawning, decode warmup, and card rendering; no operation moved between canvas and panel.
- Decision: Toolcraft derivatives were already real. Source inspection identified plausible contributors targeted by this change: on-demand browser decodes, React bridge re-render/allocation and animation-retarget pressure, and the panorama long-axis hole. Their causality and relative impact remain unverified until the two required 60-second traces in Toolcraft and standalone. Use imperative pointer subscription, sequential `Image.decode()` warmup with lifecycle cleanup, memoized `TrailCardView` with stable objects, built-in Next Image display×2 dimensions plus real CSS `sizes`, and a 1600px long-axis derivative cap.
- Alternatives rejected: original-transfer diagnosis; protocol/cache-key churn; changing trail spacing, lifetime, fade, falloff, ordering, or controls; pinning every decode in canvas/ImageBitmap memory; replacing the card renderer with an imperative DOM pool before profiling proves it necessary.
- State/output mapping: `trail.images` still selects the ordered media set; runtime Blob derivatives remain `unoptimized`; checked-in defaults retain Next optimization with display-sized candidates; pointer snapshots write directly to the existing motion values; card rank still maps to the same scale falloff and animations; derivative dimensions now satisfy the existing 800px card-axis cap and the 1600px long-axis cap.
- Performance intent: performance-iteration
- Performance request evidence: "давай внесем корректировки по плану"
- Performance paths: ["performance-path:%5B%22batch-responsive%22%2C%22media-import%22%2C%5B%22media-sync%22%2C%22preview-sync%22%5D%2C%5B%22main%22%5D%2C%5B%22trail-image-count%22%5D%5D"]
- Performance path scope: current `appPerformance` derives `media-import`, `initial-render`, `viewport-drag`, `viewport-zoom`, and `control-change`; only `media-import` is an exact affected canonical path. Sustained trail-hover/pointer sampling is not modeled as a separate current interaction, and `viewport-drag` is canvas pan rather than trail hover, so no unsupported ID is recorded.
- Protocol/cache/session lifecycle: Fine Details protocol v5, schema, controls, visual trail behavior, derivative cache key, and `sentKeys` are unchanged. An already-open Toolcraft session must reload before derivatives already present in `sentKeys` are recomputed with the new long-axis cap.
- Verification: website `pnpm dlx tsx --test src/components/pages/home/fine-details-image-trail-performance.test.ts src/components/pages/home/fine-details-image-trail-next-candidates.test.ts src/components/pages/home/fine-details-image-trail-image.test.ts src/components/pages/home/fine-details-image-trail-motion.test.ts src/components/pages/home/fine-details-image-trail-prompt-focus.test.ts src/components/pages/home/fine-details-trail-media-warmup.test.ts src/components/pages/home/fine-details-trail-card-border.test.ts src/components/pages/home/fine-details-trail-card-radius.test.ts src/components/pages/home/fine-details-unbounded-images.test.ts src/components/pages/home/fine-details-draggable-prompt.test.ts` passed 20/20; focused website `pnpm exec oxfmt --check` passed on 13 performance-scoped files; website `pnpm lint` exited 0 with two unrelated Hero `no-img-element` warnings; website `pnpm build` passed with existing `metadataBase` and dynamic-filesystem-tracing warnings and its configured type-validation skip; Toolcraft `pnpm exec vitest run src/app/fine-details-preview-image-derivative.test.ts src/app/fine-details-preview.product.test.ts --reporter=default` passed derivative 15/15 and produced 20 passes plus two unrelated current-default/acceptance-baseline failures in the preview product file; the exact bridge/media-message filter `pnpm exec vitest run src/app/fine-details-preview.product.test.ts -t "sends the canvas height through the versioned website bridge" --reporter=default` passed 1/1 with six unrelated tests skipped; Toolcraft `pnpm typecheck` passed; local Toolcraft `pnpm exec oxlint --version` and `pnpm exec oxfmt --version` both reported command not found because this app has no lint/format scripts or binaries; Toolcraft `pnpm docs:check` passed; repository-root `git diff --check` passed with no output. No 60-second Toolcraft or standalone trace, CPU 4× run, memory snapshot, protected `verify:delivery`, protected `verify:perf`, full audit, or broad browser/export matrix was run, so no measured no-freeze, frame-time, CPU, or memory claim is made.
- Risks: browser decode caches may still evict warm entries at extreme collection sizes; rank changes intentionally retain the existing scale retarget behavior; React development builds may still show GC noise; the 1600px cap changes only extreme panoramas; the editor bridge retains its normal approximately one-frame message latency; the first optimized built-in candidate may still load once in development; the known default-assets uploader baseline and unrelated current acceptance/default-test failures remain outside this performance correction.

## Decision Trail: Fine Details image carousel mode

- Request: add the supplied four-image carousel mode to Fine Details, preserve the current trail mode, and add one shared toggleable border and drop shadow treatment for every carousel image.
- Task type: completed cross-repository later-feature Tier 3 schema, controls, preview-payload, persistence, applicability, website settings, and DOM/CSS renderer update.
- User-visible result: the always-visible Images selector switches between Trail and Carousel. Carousel mode exposes authored image count, one equal Text gap from both typography edges, corner radius, horizontal gap, speed, border, and shadow controls; Trail remains the default and its existing settings and media values are preserved while hidden. Carousel card height now fills the measured typography band after the two text gaps, capped at 800px.
- Source/reference checked: the user-supplied carousel implementation plan and the four authored `restyle_8.jpeg` through `restyle_11.jpeg` website assets; current Fine Details trail, preview bridge, acceptance inventory, and pipeline contracts.
- Reference inputs: None; the still images are authored website content rather than Toolcraft-uploaded reference inputs.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, core control selection, layout, runtime boundary, core performance, schema reference, component rules, renderer technique, app performance, and acceptance testing.
- Contract rules applied: built-in Segmented, discrete/continuous Slider, Switch, ColorOpacity, and Vector controls; fourteen controls split into three balanced workflow sections with one entity id; conditional applicability preserves hidden values; no custom control, file-drop, export, layer, timeline, or carousel media protocol was added.
- View interaction intent: unchanged `fixed-camera`; the carousel is flat website DOM output with no spatial model, orbit target, or canvas handle.
- Interaction ownership: the panel owns exact carousel properties and mode selection; the website CSS renderer owns measured placement and autonomous overflow travel. No duplicate canvas editing surface was added.
- Decision: default to `trail`; use four authored static site assets in source order; center the website strip inside a ResizeObserver-measured band between the typography compositions; derive height as `min(800px, band height - 2 × carousel.textGap)`; let website CSS own static-fit versus linear-overflow rendering; keep shared border and shadow disabled by default and preserve their authored values while disabled.
- Alternatives rejected: keeping a manual Height setting, adding a Fill mode, adding separate top and bottom gap controls, placing carousel files in Toolcraft media, reusing the trail upload protocol, introducing a carousel file-drop, combining all fourteen controls into an invalid oversized section, or clearing border/shadow values when their switches turn off.
- State/output mapping: `images.mode` selects the mutually exclusive renderer; `carousel.count`, `carousel.textGap`, `carousel.radius`, `carousel.gap`, and `carousel.speed` define the strip; `carousel.border.*` and `carousel.shadow.*` define shared image effects. The paired website receiver consumes protocol v7, normalizes and persists `textGap` while ignoring legacy carousel height, serves the four static authored assets, and renders the auto-height measured carousel band; pointer and media message shapes remain unchanged.
- Performance intent: ordinary product work; the auto-height calculation adds no workload-envelope dimension. The preview-sync pass still receives fourteen carousel targets and advances to runtime id `fine-details-external-preview-v7`; no measured performance was requested or run.
- Verification: focused website carousel/settings tests passed 20/20; focused website formatting passed for the five touched source/test files; focused Toolcraft carousel contract tests passed 8/8; repository-root `git diff --check` passed. The browser scenario `browser: carousel mode replaces the trail and animates when overflowing` now uses count four plus a 100px horizontal gap for overflow and count one for static fit, but was intentionally not executed because the user requested no strong checks. No build, broad typecheck/lint, full suite, browser matrix, delivery verification, kernel benchmark, measured performance, or full audit was run.
- Risks: real browser geometry, the 800px cap, overflow travel, shared card effects, and Next Image rendering still require the user's local visual check; an older v6 website cannot acknowledge the v7 bridge.

## Decision Trail: carousel-only prompt drag affordance

- Request: `сделай на промпт блок на область за которую можно делать драг, курсор драга, только в стейте Карусель.` and `давай в трэид драг отключим`
- Task type: later Tier 2 localized website interaction and acceptance refinement; no schema, protocol, persistence, renderer-pipeline, or performance-path change.
- User-visible result: Carousel's intentionally pointer-interactive iframe lets the native website prompt chrome show `grab`, change to `grabbing` during root capture, move the complete prompt, and keep double-click reset; actual hittable interactive descendants retain normal cursors and do not move it. Selecting Trail cancels any website or defensive host gesture, releases capture, restores the transient prompt offset to zero, removes the drag cursor and bridge hit region, and makes the iframe pointer-transparent again without clearing the form value.
- Source/reference checked: the approved `docs/superpowers/specs/2026-08-25-fine-details-carousel-prompt-drag-cursor-design.md`, its implementation plan, the current website prompt wrapper and bridge, the real `images.mode` segmented control, and the existing `prompt.runtimeOffset` acceptance scenario.
- Reference inputs: None; this change follows explicit interaction requirements and current source rather than a visual or motion reference asset.
- Docs/contracts read: both active app `AGENTS.md` files, `docs/toolcraft/workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `component-rules.md`, `renderer-technique.md`, and `acceptance-testing.md`.
- Contract rules applied: preserve the signed Toolcraft runtime boundary; use the existing schema control and transient website/bridge owner; prove product behavior through the real visible control and iframe; keep interactive form controls excluded; use only later-feature focused checks and no aggregate or measured-performance proof.
- View interaction intent: unchanged `fixed-camera`; the prompt is flat website DOM content with no spatial model, orbit target, or orientation handle.
- Interaction ownership: `images.mode` remains panel-owned; Carousel's pointer-interactive iframe gives the native website prompt root session-local drag/reset ownership, while the existing host bridge remains a defensive compatibility path. Trail makes the iframe pointer-transparent and publishes no prompt hit region so wrapper-owned trail/canvas behavior remains available. Persistent authored `prompt.position` remains panel-owned, and no duplicate persistent canvas position editor was added.
- Decision: pass one required `dragEnabled={settings.imagesMode === 'carousel'}` capability into the prompt wrapper; use fresh refs inside long-lived website and host subscriptions; cancel pointer capture and bridge/native gestures on disable; clear only the transient CSS offset; conditionally publish geometry and `touch-action`; and use a local CSS module for website root `grab`/`grabbing` plus descendant `cursor: revert`. Animated reset withdraws bridge geometry before its 220ms transform and republishes only at transition completion. The defensive Toolcraft host treats its own committed mode as authoritative, ignores late Carousel geometry in Trail, stores client rather than normalized cursor coordinates, recomputes hit mapping on each geometry sync, and centralizes mode/null/lost-capture cleanup. The concurrent Carousel interaction contract is preserved: production iframe pointer events are enabled only in Carousel and remain disabled in Trail.
- Alternatives rejected: inferring image mode inside the wrapper; leaving bridge geometry active in Trail; hiding only the cursor while retaining drag/reset; persisting visitor offset into `prompt.position`; resetting form/settings/Apply/history; changing protocol or schema; and global cursor CSS.
- State/output mapping: `images.mode=carousel` maps to `data-fine-details-prompt-drag-enabled="true"`, an interactive iframe, live prompt geometry except during animated reset, native/defensive-bridge gesture handling, conditional `touch-action: none`, transient `translate3d`, and native website grab cursors. `images.mode=trail` maps to an iframe-transparent wrapper target, enabled=false, `0.00:0.00`, dragging=false, `{ interactiveRects: [], rect: null }`, released website/host capture, normal wrapper cursor, no drag touch policy, and retained prompt text/authored position.
- Performance intent: ordinary later feature work; no performance complaint, workload dimension, renderer pass, benchmark, or measured-performance authority was introduced.
- Verification: deliberate initial website RED failed 3/8 expectations; the first quality follow-up RED was website 8/9 for missing reset geometry withdrawal and Toolcraft 3/5 for missing host mode/client-coordinate/lost-capture contracts. The final readiness-lifecycle RED was Toolcraft 5/6 because canvas height still flowed through callback identities into the long-lived message subscription. Scene height now updates through a layout-synchronized ref, `getScenePoint` stays stable, and the focused regression links that stable subscription to ready and save-result acknowledgment; Toolcraft bridge tests pass 6/6 and the concurrent Carousel iframe interaction contract passes 1/1. Final website prompt tests pass 9/9, focused website formatting passes on five files, and focused website oxlint reports zero findings. The preview product file passes 6/7 but retains its unrelated current 39-error acceptance-validator baseline; Toolcraft typecheck and docs check pass. The first strengthened browser target assertion truthfully exposed the concurrent interactive-iframe contract, and two later RED runs removed test assumptions about covered descendant centers and an obsolete four-control minimum. The final exact `pnpm test:feature -- prompt.runtimeOffset` passes 1/1 and proves top-level `elementFromPoint` resolves to the iframe in Carousel and wrapper in Trail; native website grab/grabbing, root capture, movement, reset-time `rect: none` followed by final geometry, real held Carousel-to-Trail cancellation, released root capture, zero offset, no later held movement, interactive exclusions, and retained prompt text. It was not rerun for the final ref-only lifecycle correction because interaction and browser scenario source were unchanged. Scoped `git diff --check` passes. Website typecheck was previously attempted and remains blocked by three unrelated existing Trail/Hero errors. No aggregate suite, build, delivery verification, browser matrix, export matrix, kernel benchmark, or measured-performance check was run.
- Risks: Trail outer-chrome drag intentionally falls through to the Toolcraft canvas because its bridge geometry is empty; acceptance therefore proves the prompt stays at its authored position relative to the section while the canvas may pan. The host bridge remains defensive rather than Carousel's primary event path under the current interactive-iframe contract. Existing unrelated acceptance-validator and website typecheck baselines remain outside this change.

## Decision Trail: carousel card hover, manual drag, and download

- Request: make the planned hover pause work inside Toolcraft, let visitors drag the overflowing carousel directly, and show a 40px black/70% Phosphor Download action on the hovered image.
- Interaction ownership: Trail keeps the pointer-transparent iframe and its existing parent-owned pointer bridge. Carousel makes the website iframe pointer-interactive so card hover, native prompt interaction, manual carousel drag, and downloads behave the same in Toolcraft and standalone.
- Motion: retain the GPU-backed CSS marquee. Card hover pauses only the animated track under a real hover-capable pointer; empty typography-band space does not pause it. Pointer drag samples the current computed transform, normalizes it into one loop interval, moves the phase under pointer capture, and resumes automatic movement from that phase after hover ends. Static and reduced-motion rows keep their previous behavior.
- Cards: existing radius, border, and shadow move to a relative card shell without changing values or image geometry. Next Image remains optimized and uncropped. The hovered/focused shell reveals a 40px circular `rgb(0 0 0 / 70%)` action with the white Phosphor `DownloadIcon`; it downloads the authored JPEG and does not start a drag.
- Iframe sandbox: add `allow-downloads` only to permit the explicit user-triggered authored-image download. No settings, Apply/Reset payload, saved JSON, control inventory, or protocol version changes.
- Verification: focused Toolcraft carousel interaction/value/control tests passed 9/9; focused website carousel/settings tests passed 22/22; touched website source/test formatting and repository-root `git diff --check` passed. Per the user's workflow, no build, broad suite, browser matrix, performance proof, commit, or push was run.

## Decision Trail: carousel track hover and original-file download refinement

- Request: retain direct carousel dragging without a `grab`/`grabbing` cursor, keep animation paused while crossing horizontal card gaps, switch the action to Phosphor Arrow Line Down, use 40%/60% black button backgrounds, and download the original authored image while previewing through Next Image.
- Root cause: the pause selector targeted `.cardShell:hover`, so every configured gap removed the only active hover predicate and resumed the CSS marquee. The pause owner now targets the rendered `.track:hover`, whose flex box includes intra-sequence and loop-sequence gaps without claiming empty vertical typography-band space.
- Interaction ownership: manual pointer drag, CSS phase continuation, prompt drag/cursor behavior, Toolcraft iframe ownership, and Trail mode are unchanged. Only the carousel card cursor rules were removed; the download action retains its ordinary pointer cursor.
- Image delivery: each carousel asset now carries an imported static image for optimized `next/image` preview plus a direct public JPEG URL and original filename for the anchor download. The download never targets a Next optimizer response.
- Verification: focused website carousel test passed 13/13; focused website settings test passed 9/9; focused Toolcraft carousel-preview interaction test passed 1/1; touched website formatting passed; repository-root `git diff --check` passed. No build, browser matrix, performance proof, commit, or push was run.
## Decision Trail: standalone Trail-to-Carousel mock state

- Request: keep Fine Details as one section on the standalone website, start it in Trail, and replace only its image effect with Carousel after prompt Submit; Carousel must retain the existing prompt drag behavior. Remove Exact and the divider before it from the shared prompt before transfer.
- State ownership: the existing preview boundary still owns live/applied visual settings. A website-only client wrapper owns one session-local `trail | carousel` mode, starts in Trail on every page load, and idempotently selects Carousel after a valid submission. Toolcraft continues to render the settings-owned mode directly, so submitting its prompt cannot change the selected editor mode.
- Composition: background, grid, both typography compositions, and the prompt stay mounted as one persistent section. Only the mutually exclusive image renderer changes. Trail exits over 250ms while Carousel enters over 350ms through an overlapping opacity crossfade; reduced motion switches immediately and the outgoing renderer unmounts after exit.
- Interaction: the prompt forwards its existing valid-submit payload to the website wrapper. Its transient text/reference state is preserved because the form is not remounted. Prompt drag remains disabled in Trail and becomes enabled through the existing `imagesMode === 'carousel'` contract in Carousel.
- Prompt model: removed the output-mode selector, its preceding divider, the `Exact`/`Vibe` options, the local output-mode state, the exported type, and the submission property. Style preset, references, keyboard submit, disabled empty-submit behavior, and file lifecycle are unchanged.
- Verification: focused website mock/prompt/drag/Motion/Carousel contracts pass 29/29; touched files were formatted with oxfmt; the running local Next server compiled and returned HTTP 200 for `/`. `git diff --check` passed. Per the requested workflow, no broad build, full typecheck/lint, browser matrix, commit, or push was run; the visual transition remains for local user review.
- Follow-up submit-button correction: the external `prompt-submit.svg` used a circle touching every edge of its 40×40 image viewport, which clipped the top antialias, and the resulting HTML image retained native `draggable=true`, producing the grey drag ghost shown in the user's screenshot. The submit action now draws its 40px black circle in CSS, renders the same arrow as safe inline SVG paths, explicitly opts out of prompt drag, and cancels any native drag start. Focused prompt contracts pass 12/12; a real local Carousel-state drag from the button leaves prompt offset `0.00:0.00`, dragging `false`, and no image element in the button.

## Decision Trail: make the static Fine Details typography non-selectable

- Request: `убери возможность выделять у этих блоков`, with the supplied screenshot marking the static `TRY IT` composition and the `YOUR WAY` heading/body composition. This is a later Tier 1 website interaction refinement; it does not change schema, protocol, persistence, renderer workload, or performance authority.
- Diagnosis: both static typography owners already used `pointer-events-none`, but pointer hit-testing and native text selection are separate browser behaviors. Their inherited `user-select: auto` still allowed a canvas drag crossing either group to create a selection. The central prompt is a separate interactive owner and must remain selectable.
- Decision: add `select-none` only to `data-fine-details-upper-left-typography` and the shared `data-fine-details-lower-right-typography` container. This keeps the lower heading and body under one boundary while leaving the section, prompt, and all other copy unchanged. Rejected alternatives were section-wide `select-none`, repeating the rule on each lower descendant, and JavaScript selection cleanup.
- Interaction ownership: the two static owners now own only their inherited native-selection boundary. The prompt keeps native input selection and its existing Carousel drag ownership. View, camera, animation, content, layout, and output mapping are unchanged.
- Verification: the focused source contract was meaningfully RED at 2/3 because the upper static owner lacked `select-none`, then GREEN at 3/3 after both scoped classes were added. Scoped oxfmt completed. On the running website, computed `user-select` was `none` for both marked groups and `auto` for the prompt; real drags across each static group produced an empty selection, while `ControlOrMeta+A` selected all 75 prompt characters. Scoped whitespace validation passed. No broad suite, build, performance proof, commit, or push was run.
- Risks: the marked static marketing copy is intentionally unavailable for native text selection. Prompt editing/selection remains available, and selection behavior outside the two marked owners is deliberately unaffected.

## Decision Trail: Fine Details loading state

- Request: add a third editable Fine Details image state between Trail and Carousel, render exactly three shimmer placeholders while generation is pending, cover the real Toolcraft/website integration in a browser, and model the standalone mock as `trail → loading → carousel` without adding a Toolcraft timer.
- Task type: later Tier 2 schema applicability, website DOM renderer, protocol, standalone lifecycle, acceptance, and browser-integration work.
- User-visible result: the built-in Images segmented control now exposes `Trail | Loading | Carousel`. Loading renders three fixed-count equal squares with no border or shadow; current Text gap controls their shared auto-height through the same typography-band geometry as Carousel, while Corner radius and Gap apply live. The shimmer is a synchronized 1.6s vertical gradient whose effective keyframe travel is downward, and reduced motion replaces it with a static solid background.
- Source/reference checked: the approved loading-state plan, the current `fine-details-carousel-values.ts` and control sections, the website typography-band/carousel geometry, loading CSS/component, image-state transition, homepage mock, prompt drag boundary, protocol sender/receiver, acceptance metadata, and the existing Trail/Carousel browser scenario.
- Reference inputs: None; behavior comes from the explicit loading requirements and current product source.
- Docs/contracts read: `recraft-tools/fine-details/AGENTS.md`, `docs/toolcraft/workflow.md`, `core/runtime-boundary.md`, `core/performance.md`, `renderer-technique.md`, `performance.md`, `acceptance-testing.md`, the existing implementation plan, `recraft-v4-styles/AGENTS.md`, the website README, and the installed Next.js Server/Client Components guide.
- Contract rules applied: use the built-in segmented and slider controls; keep final settings in runtime state; prove a real iframe product outcome and segmented layout under the exact acceptance title; keep later work to focused unit/browser checks; preserve the website DOM renderer; do not run delivery, performance, export, build, or lint matrices in this subtask.
- View interaction intent: unchanged `fixed-camera`; Loading is flat DOM output and introduces no orbit, orientation target, canvas handle, layer, timeline, or artifact export.
- Interaction ownership: `images.mode` and Loading geometry remain panel-owned Toolcraft settings. The website loading root and all cards are pointer-transparent, the Toolcraft iframe remains pointer-transparent outside Carousel, and the prompt publishes `data-fine-details-prompt-drag-enabled="false"` in Loading. Carousel retains its existing website interactions; Trail retains its existing pointer bridge. No shimmer-speed or timer control was added.
- Decision: use exactly three squares independent of Carousel Images shown. The stale planned Height/`carousel.height` mapping was adapted because the current product removed that target: both Carousel and Loading derive square height from the current shared typography band, its 800px cap, and `carousel.textGap`. Loading reuses `carousel.radius` and `carousel.gap`; only Text gap, Corner radius, and Gap are applicable in both Loading and Carousel. Images shown, Speed, Border, Shadow, hover/drag/download behavior remain Carousel-only, while Trail controls remain Trail-only.
- Protocol and lifecycle: Fine Details sender and receiver use protocol v8 and carry `imagesMode: "loading"` in the existing settings payload. Toolcraft selection is direct and has no timer. The standalone homepage mock immediately enters Loading on a valid prompt, clears and replaces any pending timeout on a repeated submit, and enters Carousel 2600ms after the latest submit. The 2600ms timer is only a mock boundary for a future generation-complete callback and is cleared on unmount.
- Integration diagnosis: the first real browser implementation run proved Toolcraft state and protocol delivery reached `[data-fine-details-loading]`, but the non-transition Toolcraft website route lacked the required `data-fine-details-image-state="loading"` wrapper marker. The minimal website fix publishes that state on the existing Fine Details section only when the animated homepage transition wrapper is not used; it changes no layout, timer, renderer, or pointer behavior.
- Alternatives rejected: reintroducing Height/`carousel.height`; linking placeholder count to Images shown; adding a Loading timer or shimmer-speed control to Toolcraft; applying Carousel border/shadow/interactions to placeholders; duplicating typography-band measurement; or weakening reduced-motion behavior.
- State/output mapping: `images.mode=loading` maps to the iframe state marker, one pointer-transparent loading root, and three pointer-transparent cards. `carousel.textGap` changes each square's measured size; `carousel.radius` changes every computed corner radius; `carousel.gap` changes both adjacent layout gaps. Normal motion maps to a non-`none` running animation with a `100% 200%` vertical gradient and downward effective Y travel; reduced motion maps to `animation-name: none`, no background image, and a non-transparent solid background. Switching to Carousel restores the authored carousel; switching to Trail restores the trail and removes Carousel output.
- Performance intent: ordinary later functional work. The fixed three-node DOM output adds no workload dimension, render-pass change, kernel benchmark, targeted performance request, or measured-performance authority.
- Verification: TDD registration RED ran the exact Playwright title and failed at the intentional missing-assertions marker. Final exact browser command for the Loading acceptance plus standalone lifecycle passed 2/2; the Loading test attaches real `product-observable-change` and `segmented-control-layout` evidence and checks three equal squares, live Text gap drag, live radius/gap, no border/shadow, pointer behavior, prompt drag disabled, animation/keyframes, reduced motion, and reversible Carousel/Trail states. The standalone case uses Playwright's fake clock to cross the superseded deadline and resolve the replacement deadline at 2600ms. Before a second pending timeout, it records the exact 2600ms timer ID, triggers a same-realm Next client navigation that unmounts the homepage mock, and proves its cleanup passes that ID to `clearTimeout` before advancing past the deadline; the destination remains Trail with no page error or late state mutation. Website focused loading/mock/settings tests passed 19/19; scoped website formatting passed; Toolcraft typecheck passed. Thirty-two focused Toolcraft values, sections, applicability, protocol, prompt bridge, interaction, and readiness assertions passed; the only 33rd assertion was the known aggregate product-validator failure below. Focused worklog rule tests passed 77/77 outside the existing aggregate product-worklog assertion, `pnpm docs:check` passed, and repository-root `git diff --check` passed.
- Known blockers: the existing aggregate `fine-details-preview.product.test.ts` assertion still returns the same 39 unrelated acceptance-validator errors. The existing aggregate product-worklog assertion still returns 42 structural errors for the historical multi-entry worklog format. `pnpm test:feature -- images.mode` builds the finite-selector peer closure but stops before browser startup because the pre-existing peer title `browser: carousel.count changes the embedded Fine Details output` has no registered test. Website typecheck remains blocked by five unrelated existing errors: two missing API Section modules, the Fine Details trail-motion regex target, and two Hero typing errors. No loading-scoped type error was reported.
- Risks: the exact focused browser behavior is covered, but a broad manual visual review on unusual dark backgrounds, aggregate acceptance cleanup, and the parent Tier 2 lint/build gate remain outside this subtask. No commit or push was made.

## Decision Trail: Fine Details prompt flight

- Request: after generation enters Carousel, fly the prompt window to a tunable bottom-left landing point with delayed Windows-style follower ghosts; return to Trail quietly; expose every flight parameter in Toolcraft and advance the live bridge from v8 to v9.
- Task type: later Tier 3 schema, protocol, acceptance, and website-owned transition configuration; Toolcraft publishes settings but does not implement the website animation renderer.
- User-visible result: the new nine-control Prompt Flight section edits the flight gate, landing anchor, start delay, travel time, overshoot, ghost count, follower interval, opacity, and dissolve duration. The existing always-visible Images segmented remains the replay driver.
- Source/reference checked: the approved Fine Details prompt-flight design and implementation plan, the live v8 Toolcraft protocol and website receiver, the website `normalizePromptFlight` implementation, current Prompt/Carousel control-section conventions, the preview pipeline, acceptance inventory, and performance configuration.
- Reference inputs: None; this behavior follows the explicit design and current website implementation rather than a supplied motion asset.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, `core/performance.md`, `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`.
- Contract rules applied: use built-in Switch, Vector, continuous Slider, and discrete Slider controls; keep nine cohesive controls in one section with `semanticGroup` on every control; use conditional applicability instead of disabled controls; keep canonical runtime values and field-by-field normalization; declare panel/global property-edit ownership; preserve the website DOM renderer and existing Images state owner; use later-feature focused checks only.
- View interaction intent: unchanged `fixed-camera`; the flight is flat website DOM output and introduces no spatial model, orbit target, canvas handle, layer, Toolcraft timeline, or artifact export.
- Interaction ownership: the Toolcraft panel owns these nine persistent global property edits. The iframe remains the product output and interaction surface but cannot own duplicate persistent precision settings controls; the website owns only execution of the resulting transition.
- Decision: compose a dedicated website-owned fly offset after the prompt's existing base transform and drag variables; use delayed simplified follower shells; return to Trail without ghosts; and snap to the correct endpoint without animation or ghosts when Active is off or reduced motion is requested.
- Animation intent: keep `appTransferMode.animationIntent` at `{ mode: "none" }`. Prompt flight is a finite event-driven state transition owned by `images.mode`, not app-wide playback, a loop, or transport; the product must not claim autonomous/no-duration-control behavior or add a timeline until the contract supports timed UI transitions.
- Protocol: advance the atomic sender/receiver contract from v8 to v9. `settings.prompt.flight` is normalized field by field with website-identical clamps, defaults, ghost-count rounding, and per-axis vector fallback; no message type or second state controller was added.
- Alternatives rejected: animating the drag variables because that would couple flight to the drag-reset `transitionend` state machine; stationary fading copies because the requested afterimages must follow and arrive; DOM-cloning the live prompt with backdrop blur because stacked interactive/blurred copies would be heavy and semantically wrong; adding a new replay button or Toolcraft timeline because Images already owns the state transition.
- State/output mapping: `prompt.flight.enabled` selects animated versus snapped transitions; `position` sets the Carousel touchdown anchor; `startDelay` delays departure; `flightTime` sets travel duration; `bounce` sets proportional overshoot; `ghostCount` bounds follower shells to 0–8; `ghostLag` spaces their departures so the window lands first; `ghostOpacity` sets the follower opacity ceiling/base budget, with the nearest follower starting below it and later followers progressively lighter; `ghostFade` sets dissolve time at the landing point. `images.mode=carousel` starts the outward flight and `images.mode=trail` starts the ghost-free return.
- Performance intent: ordinary later feature work. The website transition is one-shot, creates at most eight blur-free shells, unmounts them after fade, and has zero steady-state cost; no workload dimension, kernel benchmark, measured iteration, or performance authority was introduced.
- Verification: new prompt-flight values and Toolcraft product tests pass 19/19; focused v9 preview payload proof passes 1/1; existing prompt-bridge and carousel Toolcraft tests pass 9/9; pointed section-size, control-order, applicability, and interaction-ownership checks pass 24/24; `pnpm typecheck` passes; `pnpm exec oxfmt --write` completed for the 12 touched TypeScript files; `git diff --check` passes. The targeted aggregate product assertion was also run and retains exactly its prior 39 unrelated acceptance-validator errors, with no prompt-flight error added. Browser, build, delivery, export, kernel, broad-suite, manual, and measured-performance checks were intentionally not run per the requested focused scope.
- Risks: the flight implementation must keep its animation isolated from drag-reset `transitionend`; mid-flight Images changes must cancel and restart from the current offset; v8 preview tabs cannot acknowledge v9 messages until reloaded; exact landing geometry, ghost arrival order, reduced-motion snap, and post-landing drag remain for website/manual verification.

## Decision Trail: Fine Details prompt typing

- Request: add a Toolcraft Prompt Typing section for Trail with an editable ordered phrase cycle, typing/deletion timing and humanization, Backspace or Instant deletion, focus/input and image-mode gates, and reduced-motion behavior; integrate it through the real website iframe and cover the behavior with one focused product browser scenario.
- Task type: later Tier 2 schema, autonomous website animation, protocol, interaction-state, and focused real-iframe integration work.
- User-visible result: when Prompt Typing is enabled in Trail and the real prompt is empty and unfocused, a visual ghost types the authored phrases in order, holds them, deletes them according to the selected style, and waits for the configured gap; focus, entered input, another image mode, or switching the feature off removes the ghost, while reduced motion shows the first nonempty phrase statically without a caret.
- Source/reference checked: the existing Fine Details Toolcraft schema and preview bridge, the website `AiPromptInput`, Fine Details prompt wrapper/settings boundary, the pure typing state engine and hook, the current prompt-drag and preview product Playwright conventions, and the browser control-target helpers.
- Reference inputs: None; the behavior comes from the explicit prompt-typing request and current product contracts rather than a video, GIF, screen recording, contact sheet, extracted frames, or external design reference.
- Docs/contracts read: `recraft-tools/fine-details/AGENTS.md`, `docs/toolcraft/workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/timeline-animation.md`, `core/performance.md`, `schema-reference.md`, `component-rules.md`, `decision-contract.md`, `acceptance-testing.md`, and `performance.md`.
- Contract rules applied: use the built-in Switch, `collectionActions` Text items, continuous Sliders, and Segmented controls; keep user-authored configuration in canonical Toolcraft state; classify the self-running hint as autonomous decoration without Timeline transport; preserve product output in the website iframe; exercise real controls and the real iframe textarea without direct runtime/value mutation; keep later work to one focused browser check and run no measured performance.
- View interaction intent: unchanged `fixed-camera` for the existing flat, non-spatial website composition; prompt typing adds a visual DOM overlay but no spatial model, orbit, camera path, orientation target, canvas handle, layer, or artifact export.
- Interaction ownership: the panel owns enabled state, phrase collection cardinality/content, rates, hold/gap, humanization, and deletion style. The website owns only the transient ghost animation, focus/input observation, caret presentation, and animation-frame lifecycle. The overlay is `aria-hidden`, pointer-transparent visual text and never writes the real textarea value; duplicating phrase editors or transport controls on the canvas/website was rejected. Trail's Toolcraft iframe remains pointer-transparent, so the focused browser proof uses `locator.focus()` on the real iframe textarea rather than changing pointer ownership or manufacturing a forced pointer hit.
- Decision: carry the complete typing object through Fine Details protocol v9 and evaluate it in a pure `advancePromptTyping` state engine whose random source is injected. The website hook supplies `Math.random`, owns requestAnimationFrame creation/cancellation, and gates output on enabled Trail state, at least one nonempty phrase, an empty unfocused input, image-mode changes, settings changes, and reduced-motion preference. Reduced motion bypasses the engine and caret entirely.
- Alternatives rejected: writing animated text into the textarea value; using placeholder mutation; adding a custom Toolcraft control or panel transport; using Toolcraft Timeline for an idle hint with no play/pause, scrub, duration, loop, restart, or export-at-time behavior; duplicating the animation in Toolcraft; direct browser runtime mutation; enabling Trail iframe pointer events; deterministic production randomness; and asserting a precise random character trajectory.
- State/output mapping: `prompt.typing.enabled` gates the ghost; `prompt.typing.phrases` supplies the ordered nonempty phrase set; `typeSpeed`, `hold`, `gap`, and `humanize` shape typing cadence; `deleteSpeed` applies only to Backspace; `deleteStyle=instant` maps a completed phrase directly to empty after Hold before Gap. `images.mode=trail`, empty input, and blur permit output; focus, nonempty input, Carousel/Loading, or disabled state remove it. `prefers-reduced-motion: reduce` maps to the first nonempty phrase with no caret or time-varying text. Protocol v9 synchronizes the same canonical values on Toolcraft and website sides.
- Performance intent: ordinary later functional work. The engine is one bounded pure state advance, the hook owns one cancelable animation frame only while eligible, and no workload dimension, renderer-pass change, benchmark requirement, performance complaint, targeted iteration, or measured-performance authority was introduced.
- Verification: later-feature focused verification ran only the exact product Playwright case `browser: prompt typing animates phrases until focus`; the final corrected run passed 1/1 in 18.7s. The case uses real Toolcraft controls and collection actions, the real website iframe, bounded observations for phrase changes and Instant clearing, real textarea focus/keyboard blur, reversible image-mode gates, reduced-motion stability, and the final off gate. Scoped diff checking passed. No full browser matrix, aggregate tests, lint, build, delivery, export, kernel, or measured-performance command was run for this batch.
- Risks: Trail deliberately keeps the iframe pointer-transparent to preserve Toolcraft canvas and trail ownership, so a physical pointer click cannot reach its textarea from the host; the focused test proves the same real textarea focus gate without changing runtime/value state and records that boundary rather than weakening existing pointer proofs. Bounded polling proves visible semantic transitions but does not claim an exact random cadence, frame-by-frame timing, broad visual fidelity, or measured performance. The website must be available at `localhost:3000` for the iframe scenario; no commit or push is part of this task.

## Decision Trail: repair the local Fine Details Toolcraft preview route

- Request: run the Toolcraft app for the second section and replace the 404 shown inside its canvas with the actual Fine Details section.
- Task type: later Tier 2 localized preview-route regression repair.
- User-visible result: `http://127.0.0.1:3001/` remains the Fine Details Toolcraft shell and its embedded product canvas now renders the live Fine Details section instead of the website 404 page.
- Source/reference checked: the reproduced Toolcraft iframe DOM, the running Next.js routes on port 3000, `FINE_DETAILS_PREVIEW_URL`, and the website `BASE_PATH` constant.
- Reference inputs: the user's 404 screenshot; it documents the regression state but supplies no product behavior instructions.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, `decision-contract.md`, `core/runtime-boundary.md`, `component-rules.md`, `renderer-technique.md`, `acceptance-testing.md`, `core/performance.md`, and `performance.md`.
- Contract rules applied: preserve the signed Toolcraft host and existing iframe product surface; repair the product-owned preview endpoint only; use later-feature focused verification without aggregate or measured-performance work.
- View interaction intent: unchanged `fixed-camera`; the repair changes only the local receiver path and introduces no spatial behavior.
- Interaction ownership: unchanged; Toolcraft controls remain panel-owned and the website iframe remains the product output/interaction surface according to image mode.
- Decision: align the preview receiver URL with the website's canonical `/v4styles` base path and keep the protocol origin, version, settings, controls, and iframe sandbox unchanged.
- Alternatives rejected: adding a duplicate website redirect solely for Toolcraft, changing the Next.js base path, proxying port 3000, or replacing the iframe with duplicated product markup.
- State/output mapping: existing Toolcraft state continues through protocol v11 to `/v4styles/toolcraft/fine-details`; only the receiver pathname changes.
- Performance intent: ordinary bug repair; no workload dimension, renderer pass, benchmark, targeted iteration, or measured-performance authority was introduced.
- Verification: the focused URL contract passed 1/1. A local browser opened `http://127.0.0.1:3001/`, observed the Toolcraft Controls panel and the embedded `Fine details` region, and observed no 404. The known unrelated 41-error aggregate acceptance baseline remains unchanged. No build, lint, broad suite, browser matrix, delivery verification, export matrix, kernel benchmark, or measured-performance check was run.
- Risks: the embedded preview still depends on the local Next.js server remaining available on port 3000 with base path `/v4styles`.

## Decision Trail: align Prompt Flight with the fixed-step protocol v12 model

- Request: complete the approved Prompt Flight plan on the Toolcraft side after the website moved to protocol v12, replace delayed follower tuning with fixed-step onion-skin tuning, and preserve the later Run/Reset actions plus the repaired `/v4styles/toolcraft/fine-details` preview route.
- Task type: later Tier 3 schema, protocol, acceptance, renderer-pipeline metadata, and dormant browser-evidence alignment for an existing website-owned transition.
- User-visible result: one ten-control Prompt Flight section exposes Active plus all nine gated persistent settings, followed by a one-control Playback section for Run/Reset. The iframe sends the exact v12 flight shape to the repaired `http://localhost:3000/v4styles/toolcraft/fine-details` route.
- Source/reference checked: the approved Prompt Flight design and both implementation plans, the website's live v12 receiver and `FineDetailsPromptFlightSettings` normalizer, the existing Toolcraft value/control/pipeline/acceptance/performance modules, and the prior route-repair diff.
- Reference inputs: the fixed-step onion-skin behavior is already approved in `docs/superpowers/specs/2026-08-25-fine-details-prompt-flight-design.md`; no new video, GIF, screen recording, or motion asset was introduced in this Toolcraft-only pass.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, `core/performance.md`, `core/timeline-animation.md`, `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, `performance.md`, and `decision-contract.md`.
- Contract rules applied: keep canonical schema values and field-by-field normalization; use built-in Switch, Vector, continuous Slider, discrete Slider, and Actions controls; declare semantic groups and conditional applicability; keep precise settings panel-owned/global; preserve the website DOM renderer, current Images driver, one-shot command bridge, and later-feature focused verification posture.
- View interaction intent: unchanged `fixed-camera`; this remains flat website DOM output with no spatial model, orbit target, canvas handle, Toolcraft timeline, layer, or artifact export.
- Interaction ownership: the Toolcraft panel owns the ten persistent global flight properties in the complete `fine-details-prompt-flight` entity. The adjacent `fine-details-prompt-flight-playback` local-command entity and its separate panel `command` owner/acceptance row cover both one-shot Run/Reset values; the pointer-transparent iframe executes and displays the transition without duplicating settings or command controls, and neither action mutates settings.
- Decision: emit only `ghostStep`, `ghostOpacity`, `ghostFalloff`, and `ghostSettle` in the v12 flight payload. The stack uses a bounded count of zero through ten fixed-step, blur-free onion-skin shells derived from one live path distance; after landing, each emerged copy collapses into the prompt and dissolves over its per-copy settle duration. Run replays current settings and Reset cancels motion/ghosts without changing settings. The Toolcraft sender rejects both commands until the iframe has emitted protocol readiness, so a loaded-but-unready frame cannot report false success.
- Alternatives rejected: retaining `ghostLag`/`ghostFade` aliases in emitted or persisted state; delayed per-ghost path replay; stationary dropped copies; adding a second playback state owner; removing Run/Reset; reverting the repaired preview pathname; or patching the signed runtime.
- State/output mapping: `enabled` selects animated versus snapped endpoints; the concurrent website-and-Toolcraft Landing default `{ x: -1, y: 1 }` remains canonical and `position` selects the Carousel landing anchor; `startDelay`, `flightTime`, and `bounce` tune departure, travel, and overshoot; `ghostCount` bounds shells to 0–10; `ghostStep` sets 4–80px along-path spacing; `ghostOpacity` sets the 5–100% opacity base; `ghostFalloff` applies 0–60% per-copy opacity decay; `ghostSettle` sets each emerged copy's 100–1500ms post-landing collapse/dissolve duration. Protocol v12 carries these fields through the existing atomic settings bridge at `http://localhost:3000/v4styles/toolcraft/fine-details`; the separate `promptFlight.commands` control target is transient and is never emitted or persisted as a setting.
- Performance intent: ordinary later functional work. The transition remains one-shot with at most ten blur-free shells derived from one motion value and zero steady-state work after settle; no workload dimension, renderer pass, kernel benchmark, performance complaint, targeted iteration, measured evidence, or full-audit authority was introduced.
- Verification: initial TDD RED was captured with 23 focused failures and 24 passes before implementation, exposing v11 plus the legacy fields/defaults/domains. Review-correction RED then captured four focused failures for settle wording and action inventory/ownership/acceptance. Final-layout RED captured eight failures and 28 passes for the exact 10+1 sections, localhost route, readiness guard, inventory, and worklog. The final quality-review RED captured the missing canonical command-contract module before the production helper was added. Focused GREEN passed 46/46 values, command-contract, readiness-sender, panel-action, control, bridge, carousel, and typing tests plus 2/2 narrow protocol/product tests; the sender's 4/4 executable cases prove rejection without readiness or a content window and exact v12 Run/Reset posting only when ready. Playwright discovery lists exactly one test named `browser: Prompt Flight Run replays current settings and Reset cancels transient motion`; the registered scenario drives real Run/Reset controls, observes website flight state, and compares settings plus local persistence before/after, but that full case was not executed. A later manual smoke on the running Toolcraft `http://127.0.0.1:3001/` and website receiver sampled six live flight shells at 24px spacing with opacities `0.55, 0.429, 0.33462, 0.261004, 0.203583, 0.158795`; settle faded nearest-first with the 40ms stagger, Run replayed in Carousel, the settled layer unmounted, and Trail returned through `returning → idle` with zero flight shells. The two Prompt Flight Playback gaps and all interim cross-section flight diagnostics are gone; with generic validators unchanged as required, the current 39-error aggregate acceptance baseline contains only pre-existing unrelated diagnostics. The focused structural performance validator still has its single unrelated existing `trail-card-count` workload-role baseline from the earlier pass; performance was not rerun for this final correction. Targeted formatting restored the protocol, carousel, typing, and bridge files to semantic-only diffs; scoped lint and diff checks cover only this task's files and preserve concurrent unrelated changes. Final source scans keep the legacy names only in the negative no-migration unit test and find no v11 production/test references. No build, full lint, full suite, browser matrix, delivery, export, kernel, or measured-performance check was run.
- Risks: stale preview tabs must reload for protocol v12. Reduced-motion emulation, the full settings round-trip, and the standalone homepage generation flow remain outside this focused pass. The remaining 39 aggregate acceptance diagnostics and the previously observed performance-validator baseline are unrelated to Prompt Flight.

## Decision Trail: Prompt flight breadcrumbs & corner landing

- Request: replace the previous moving onion-skin stack with stationary copies dropped behind the prompt during every movement, make them vanish first-dropped-first after arrival, and land the prompt in the extreme bottom-left corner of the typography rectangle. The supplied screenshot marks the upper-left typography's exact left edge and the lower-right typography's exact bottom edge with red guides; those two guides define the touchdown corner.
- Task type: later focused Toolcraft values, schema sections, protocol, renderer-pipeline metadata, acceptance inventory, dormant browser-case, and worklog migration for the already implemented website behavior.
- User-visible result: `Prompt Flight` now has Active, Offset X/Y, Start delay, Flight time, and Bounce; a separate `Prompt Ghosts` entity has Active, Spacing, Opacity, Falloff, Vanish stagger, and Vanish time. Playback Run/Reset remains unchanged. Offset 0/0 aligns the prompt's left edge to the upper-left typography's left edge and its bottom edge to the lower-right typography's bottom edge; the two pixel sliders fine-tune that deterministic anchor.
- Source/reference checked: the approved breadcrumbs/corner-landing design and implementation plan, the live website v13 settings normalizer and receiver, the existing dirty v12 Toolcraft values, sections, preview pipeline, acceptance inventory, performance record, browser cases/evidence, and protected public browser-evidence helpers.
- Reference inputs: the user's red-guide screenshot describes the exact typography-corner anchor; no video, GIF, screen recording, contact sheet, extracted frames, or motion reference was supplied.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, `core/performance.md`, `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, `performance.md`, and `acceptance-testing.md`.
- Contract rules applied: keep canonical key-by-key values; use built-in Switch and continuous Slider controls; split the flight and breadcrumb visual entities into two six-control sections; gate flight properties by flight Active and breadcrumb strengths by both Active switches; keep roles ordered mode → spatial → strength; preserve panel/global property-edit ownership, website DOM output, fixed-camera view intent, existing Images replay owner, and protected app-owned evidence helpers.
- View interaction intent: unchanged `fixed-camera`, sourced from the inspected flat website reference; breadcrumbs and the corner landing add no spatial model, orbit target, canvas handle, Toolcraft timeline, layer, or artifact export.
- Decision: the earlier rejection of stationary copies and its fixed-step onion-skin fan are explicitly superseded by the user's later direction. Breadcrumbs are stationary by construction, drop on outbound and return flights, and vanish in place from the takeoff end after arrival. No compatibility switch retains the previous visual model.
- Alternatives rejected: preserving the old pad/count/step/settle fields as aliases; retaining the moving fan behind a mode switch; exposing derived count as workload tuning; duplicating replay or breadcrumb controls in the iframe; modifying signed runtime/evidence files; or changing Apply behavior.
- State/output mapping: Protocol v13 carries `enabled`, scalar Toolcraft targets for `offset.x`/`offset.y` composed into `offset`, `startDelay`, `flightTime`, `bounce`, `ghosts`, `ghostSpacing`, `ghostOpacity`, `ghostFalloff`, `vanishStagger`, and `vanishTime`. Removed persisted `position`, `ghostCount`, `ghostStep`, and `ghostSettle` targets are ignored by key-by-key reads. Breadcrumb count is derived from path length and spacing, capped at 32; tiny requested spacing on a long path therefore widens to honor the cap instead of creating a new workload dimension.
- Interaction ownership: all twelve persistent properties remain precise panel-owned global edits. The iframe executes and displays the transition without duplicating persistent controls. Images and Playback remain complementary replay/command owners; Run starts the outbound breadcrumb flight and Reset intentionally starts the return-to-base breadcrumb flight without mutating settings.
- Performance intent: ordinary later functional work. Every new control remains `responsiveness`; the one-shot DOM transition derives at most 32 blur-free copies and has no user-controlled workload dimension or steady-state cost after vanish completion. No performance complaint, targeted iteration, kernel benchmark, measured evidence, or full-audit authority exists for this batch.
- Verification: focused TDD RED was captured in `fine-details-prompt-flight-values.test.ts` with 4/4 expected failures against the v12 shape. Final exact values, Toolcraft, and command-contract GREEN passed 30/30; directly affected prompt bridge, panel action, carousel protocol, and typing protocol files passed 18/18; the narrow preview v13 payload assertion passed 1/1. A follow-up browser-evidence quality review recorded a focused 2/2 RED before adding geometry helpers, then passed 24/24 helper and Toolcraft assertions. The dormant cases now independently measure the exact two typography anchors, pair stationary ghost rectangles with meaningfully moving prompt samples while both remain in `flying`, determine first/last drops by path projection instead of DOM index, and bracket a selected 700ms fade from observed onset at 600–800ms with bounded polling. Playwright discovery only listed all 14 Prompt Flight product cases, including twelve persistent controls, the direction-agnostic transition, and Run/Reset; no case executed. `pnpm docs:check` and `git diff --check` passed. No local `oxfmt` executable exists, so no formatter was run or installed. The aggregate preview-product assertion was inspected once and remains outside the focused gate: its existing 39 unrelated diagnostics are joined by seven generic dependency-cohesion diagnostics caused by the explicitly required separate `fine-details-prompt-ghost-trail` entity depending on Prompt Flight Active; protected validators were not changed and the required split was not weakened. No broad test/build/typecheck/AI check, browser execution, delivery, export, kernel, manual, or measured-performance run occurred.
- Risks: a stale preview tab remains on the old handshake until reload; very small Spacing on a long path widens effective spacing because the derived copy count is capped at 32; Reset now intentionally produces return breadcrumbs and users can switch them off with Prompt Ghosts Active.

## Decision Trail: Loading Wave generation-state animation

- Request: replace the plain vertical shimmer on the generation-state placeholder cards with a user-chosen «волна проявления» — a static checker grid whose cells flip as a soft diagonal band sweeps across each card, fully tunable from Toolcraft (cell, contrast, band width/softness/angle, pass time, pause, per-card stagger).
- Task type: later focused feature across the website loading placeholders and this workspace's values, schema section, protocol, pipeline metadata, acceptance records, and worklog.
- User-visible result: a new nine-control `Loading Wave` section (Active switch plus eight sliders in pattern/wave/rhythm groups), visible while Images = Loading; Active off restores the legacy shimmer exactly. Website renders the wave as two CSS checker layers (`repeating-conic-gradient`, tone B via `color-mix` contrast) with the inverted layer revealed through an animated `mask-position` band; geometry (mask gradient stops, tile size, travel vector, duration = pass + pause) is computed in `fine-details-loading-wave.ts` from settings and card size, so the pause is constant-speed off-card travel and any 0–360° direction works without degenerate sweeps. Cards get `animation-delay = index × stagger`; reduced motion hides the band and keeps the static checker.
- Source/reference checked: the live v13 website receiver and loading placeholders, carousel/trail values and sections as templates, the acceptance validator baseline, and the family worklog decisions above.
- Reference inputs: the user's two loading-state screenshots select the concept; no video, GIF, screen recording, contact sheet, extracted frames, or motion reference was supplied — `referenceInputs` stays as declared.
- Contract rules applied: canonical key-by-key values on both sides; built-in Switch and continuous Sliders; one nine-control section with semantic groups and mode → strength order; conditional applicability on Images = Loading matching the Trail/Carousel-family gating precedent; panel-owned global property edits; DOM renderer and fixed-camera intent unchanged.
- Decision: protocol v13 → v14 on both sides (all version pins swept, runtimeId `fine-details-external-preview-v14`); new top-level `loading` settings group with defaults cell 20 / contrast 14 / waveWidth 45 / softness 60 / angle 45 / passTime 1600 / pause 300 / stagger 180; the legacy shimmer remains the switch-off state rather than being deleted.
- Alternatives rejected: marching-checker background-position scroll, marching-ants selection frame, and scanline printer (offered; the user chose the reveal wave); per-cell DOM grids (hundreds of nodes) and canvas re-render (duplicates the CSS renderer); baking the pause into keyframe offsets (impossible with custom-property keyframes — encoded as extra travel instead).
- Interaction ownership: nine new panel-owned global property-edit entries mirror the prompt-flight pattern; the iframe stays the product output surface with no duplicated controls.
- Performance intent: ordinary later functional work; every control is `responsiveness` — the effect is a compositor-driven CSS mask animation on the loading cards whose per-frame cost is constant for every slider value; no workload dimension, kernel, or measured iteration.
- Concurrency note: implemented while another session was reshaping the loading composition on the website (three placeholders → two, tablet paddings). Adopted its state: `FINE_DETAILS_LOADING_CARD_COUNT = 2`, and the imagesMode browser case was renamed to "browser: loading state shows two placeholder cards" with the count assertion updated; its tablet media query in the loading CSS module was preserved verbatim.
- Verification: focused GREEN — website `fine-details-loading.test.ts` 9/9 (wave geometry math: symmetric travel, pause ratio, horizontal-angle case, ordered gradient stops at softness extremes; source assertions for the checker/mask/reduced-motion CSS and class swap), `fine-details-settings.test.ts` 27/28 with the pre-existing missing-public-WebP stat failure only, boundary and flight-runtime version pins green; workspace `fine-details-loading-values.test.ts` 2/2, `fine-details-loading-toolcraft.test.ts` 15/15, plus carousel/flight/typing/values/command/panel/bridge/product suites 65/65 after updating exact-shape pins (section title list, defaults payload, drag-target list relaxed to containment, v14 strings). Meta suites control-order, control-labels, control-naming-rules, control-applicability, and interaction-ownership all green. `tsc --noEmit` on this workspace reports only the six pre-existing e2e/panel-actions-test errors and none in the new files; a cross-repo `tsc` sweep of the website likewise shows no errors in the new loading files. The aggregate acceptance-coverage validator remains outside the focused gate per the standing family posture: its pre-existing 46 diagnostics grow to 56 with nine images.mode dependency-cohesion entries (same class as every Trail/Carousel-conditional control) and one `loading.pause` transport-word heuristic (same class as the standing `trail.resumeDelay`/`resumeRamp` entries); protected validators were not modified. Browser cases were not executed in this pass (no running dev servers from the automation VM); native linux-arm64 npm binaries (esbuild, rolldown, lightningcss, tailwind oxide) were added inside `node_modules` only, to make vitest/tsx runnable from the automation VM — they are not referenced by any lockfile.
- Risks: stale preview tabs must reload for the v14 handshake; the darwin-installed `node_modules` now also carry linux platform folders (inert on macOS, removable by any reinstall); the loading browser case count assertion now expects two cards and will fail if the concurrent composition work changes the count again.

## Decision Trail: Loading Wave gloss, base tone, and band distortion

- Request: the wave blended into the background; the user asked for a shinier look — «за счёт оверлея и белого фона например», offered latitude for an own solution, and asked whether the wave can distort the grid beneath itself.
- Task type: later focused revision of the loading-wave surface model across the website renderer and this workspace's values, split schema sections, protocol, pipeline metadata, acceptance records, and worklog.
- User-visible result: the checker palette is rebuilt — `Base tone` lightens the card surface from the base gray toward white (default 85 ≈ near-white cards), `Contrast` now darkens the cells against that surface instead of lightening them, `Glare` whitens the flipped cells inside the band into a glossy sheen (default 55), and `Distortion` shifts the checker under the band by up to 24 px along the travel axis so the wave visibly refracts the pattern (default 6). Twelve controls split into two adjacent workflow stages: `Loading Wave` (surface: Active, Cell size, Contrast, Base tone, Glare, Distortion) and `Wave Motion` (Wave width, Softness, Angle, Pass time, Pause, Stagger).
- Decision: gloss is composed entirely in CSS custom-property color math — `--fd-wave-base` = mix(gray → white, baseTone), `--fd-wave-tone` = mix(base → black, contrast), flip tones additionally mixed toward white by glare — and the distortion is a `background-position` offset of the overlay checker (vector = distort × travel axis, computed in `fine-details-loading-wave.ts`), so the effect stays a compositor-driven mask animation with constant per-frame cost. Protocol v14 → v15 on both sides; runtimeId `fine-details-external-preview-v15`; all version pins swept.
- Alternatives rejected: a separate white overlay layer above the checker (mutes the cell flip instead of glossing it); SVG `feDisplacementMap` refraction (real displacement but paint-stage cost and a second rendering mechanism for one band); `backdrop-filter` bands (cannot displace the pattern and cost compositor memory per card).
- Interaction ownership and records: the nine panel-owned global property edits grow to twelve with the same panel/global posture; the single inventory entry splits into `surface` and `motion` stages with a shared `splitReason`; three new acceptance rows (base tone, glare, distortion) and a reworded darker-cells contrast observable join the family-standard generic rows.
- Performance intent: unchanged `responsiveness` for every control — the added work is color-mix declarations and one static background offset; no workload dimension, kernel, or measured iteration.
- Verification: focused GREEN — workspace `fine-details-loading-values.test.ts` (clamps for the three new fields), rewritten `fine-details-loading-toolcraft.test.ts` 18/18 (two-section shape, gating, domains, v15 payload, pipeline registration, split inventory, twelve owners/rows, twelve mapping cases), carousel/flight/typing/values/command/panel/bridge/product suites 75/76, meta suites control-order/labels/naming/applicability/interaction-ownership 34/34; website `fine-details-loading.test.ts` (shift-vector math incl. horizontal-angle and magnitude cases, flip-var and background-position source assertions) and `fine-details-settings.test.ts` green except the pre-existing missing-public-WebP stat failure; cross-repo `tsc` sweeps show no errors in the touched files. One sweep collateral was caught and reverted: the v14→v15 pin sweep briefly rewrote the trail `cardRadius` default assertion (`toBe(14)`), restored immediately. The aggregate acceptance-coverage validator remains outside the focused gate per the standing family posture: the baseline grows 56 → 59 with the three new sliders' images.mode dependency-cohesion entries of the accepted class; protected validators were not modified. Browser cases were not executed in this pass.
- Risks: stale preview tabs must reload for the v15 handshake; applied payloads created before this revision carry no `loading` group, so the new near-white default surface appears immediately everywhere until the user tunes and applies; `Contrast` changed direction (darker cells), so any previously noted contrast value produces a visually different (inverted) result.

## Decision Trail: Loading placeholder card chrome (carousel shadow + light outline)

- Request: give the loading placeholders the same shadow as the post-generation carousel cards, plus a light outline «типо black опасити 12».
- Task type: later focused addition to the loading-state card chrome across the website renderer and this workspace's values, surface section, protocol, pipeline metadata, acceptance records, and worklog.
- User-visible result: the placeholders now cast exactly the carousel cards' shadow — the component consumes the same `carousel.shadow` settings through the identical formula (`offset × 48 px`, blur, spread, `rgb(r g b / o%)` color; `enabled: false` → none), so tuning Carousel Shadow (currently applied: blur 15, #000 35 %, offset y 0.13, spread −8) restyles both. Each card also gets its own outline from the new `loading.border` group — default `#000000` at 12 % opacity, 1 px — via two new `Loading Wave` controls (`Border width` 0–8 px step 0.5, zero removes it; `Border color` colorOpacity). The chrome applies with the wave on or off, so both controls are gated on Loading mode only.
- Decision: reuse `carousel.shadow` verbatim instead of cloning a second shadow group — «такая же тень» stays true by construction and adds no controls; the carousel component's pinned source (its test asserts the formula) is left untouched, the placeholder duplicates the five-line color helper and formula, and a website source test pins both files to the same `shadowOffsetPixels = 48` scale so they cannot drift silently. Protocol v15 → v16 on both sides; runtimeId `fine-details-external-preview-v16`; all pins swept (collateral scan clean this time).
- Alternatives rejected: a separate `loading.shadow` settings group (drifts from the cards and duplicates five controls); exporting the carousel's private helpers (couples a pinned file); a border switch control (width 0 already disables; keeps the section at eight controls).
- Records: fourteen panel-owned global property edits (ownership map auto-extends, wording updated); the surface inventory stage gains both border targets; two new acceptance rows (slider + colorOpacity with hex/opacity part coverage).
- Performance intent: unchanged `responsiveness` — inline border/box-shadow styles on two cards; no workload dimension.
- Verification: focused GREEN — workspace loading values/contract tests updated and passing (border clamps, eight-key surface section with the colorOpacity control's undefined orderRole mirrored from the carousel precedent, mode-only gating for both border controls, fourteen owners, fourteen mapping cases), carousel/flight/panel/bridge/command/product suites 66/67, meta suites incl. color-row-grouping 37/37; website loading/settings/boundary/runtime/carousel tests 50/51 (the one failure is the pre-existing missing-public-WebP stat); cross-repo `tsc` sweeps show no errors in the touched files (the six workspace errors are the known pre-existing e2e/panel-actions baselines). The aggregate acceptance-coverage validator baseline grows 59 → 61 with the two border controls' images.mode dependency-cohesion entries of the accepted class. Browser cases were not executed in this pass.
- Risks: stale preview tabs must reload for the v16 handshake; the placeholder shadow follows every future Carousel Shadow tuning by design — if the user later wants them decoupled, that is a new settings group, not a tweak.

## Decision Trail: Loading Wave per-card cycle desync

- Request: «сделай волны в рассинхрон» — the card waves should not tick in lockstep.
- Decision: a new `Desync` control (`loading.desync`, % 0–50, default 12, Wave Motion section) lengthens each next card's full cycle: card *i* runs `duration × (1 + i × desync/100)`, applied as a per-card `--fd-wave-duration` override computed in the component — no CSS changes. Unequal periods make the phases continuously drift apart and re-converge (true desynchronization), unlike `Stagger`, which only sets a constant initial offset; the two compose. Zero restores identical cycles. Protocol v16 → v17 on both sides; runtimeId `fine-details-external-preview-v17`; pins swept, collateral scan clean.
- Alternatives rejected: randomized per-mount phase seeds (not reproducible for tuning and forbidden-by-convention nondeterminism); per-card explicit duration controls (control bloat for two cards); animating `animation-delay` (restarts the animation on every tuning change without changing relative speed).
- Records: fifteen panel-owned global property edits; the motion inventory stage gains the target; one new acceptance row; performance posture unchanged (`responsiveness`, same compositor animation, no workload dimension).
- Verification: focused GREEN — loading values/contract suites updated (seven-key motion section, fifteen owners, fifteen mapping cases, desync domain), carousel/flight/panel/bridge/command/product plus order/labels/applicability/ownership metas 95/96; the single failure remains the standing-red aggregate coverage validator, whose accepted-class baseline grows 61 → 62 with the desync slider's images.mode cohesion entry. Website loading/settings/boundary/runtime/carousel tests 50/51 (pre-existing WebP stat failure only); the loading source test pins the per-card duration formula. Browser cases not executed in this pass.
- Risks: stale preview tabs must reload for the v17 handshake; with nonzero desync the cards' pauses no longer align by design — synchronized rhythm is one slider away at zero.
