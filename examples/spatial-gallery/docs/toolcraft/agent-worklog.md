# Implementation Worklog

## Status

Mode: product

## Decision Trail

### Iteration 16 — Portable Gallery App Deployment

- Request: Задеплоить Gallery App по аналогии с остальными приложениями в Toolcraft.
- Task type: Tier 4 — portable generated-app repository import, canonical base-path adaptation, standalone Vercel project, website proxy, and App Gallery integration; gallery controls, layouts, defaults, renderer, navigation, persistence, and export behavior remain unchanged.
- User-visible result: The existing Image Gallery opens unchanged at local root and at `/demos/spatial-gallery`, loads all fourteen built-in images behind the canonical base path, and can be published as a separate Git-connected Vercel application.
- Source/reference checked: `/Users/kusnizza/Projects/toolcraft-apps/spatial-gallery`; the existing `examples/donut` deployment contract; the website demo registry and App Gallery content contract.
- Reference inputs: The complete current standalone Gallery App folder and its fourteen local JPEG presets.
- Docs/contracts read: `AGENTS.md`, `docs/toolcraft/workflow.md`; Plan route `core/runtime-boundary.md` and `assembly-workflow.md`; Implementation route `decision-contract.md`; `brainstorming`, `writing-plans`, and `vercel-deploy` workflows.
- Contract rules applied: `runtime-shell-required`, `reference-clone-source-of-truth`, `acceptance-product-observable`, `performance-coverage-levels`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; wheel, drag, and keys continue to browse cards without adding camera ownership.
- Interaction ownership: Unchanged; the panel owns media collection editing and precise settings while the canvas owns gallery browsing.
- Decision: Preserve the portable product snapshot, keep the signed TanStack router's dynamic Vite base, resolve only preset public URLs from `import.meta.env.BASE_URL`, and add the standard `/demos/spatial-gallery` Vercel rewrite contract. Host the app in a dedicated `pixelpoint/spatial-gallery` project and proxy it through the website rather than embedding it in Next.js.
- Alternatives rejected: A direct Vercel-only link would bypass the canonical Toolcraft route; embedding the product in `apps/website` would couple its generated runtime to the website; root-absolute preset URLs would break behind `/demos/spatial-gallery`.
- State/output mapping: The existing schema still supplies the same fourteen media defaults in the same order; Vite's base changes only their public URL prefix. All controls and runtime state continue to feed the same WebGL preview and PNG/JPG/code export paths.
- Files changed: `src/app/app-schema.ts`, `src/app/gallery-preset-url.ts`, `src/app/gallery-preset-url.test.ts`, `src/app/app-acceptance-data.ts`, `src/app/app-performance-impact.json`, `src/app/spiral-gallery/spiral-gallery-stack.test.ts`, `scripts/demo-deployment.test.mjs`, `vercel.json`, and this worklog, plus repository-owned website integration files outside this standalone snapshot.
- Performance intent: ordinary-product-work
- Verification: pnpm verify:delivery
- Skipped checks: A separate full performance certification is not selected because this hosting adaptation does not change renderer passes or workload.
- Risks: The standalone and website Git deployments depend on the Vercel project remaining connected to `pixel-point/primeui-v2` with `examples/spatial-gallery` as its root directory.

## Historical Decision Trail

### Iteration 15 — Light Background Default

- Request: Обновить цвет фона на `#EDEDED` и не запускать тяжёлые проверки.
- Task type: Tier 2 — one schema default, its renderer/code-export fallbacks, persistence identity, focused expectation, and visible initial output change; controls, layout, renderer passes, workload, media, timeline, layers, and export algorithms remain unchanged.
- User-visible result: A fresh load and Background Reset use the included light gray `#EDEDED` background while every other Iteration 14 gallery setting remains unchanged.
- Source/reference checked: The current Background schema section, WebGL settings reader, portable code-export fallback, persistence v8 state, focused product contract, and live local app.
- Reference inputs: The explicit user-provided hex value `#EDEDED`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification route `acceptance-testing.md`; Toolcraft `brainstorming` and `writing-plans` workflows.
- Contract rules applied: `controls-product-coverage`, `output-export-required`, `persistence-policy-explicit`, `acceptance-product-observable`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; the background fill does not affect gallery navigation or camera ownership.
- Interaction ownership: Unchanged; the panel Background color control remains the sole precise-value owner and the canvas only previews the resulting fill.
- Decision: Change `appearance.background` default and both defensive background fallbacks to `#EDEDED`, keep Include enabled, and advance persistence to v9 so the visible app does not retain the superseded v8 default.
- Alternatives rejected: A CSS-only fill would bypass preview/export state; changing only the current browser value would not affect Reset or fresh loads; retaining persistence v8 would allow the previous saved color to conceal the new default.
- State/output mapping: The built-in color control writes `appearance.background`; the settings reader feeds that color into live preview, PNG/JPG export, and the serialized code package whenever Include is on.
- Files changed: `src/app/app-schema.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-code-export.ts`, `src/app/spiral-gallery-product.test.ts`, and this worklog.
- Verification: `npm run typecheck` passed; focused `npx vitest run src/app/spiral-gallery-product.test.ts --reporter=default` passed (1 test); the live app's standard Background Reset changed the value to `#EDEDED`, a real reload preserved it, and the browser reported no console errors.
- Skipped checks: The protected delivery gate, aggregate browser acceptance, and all performance suites are intentionally skipped at the user's request because the change does not alter renderer structure, workload, interaction logic, media, or export algorithms.
- Risks: Persistence v9 intentionally supersedes v8 once, resetting previously persisted local settings to the current product defaults.

### Iteration 14 — Imported Settings As Product Defaults

- Request: Сделать настройки из `/Users/kusnizza/Downloads/image-gallery-settings (2).json` дефолтными.
- Task type: Tier 2 — schema defaults, renderer fallback values, persistence identity, and focused product-contract expectations change; controls, sections, renderer passes, media, interactions, export algorithms, timeline, and layers remain unchanged.
- User-visible result: A fresh load and Reset now start from the supplied 1920×1080 Flow preset: render scale 2, radius 2.8, twist 20.4°, vertical gap 0.35, the supplied Flow/Deck/card/depth/physics/interaction/view values, 15% black shadow, included `#171717` background, PNG at 4K, and the existing fourteen-image preset collection.
- Source/reference checked: The supplied settings export, current schema defaults, renderer fallback reader, Deck scroll fallback, portable code-export fallback, persistence policy, and focused product contract test.
- Reference inputs: `/Users/kusnizza/Downloads/image-gallery-settings (2).json`.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan route `core/control-selection.md` and `core/layout.md`; Implementation route `schema-reference.md` and `component-rules.md`; Verification route `acceptance-testing.md`; Toolcraft `brainstorming` and `writing-plans` workflows.
- Contract rules applied: `controls-product-coverage`, `controls-section-inventory-required`, `persistence-policy-explicit`, `acceptance-product-observable`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; the imported view values change the initial framing only and add no camera operation.
- Interaction ownership: Unchanged; canvas owns browsing and the panel owns precise gallery properties. No new or duplicated interaction surface is introduced.
- Decision: Map every applicable exported `values` entry to its existing schema `defaultValue`, preserve the already-matching 1920×1080 canvas and PNG/4K settings, set render scale to 2, mirror all product defaults in defensive renderer fallbacks, set the portable section's missing-background fallback to `#171717`, and advance persistence to v8 so an older saved state cannot conceal the requested defaults. Preserve the fourteen built-in images through runtime `media.defaultAssets`; the exported `source.images: null` does not remove that runtime-owned default collection.
- Alternatives rejected: Importing the JSON only into the current browser would not affect fresh loads or Reset; changing schema without renderer fallbacks would allow partial/portable state to render a different first frame; retaining persistence v7 would let stale local values override the new defaults; enabling a timeline from generic exported timeline metadata would change product structure even though this gallery intentionally has no timeline UI.
- State/output mapping: Schema defaults hydrate runtime values and Reset behavior; `readSpiralGallerySettings` maps the same values into the retained WebGL Flow/Deck renderer and both export paths; code export serializes the resolved state. Persistence v8 makes the defaults visible on the next load.
- Files changed: `src/app/app-schema.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-stack.ts`, `src/app/spiral-gallery/spiral-gallery-code-export.ts`, `src/app/spiral-gallery-product.test.ts`, and this worklog.
- Verification: `npm run typecheck` passed; focused `npx vitest run src/app/spiral-gallery-product.test.ts --reporter=default` passed (1 test); the live app on saved port 3016 was reset once after hot reload and reloaded, then showed the imported Flow, geometry, shadow, depth, physics, interaction, view, background, image-export, canvas-size, and render-scale defaults with no console errors.
- Skipped checks: Full performance certification, aggregate browser performance, and the heavy protected delivery gate are intentionally not selected because the user asked to avoid heavy global checks and no renderer pass, workload boundary, control inventory, media lifecycle, or export algorithm changed.
- Risks: Persistence v8 intentionally supersedes v7 once, so previously customized local values reset to the new preset. The exported timeline metadata is intentionally not applied because the product has no timeline surface.

### Iteration 13 — Dual Raster And Agent-ready Code Export

- Request: «верни экспорт пнг»; clarified by «должен быть экспорт кода и пнг».
- Task type: Tier 3 — sticky delivery actions, the shared export interaction, the agent-ready archive builder, raster backing resize scheduling, and export acceptance/performance coverage change; media defaults, layout behavior, physics, and current Shadow controls remain intact.
- User-visible result: The sticky footer exposes `Export PNG` and `Export Code` side by side. `Export PNG` continues to honor PNG/JPG, 2K/4K/8K, current media order/transforms, Flow/Deck state, current position, card shadows, bends, and optional background. `Export Code` downloads `image-gallery-agent-kit.zip` with an integration guide, current config (including Shadow color/opacity/blur/offset), portable React/CSS/renderer/physics modules, and the current image assets.
- Source/reference checked: The current PNG export contract and Image Export controls, the shared WebGL renderer/export pass, the current Shadow value model, the previous code-export implementation, the supplied settings defaults, and the live app at `http://127.0.0.1:3016/`.
- Reference inputs: `/Users/kusnizza/Downloads/image-gallery-settings.json` remains the source for the current gallery defaults; no new visual reference was introduced.
- Docs/contracts read: `docs/toolcraft/workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, `core/media-upload.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification routes `acceptance-testing.md` and `performance.md`; Toolcraft `brainstorming`, `writing-plans`, `systematic-debugging`, and `browser` workflows.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `output-export-required`, `controls-section-inventory-required`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; both outputs preserve the user-browsed gallery state and add no camera control.
- Interaction ownership: Unchanged; the panel owns both delivery actions while the canvas continues to own browsing.
- Decision: Keep required raster output and add code as an adjacent action rather than replacing either path. Raster still uses `createToolcraftPngExportCanvas` and the retained physical WebGL scene; 4K/8K is composed from projection-correct WebGL tiles capped at 2048 px per axis, so each tile is a true frustum segment of the one full-resolution scene. Code packaging lives in `spiral-gallery-code-export.ts`, is loaded lazily only after `Export Code` is clicked, serializes all current state and media, rewrites the renderer into portable project-local modules, and ships an explicit agent integration contract preserving both vertex-shader bends and input physics. Both actions report sticky progress through `spiral.export`.
- Alternatives rejected: Code-only delivery violates the still-output contract and the clarification; PNG-only delivery omits the requested integration artifact; loading the ZIP builder, raw source modules, and `fflate` in the initial application chunk increased startup pressure; reducing raster resolution or upscaling a smaller frame would violate selected output fidelity.
- State/output mapping: Image format/resolution/background, canvas size, current settings, current Shadow controls, runtime media order/transforms, and the current physical position feed PNG/JPG rendering. The same runtime state is serialized into `gallery.config.json`, portable TypeScript/CSS modules, documentation, and bundled assets for code export. Both actions resolve through the canonical panel action and renderer-pipeline context.
- Files changed: `src/app/app-schema.ts`, `src/app/app-acceptance-data.ts`, `src/app/app-performance.ts`, `src/app/app-performance-impact.json`, `src/app/spiral-gallery/spiral-gallery-code-export.ts`, `src/app/spiral-gallery/spiral-gallery-code-export.test.ts`, `src/app/spiral-gallery/spiral-gallery-export.ts`, `src/app/spiral-gallery/spiral-gallery-gl.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/spiral-gallery-product.test.ts`, `e2e/spiral-gallery-test-helpers.ts`, `e2e/spiral-gallery.spec.ts`, `e2e/stack-gallery.spec.ts`, `e2e/app-performance-path-adapters.ts`, `docs/plans/2026-08-04-gallery-png-export-restore.md`, and this worklog.
- Performance intent: ordinary-product-work. `spiral.export` stays batch-responsive and linear in the existing `repetition-count` dimension. The code builder is lazy-loaded so its compression library and raw portable sources do not burden initial render. Directly reallocating the retained WebGL backing to 4K produced unstable 82–109 ms gaps; projection-correct tiling bounds each WebGL axis to 2048 px, yields between tile render/readback/composition, restores preview, and adds two final stabilization frames before download without reducing final dimensions or geometry fidelity.
- Verification: `npm run typecheck`; targeted Vitest for product, code archive, and performance gates (14 passed); combined Flow PNG/JPG/code browser acceptance and Deck PNG/code acceptance (2 passed); exact `spiral.export` canonical performance path (passed); protected `npm run verify:delivery` passed with both unit files, both functional browser scenarios, production build, and all six impact-derived canonical performance paths (6 passed). Full performance certification was not requested or run.
- Skipped checks: Full performance certification was not requested; only the exact impact-derived delivery paths are selected. No dependency or lockfile change requires reinstall.
- Risks: A destination still needs a React/TypeScript host and an agent to map the supplied adapter into project conventions. 8K final Canvas 2D allocation and browser encoding can still fail on severely memory-constrained devices even though the renderer does not silently lower the chosen resolution.

### Iteration 12 — Full Shadow Controls: Color With Opacity, Blur, Offset

- Request: добавь параметры настройки тени, цвет с опасити, размытие, смещение.
- Task type: Tier 3 — the shadow entity graduates from two Cards sliders to its own Shadow section with typed compound controls, and the renderer's shadow pass consumes color, opacity, blur, and a light offset; pass structure, workload dimensions, media, and layouts remain unchanged.
- User-visible result: A new Shadow section tunes the cast shadow of every card: `Color` is a colorOpacity control (hex plus 0–100% opacity, 0% removes the shadow and its extra draws), `Blur` controls the feathered spread, and `Offset` is a two-axis vector pad moving the shadow as if repositioning the light (screen-coordinate pad: dragging down-right moves the shadow down-right). Defaults keep the previous look (black, 35%, 0.35 blur, down-right offset). The effective shadow state is exposed on the canvas as `data-image-gallery-shadow` for browser evidence.
- Source/reference checked: The built-in control owners for the requested value models (`colorOpacity` is the exact owner of color-plus-opacity, `vector` the exact owner of a stable authored offset/light direction with `direct-authored` movement ownership), the colorOpacity value schema (`{hex, opacity 0..100}`), the vector value model (string coordinates clamped ±1, screen coordinate mode, editable value label, per-axis and pad inputs), the compound-part acceptance requirements (`colorOpacity.hex/opacity`, `vector.x/y` with per-part browser evidence), and the Iteration 11 shadow pass this extends.
- Reference inputs: None. The controls follow the user's requested parameter list.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md` and `core/layout.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification route `acceptance-testing.md` (compound-control section). All were read earlier in this session and re-consulted.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `controls-component-layout-invariants`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; the offset pad authors a stable light parameter, not a camera or orbit.
- Interaction ownership: Unchanged; all three controls are panel property edits on the shadow entity with no plausible canvas counterpart, and the vector pad classifies as `direct-authored` movement ownership.
- Decision: Replace `card.shadowStrength`/`card.shadowSoftness` with a dedicated Shadow section and entity targets: `shadow.color` (colorOpacity, opacity folds the old strength into the compound control per the no-detached-opacity rule), `shadow.blur` (continuous slider), and `shadow.offset` (default vector pad, value strings ±1, mapped to world units through `spiralShadowOffsetScale` with the screen-coordinate Y flip in the resource). The settings reader normalizes hex to lowercase six digits, clamps opacity to 0–1 and axes to ±1, and the GL pass takes a straight-alpha `uShadowColor` vec4 plus the per-frame offset, keeping the analytic SDF feather. The frame snapshot now carries a shadow signature (`hex|opacity%|blur|x|y`) that the canvas publishes as a data attribute, giving the compound-part browser recipes an exact expected outcome while the rows keep separate rendered-pixel change evidence.
- Alternatives rejected: Keeping a separate strength slider beside a color control violates the colorOpacity ownership rule; paired X/Y sliders for the offset are an explicit wrong-substitution case for `vector`; folding the three controls into Cards would push that section past the layout heuristics and mix entities; a screen-space blur pass was rejected again in favor of the existing analytic feather.
- State/output mapping: `shadow.color`/`shadow.blur`/`shadow.offset` flow through `readSpiralGallerySettings` into the per-frame card uniforms and shadow-pass matrices; every rendered frame carries the same treatment into preview, the published shadow signature, and PNG/JPG export.
- Files changed: `src/app/spiral-gallery/spiral-gallery-gl.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-types.ts`, `src/app/spiral-gallery/spiral-gallery-canvas.tsx`, `src/app/app-schema.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/app-acceptance-data.ts`, `src/app/spiral-gallery-product.test.ts`, `e2e/spiral-gallery-test-helpers.ts`, `e2e/spiral-gallery.spec.ts`, and this worklog.
- Performance intent: ordinary-product-work. The shadow pass keeps its Iteration 11 cost shape (one extra draw per visible card, skipped entirely at 0% opacity); the reworked uniforms add constant per-frame work. `shadow.blur`/`shadow.offset` join the drag-continuous target list and `shadow.color` the change-target list of the existing preview/export passes; no new workload dimension or pass.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-discrete%22%2C%22control-change%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser/performance suites could not run in the sandboxed authoring environment (no npm registry access for a Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: full `tsc --noEmit` and the dependency-free docs/integrity/code-health/AST gates; the compound-part browser coverage was authored against the inspected control markup (hex/opacity input labels, vector value-label editor) and the published shadow signature. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: Persisted `card.shadowStrength`/`card.shadowSoftness` values from the short-lived Iteration 11 become ignored orphans and the shadow returns to defaults once; the compound browser coverage depends on the runtime's stable input labels (`Color hex`, `Color opacity`, `Edit Offset value`), which the delivery gate will confirm on the development machine.

### Iteration 11 — Soft Cast Card Shadows

- Request: добавь тени от изображений.
- Task type: Tier 3 — the WebGL renderer gains a per-card shadow pass inside `spiral.preview`/`spiral.export` and the Cards section gains two sliders; media, layouts, physics, workload dimensions, and pass structure remain unchanged.
- User-visible result: Every image card in Flow and Deck casts a soft translucent shadow, offset down-right as if lit from the upper left, drawn in painter's order so a front card's shadow falls on the cards behind it. The shadow follows the card's rotation, cylindrical bend, flex, scale, and fade (a falling Deck card's shadow fades with it), appears in live preview and still export (as semi-transparent black over an excluded background), and is tuned by two new Cards sliders: `Shadow` (strength, 0 disables) and `Shadow softness` (spread/feather).
- Source/reference checked: The existing GL frame contract (per-frame card uniforms, painter's sort, no depth buffer, straight-alpha blending), the rounded-corner SDF already used for card silhouettes, the Iteration 1 decision that removed the old DOM overlay chrome (the removed decoration was app chrome; this shadow is product output rendered by the product pass), and the shared slider/acceptance patterns for card controls.
- Reference inputs: None. The shadow treatment is a product-authored refinement requested in conversation.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, and `renderer-technique.md`; Verification route `acceptance-testing.md`. All were read earlier in this session and re-consulted.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-technique-inventory`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; the shadow adds no interaction.
- Interaction ownership: Unchanged; both new controls are panel sliders on the card entity.
- Decision: Render the shadow as a second draw of the same deformed plane per card, immediately before the card in the sorted painter's order: the model matrix adds a fixed world-space offset (`spiralShadowOffset`, 0.12/−0.2) and scales by `1 + softness`, and a shadow fragment branch paints the card's rounded silhouette (inset by the inverse spread) in black with a feathered smoothstep falloff, multiplied by the shadow strength and the card's own opacity. Strength and softness are shared card settings (`card.shadowStrength`, `card.shadowSoftness`, defaults 0.35/0.35) flowing through the existing per-frame card uniforms; both targets join the pipeline slider inventory, the shared acceptance slider rows, the Cards section inventory, and the browser slider sweep.
- Alternatives rejected: A DOM/CSS shadow element would reintroduce the removed overlay chrome and never reach the export; a blurred framebuffer pass would add render-target lifecycle and cost for a look the analytic SDF feather already produces; per-card shadow color/direction controls were left out to keep the Cards section within the layout heuristics (six controls) — the fixed upper-left light matches both layouts.
- State/output mapping: `card.shadowStrength`/`card.shadowSoftness` flow through `readSpiralGallerySettings` into the per-frame card uniforms; the renderer derives spread and inset once per frame and draws shadow-then-card per sorted entry, so the same treatment reaches live preview, the frame snapshot, and PNG/JPG export.
- Files changed: `src/app/spiral-gallery/spiral-gallery-gl.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/app-schema.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/app-acceptance-data.ts`, `src/app/spiral-gallery-product.test.ts`, `e2e/spiral-gallery-test-helpers.ts`, and this worklog.
- Performance intent: ordinary-product-work. The shadow pass doubles draw calls and fragment fill inside the existing linear repetition-count relationship (bounded by the same card cap); no new workload dimension, resource, render target, or lifecycle is added, and `card.shadowStrength: 0` skips the extra draws entirely. `spiral-gallery-gl.ts` keeps its existing performance classification over `spiral.webgl-resource`, `spiral.preview`, and `spiral.export`.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22animation-frame%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser/performance suites could not run in the sandboxed authoring environment (no npm registry access for a Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: full `tsc --noEmit` over the changed project and the dependency-free docs/integrity/code-health/AST gates; the shader branch reuses the already-verified rounded-rect SDF. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: The added fill cost is largest at maximum repetitions with large canvases; the strength slider's zero position restores the previous draw count exactly. Export keeps straight-alpha semi-transparent shadows over an excluded background, which is intended for compositing but slightly changes existing background-off exports at the new defaults.

### Iteration 10 — Remove The Blinds Layout

- Request: давай удалим этот третий вариант.
- Task type: Tier 3 — the third layout branch is removed from the schema, settings reader, renderer resource, pipeline target inventory, acceptance data, impact inventory, and browser specs; the remaining Flow and Deck behavior, including the Iteration 7 weighted Deck scroll, is unchanged.
- User-visible result: The Layout control offers Flow and Deck again. The Blinds section, its seven sliders, and the louver-wall rendering are gone. Galleries persisted while Blinds was selected reopen safely in Flow, and orphaned `blinds.*` values in existing local storage are ignored.
- Source/reference checked: The Iteration 8–9 change set (every file that introduced the mode) and a repository-wide search confirming no production, schema, acceptance, pipeline, impact, or browser-spec reference to the removed mode remains; the only deliberate remaining mention is a product-test guard proving the legacy persisted `layout.mode: "blinds"` value falls back to Flow.
- Reference inputs: None. This delivery removes a product branch at the user's explicit request.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification route `acceptance-testing.md`. All were read earlier in this session and re-consulted for the removal.
- Contract rules applied: `runtime-shell-required`, `controls-product-coverage`, `controls-section-inventory-required`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` for the two remaining layouts.
- Interaction ownership: Unchanged; removing the mode adds no new surface.
- Decision: Delete `spiral-gallery-blinds.ts`, its unit tests, and `e2e/blinds-gallery.spec.ts`; revert the settings type/reader, the renderer's mode branches (snap, spring weight, scene composition, card uniforms, placement loop, input direction), the pipeline slider-target list, the Layout segmented options and description, the Deck-era acceptance rows and section inventory, and the impact inventory to their pre-Blinds state while keeping the Iteration 7 weighted-scroll work intact. Remove `videoReferenceStudy` together with the feature it studied so the transfer intent no longer cites a video reference; the study remains recorded in this worklog's history. Keep the schema `layout.mode` fallback tolerant so a persisted `"blinds"` value degrades to Flow without migration.
- Alternatives rejected: Hiding the option while keeping dead code would leave an unreachable schema section and untestable acceptance rows; a persistence version bump is unnecessary because the removed targets simply become ignored values and the mode value falls back safely; deleting the Iteration 8–9 worklog history would erase the decision trail the contract requires.
- State/output mapping: `layout.mode` again selects only the Flow or Deck branch in `renderScene`; all other state flows are unchanged.
- Files changed: removed `src/app/spiral-gallery/spiral-gallery-blinds.ts`, `src/app/spiral-gallery/spiral-gallery-blinds.test.ts`, and `e2e/blinds-gallery.spec.ts`; reverted `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/app-schema.ts`, `src/app/app-acceptance-data.ts`, `src/app/spiral-gallery-product.test.ts`, `src/app/app-performance-impact.json`, and `e2e/stack-gallery.spec.ts`; updated this worklog.
- Performance intent: ordinary-product-work. The removal deletes per-frame work and seven responsiveness targets; no workload dimension, pass, adapter, or export stage changes.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --unit-test=src/app/spiral-gallery/spiral-gallery-stack.test.ts --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync"` is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser suites could not run in the sandboxed authoring environment (no npm registry access for a Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: a repository-wide reference sweep, full `tsc --noEmit` over the reverted project, and the dependency-free docs/integrity/code-health/AST gates. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: None: the removal restores the previously delivered two-layout surface; the only behavioral difference against Iteration 7 is that a persisted `"blinds"` layout value now renders Flow.

### Iteration 9 — Blinds Rework: Stationary Wall With Traveling Focus

- Request: изучи этот видео референс галереи, то что ты сделал сейчас вообще не то как я задумывал, прикладываю видео референес (CleanShot 2026-08-04 at 13.28.14 2.mp4).
- Task type: Tier 3 — the Blinds placement math and focus model inside `spiral.preview` change against the full video reference; schema controls keep their targets (two defaults retuned), and media, shaders, export algorithms, workload dimensions, and pass structure remain unchanged.
- User-visible result: The Blinds wall now behaves like the reference recording. Every image is one tall panel at a fixed position in a wall spanning the scene — the wall never slides. Scrolling moves the focus along the wall: the focused panel swings open in place (its projected width growing ~2.2× purely from the rotation), brightens, and pulls slightly forward; neighbors open partially; the focus glides with the weighted spring, settles on a whole panel, and stops at the first and last panel instead of wrapping.
- Source/reference checked: The full 14.2 s reference recording studied frame by frame (1 fps overview, 6 fps dense pass, full-resolution label-strip crops): panel x-positions were confirmed static across the sweep while the highlighted collection label and open slot moved; the open/closed projected-width ratio (~2–3×) matches the cos(open)/cos(rest) rotation prediction; the focus reaches the wall ends without wrapping; the later zoomed phase shows the same mechanic at a closer camera. The previous Iteration 8 implementation (wrapped sliding wall with a fixed center focus) was diagnosed as the wrong mechanic against this evidence.
- Reference inputs: `/Users/kusnizza/Desktop/CleanShot 2026-08-04 at 13.28.14 2.mp4` — 14.2 s, 3674×2074 at 60 fps reference screen recording of the louver-wall gallery (supersedes the four extracted frames used in Iteration 8).
- Docs/contracts read: `workflow.md`; Plan routes `core/reference-study.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `renderer-technique.md` and `component-rules.md`; Verification route `acceptance-testing.md` (video-reference section). All were read in Iterations 7–8 of this session and re-consulted for this correction.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `video-reference-analysis`, `interaction-surface-ownership`, `controls-product-coverage`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`; the recording browses with a stationary camera (plus an app-side zoom later), and Blinds input drives only the focus offset. Canvas zoom remains the runtime toolbar's job.
- Interaction ownership: Unchanged; the canvas keeps transient browsing, the panel keeps mode choice and Blinds tuning.
- Decision: Rework `spiral-gallery-blinds.ts` around a stationary finite wall: panel x comes only from the panel's index (`(index − (count−1)/2) × spacing`), the focus offset is clamped to `[0, count−1]` (`clampBlindsOffset`, applied to the shared target offset each Blinds frame), openness derives from the unwrapped distance to the clamped focus, every panel stays visible (no wrap, no edge fade), and paint order stacks toward the focus. Rest/open tilt interpolation, the rest-brightness floor, the forward pull, the panel stretch, and the weighted-spring-plus-snap motion carry over from Iteration 8. Defaults retuned to the video: open tilt 20°→14° (width gain 2.21×), spacing 0.62→0.72 (a fourteen-image wall spans ~11 of the ~12.3 visible units at the default camera).
- Alternatives rejected: Keeping the sliding-wall model with a larger focus width cannot reproduce the reference — the recording proves fixed panel columns under fixed labels; wrapping the focus at the ends would teleport the open slot across the screen; hover-driven focus was rejected because the recording drives focus by scrolling while the pointer rests, and pointer hover already owns parallax in this product; per-panel width animation was rejected because the measured width gain equals the pure rotation ratio.
- State/output mapping: `layout.mode = "blinds"` selects the branch; the input-and-snap-owned target offset is clamped to the wall, followed by the weighted spring into `currentOffset`, and fed with each panel's index into `computeBlindsCardPlacement`; the fixed slot positions, focus-driven tilt/brightness/pull, and stretched card uniforms render the same scene into live preview, the settled/current-index snapshot, and still export.
- Files changed: `src/app/spiral-gallery/spiral-gallery-blinds.ts`, `src/app/spiral-gallery/spiral-gallery-blinds.test.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/app-schema.ts`, `src/app/app-acceptance-data.ts` (rewritten videoReferenceStudy from the full recording, corrected observables), and this worklog.
- Performance intent: ordinary-product-work. The wall draws at most the bounded 24 sources with constant per-panel math; clamping replaces wrapping at equal cost; no pass, workload dimension, adapter, or export change.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-blinds.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: blinds layout controls update rendered output" --browser-test="browser: blinds navigation and image-only export stay in sync" --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync"` is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser/performance suites could not run in the sandboxed authoring environment (no npm registry access for a Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: the frame-by-frame video study with static-wall and width-ratio measurements; deterministic numeric validation of the reworked placement (slot positions independent of focus, local two-to-three-panel envelope, edge clamping, whole-panel settle across weights) ; full `tsc --noEmit`; and the dependency-free docs/integrity/code-health/AST gates. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: With more than ~17 images at the default spacing the wall overflows the default camera view — Panel spacing, Camera distance, and Scene offset cover this; a future auto-fit was deliberately not added to keep the reference's fixed-pitch look. Existing persisted `blinds.spacing`/`blinds.openTiltDegrees` values from Iteration 8 sessions are kept as user-edited state and are not migrated to the retuned defaults.

### Iteration 8 — Blinds Louver-Wall Layout From Video Reference

- Request: я хочу чтобы ты сделал еще один тип галереи, механика показана на скринах.
- Task type: Tier 3 — a third layout branch is added to the retained WebGL renderer's `spiral.preview` placement math with one new schema section and mode option; media flow, shaders, export algorithms, workload dimensions, and pass structure remain unchanged.
- User-visible result: A third Layout option `Blinds` turns the same source sequence into a dark louver wall of tall stretched image panels. Panels rest almost edge-on at a shared steep tilt and stay dim; the panel at the focus swings open toward the camera, brightens to full color, and pulls slightly forward, with neighbors opening partially inside a distance envelope. Wheel, drag, and arrow input slides the wall through the shared weighted spring and always settles with one whole panel open. A new Blinds section exposes Panel spacing, Panel stretch, Rest tilt, Open tilt, Focus width, Rest brightness, and Scroll weight.
- Source/reference checked: The four user-provided extracted frames of the reference screen recording (studied as the typed Video Reference Study in `appTransferMode.videoReferenceStudy`); the existing three-way renderer state (`renderScene` offsets, snapping, spring, card uniforms); the GL card contract (per-frame card width/height/focusFloor uniforms, per-card focus/flatten/renderOrder); the Deck section, acceptance, and browser-test patterns this mode mirrors.
- Reference inputs: `CleanShot 2026-08-04 at 13.32.41@2x.png`; `CleanShot 2026-08-04 at 13.32.55@2x.png`; `CleanShot 2026-08-04 at 13.33.05@2x.png`; `CleanShot 2026-08-04 at 13.33.16@2x.png` — four extracted frames sampling a three-second reference screen recording of the louver-wall gallery mechanic.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`, and `renderer-technique.md`; Verification route `acceptance-testing.md` (video-reference section).
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `video-reference-analysis`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `controls-layout-heuristics`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` with the prior explicit-user-request evidence; the reference recording browses the wall without rotating the camera, and Blinds input drives only the shared wrapped offset.
- Interaction ownership: Unchanged. The canvas remains the transient browsing surface for the new wall; the panel owns the mode choice and all Blinds tuning without duplicating canvas navigation.
- Decision: Implement the mechanic as pure placement math in `spiral-gallery-blinds.ts`: a centered wrapped distance, a smoothstep openness envelope bounded by `Focus width`, tilt interpolation from `Rest tilt` to `Open tilt` (negative rotation so faces open toward the camera side seen in the frames), focus equal to openness over a mode-specific `Rest brightness` floor, a constant forward pull, bounded visible depth with edge fade, and focused-on-top paint order. The resource keeps one card set (single repetition), stretches the shared card into tall slats through the per-frame width/height uniforms (`blindsPanelWidthScale` × `Panel stretch`), swaps the frame focus floor to the wall's rest brightness, forces `flatten: 1` so slats stay rigid, reuses the Deck's minimum snap and weighted critically damped spring with a Blinds-scoped `Scroll weight`, and flips input direction so wheel-down and ArrowDown advance the sequence. The study frames map to acceptance rows through `videoReferenceStudy.acceptanceMapping`.
- Alternatives rejected: A DOM/CSS panel wall would abandon the shared WebGL pipeline and its export path; reusing the Deck placement with different constants cannot produce a side-by-side louver row; per-card geometry variants would break the single-plane-grid renderer, so the tall slats come from the existing per-frame card uniforms; a fourth "reflection floor" treatment visible in some reference products was not in the provided frames and was not added; putting Blinds tuning into the shared Physics/Depth sections would gate mode-scoped values outside their entity and push those sections past seven controls.
- State/output mapping: `layout.mode = "blinds"` selects the branch; `blinds.*` values flow through `readSpiralGallerySettings` into `renderScene`, where the wrapped offset (input + snap + weighted spring) drives `computeBlindsRelative`/`computeBlindsCardPlacement` panel transforms, per-card focus against the Blinds rest-brightness floor, and the stretched card uniforms; the same scene renders live preview, the settled/current-index snapshot consumed by canvas data attributes and browser evidence, and the still PNG/JPG export.
- Files changed: `src/app/spiral-gallery/spiral-gallery-blinds.ts` (new), `src/app/spiral-gallery/spiral-gallery-blinds.test.ts` (new), `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/app-schema.ts`, `src/app/app-acceptance-data.ts`, `src/app/spiral-gallery-product.test.ts`, `src/app/app-performance-impact.json`, `e2e/blinds-gallery.spec.ts` (new), `e2e/stack-gallery.spec.ts`, and this worklog.
- Performance intent: ordinary-product-work. Blinds renders one repetition with a bounded visible depth (at most 25 slats pass the opacity cull), below the Flow maximum of sources × 8 repetitions; per-frame math stays constant-cost per card inside `spiral.preview`. The seven Blinds sliders join `spiralSliderTargets`, so derived control-drag/animation-frame paths and their scenarios cover them like the other responsiveness sliders; no workload dimension, pass, lifecycle, or export stage changes. `spiral-gallery-blinds.ts` is classified in the impact inventory as a performance module owning `spiral.preview` and `spiral.export`.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-blinds.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: blinds layout controls update rendered output" --browser-test="browser: blinds navigation and image-only export stay in sync" --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync"` is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser/performance suites could not run in the sandboxed authoring environment (no npm registry access for a Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: the typed Video Reference Study; deterministic numeric validation of the Blinds placement (openness envelope symmetry and bounds, tilt interpolation without overshoot, edge fade, paint order, wrap behavior) and of the integrated wall state machine (nudge → weighted glide → snap → settles with one whole panel open across weights and frame scales); full `tsc --noEmit`; and the dependency-free docs/integrity/code-health/AST gates. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: Default Blinds composition (spacing 0.62, stretch 3.8, rest tilt 64°) was tuned numerically against the reference frames rather than visually in a browser; the seven sliders cover the full plausible range if the first-run look needs adjustment. Persisted galleries keep working: the new targets simply adopt defaults, and existing Flow/Deck behavior is untouched.

### Iteration 7 — Weighted Adjustable Deck Scroll

- Request: Изучи приложение и сделай в версии Deck более приятный скролл изображений утяжеленный, и чтобы этот параметр можно было регулировать. сейчас резко и не тактильно.
- Task type: Tier 3 — the Deck navigation response inside the `spiral.preview` frame math changes and one new Deck schema slider is added; media, layouts geometry, shaders, export algorithms, workload dimensions, and pass structure remain unchanged.
- User-visible result: In Deck layout, wheel, drag, and arrow navigation now moves the cards through a weighted critically damped spring: velocity ramps up over several frames, glides, and settles softly onto a whole card instead of jumping on the first frame. A new continuous `Scroll weight` slider (0–1, default 0.55) in the Deck section tunes how heavy that motion feels, from near-immediate (0) to a slow heavy glide (1). Flow layout and the reduced-motion accessibility path keep the previous first-order response.
- Source/reference checked: The existing `spiral-gallery-resource.ts` offset integration (first-order lerp with `physics.inertia`), the forced `stackMinimumSnapStrength` deck snapping, the deck wheel/drag/key nudge path, the Deck section schema and acceptance rows, the stack placement math and its tests, and the derived pipeline slider-target inventory.
- Reference inputs: None. The change is a product-authored motion refinement of the existing deck behavior.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/runtime-boundary.md`, and `core/performance.md`; Implementation routes `schema-reference.md`, `component-rules.md`; Verification route `acceptance-testing.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` with the prior explicit-user-request evidence. The spring changes only how the shared deck offset follows input; wheel, drag, and arrow keys still browse images without rotating the camera.
- Interaction ownership: Unchanged. The canvas remains the transient browsing surface; the panel owns precise motion tuning, so the new `Scroll weight` slider joins the other Deck parameters without duplicating canvas navigation.
- Decision: Keep `targetOffset` input accumulation and deck snapping as they are, and replace only the Deck-mode offset follower with a critically damped spring (`resolveStackScrollSpring` + `advanceStackScroll` in the pure stack module): stiffness maps linearly from `stack.scrollWeight` between 0.09 (light) and 0.011 (heavy) per frame, damping stays critical (`2√k`), and integration uses semi-implicit Euler with exponential damping so the renderer's clamped 0.001–2 frame scales stay stable. The settled snapshot additionally requires negligible spring velocity so the frame loop keeps running through the glide and still suspends afterwards. The new slider lives in the Deck section (`visibleWhen` stack), is continuous with no unit, and reuses the shared responsiveness slider helper.
- Alternatives rejected: Raising or lowering `physics.inertia` alone keeps the first-order shape whose velocity peaks on the first frame — exactly the abrupt feel the user rejected. An underdamped spring adds bounce that fights deck snapping and reads as overshoot on card flips. A duration-based tween per card would drop mid-glide wheel input instead of absorbing it into momentum. Placing the slider in the shared Physics section would grow that section past seven controls and gate a stack-only value outside its entity section.
- State/output mapping: `stack.scrollWeight` flows through `readSpiralGallerySettings` into `renderScene`, where the spring advances `currentOffset` toward the input-and-snap-owned `targetOffset`; the offset drives `computeStackRelative`/`computeStackCardPlacement` card transforms, the existing velocity-derived flex, and the settled/current-index frame snapshot consumed by the canvas data attributes, browser evidence, and still export.
- Files changed: `src/app/spiral-gallery/spiral-gallery-stack.ts`, `src/app/spiral-gallery/spiral-gallery-settings.ts`, `src/app/spiral-gallery/spiral-gallery-resource.ts`, `src/app/spiral-gallery/spiral-gallery-stack.test.ts`, `src/app/app-schema.ts`, `src/app/spiral-gallery/spiral-gallery-pipeline.ts`, `src/app/app-acceptance-data.ts`, `src/app/spiral-gallery-product.test.ts`, `e2e/stack-gallery.spec.ts`, and this worklog.
- Performance intent: ordinary-product-work. The spring adds constant per-frame arithmetic to the existing offset update inside `spiral.preview`; no workload dimension, pass, invalidation target list beyond the new slider target, resource lifecycle, or export stage changes. `stack.scrollWeight` joins `spiralSliderTargets`, so the derived control-drag/animation-frame paths cover it like the other physics sliders.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-stack.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync"` is the exact delivery command for this batch.
- Skipped checks: The protected delivery gate and browser/performance suites could not be executed in the sandboxed authoring environment (no npm registry access for the Linux toolchain; the project toolchain is installed for macOS). Development evidence recorded instead: deterministic numeric validation of `resolveStackScrollSpring`/`advanceStackScroll` and of the integrated nudge→glide→snap→settled state machine across weights 0/0.55/1 and frame scales 0.001–2 (no overshoot, whole-card settle, monotonic weight-to-settle-time mapping, reduced-motion parity), full `tsc --noEmit` typecheck, and the dependency-free docs/integrity/code-health gates. The recorded `verify:delivery` command remains to be run on the development machine.
- Risks: Settle-to-epsilon takes up to ~3 s at maximum weight (perceived motion ends earlier; the tail is sub-pixel), matching the magnitude of the previous first-order tail, so browser settle waits remain within their 10 s windows. Persisted decks without `stack.scrollWeight` adopt the 0.55 default, which intentionally changes the default Deck feel toward the requested weighted glide.

### Iteration 6 — Replace Two Cards With User AVIF Sources

- Request: Заменить две отмеченные карточки на последние два предоставленных файла.
- Task type: Tier 3 — two default media assets, persistence identity, acceptance expectations, and initial WebGL output change; renderer passes, physics, controls, workload limits, and export algorithms remain unchanged.
- User-visible result: `Salt Ponds Aerial.jpg` is replaced by `White SUV Desert Photo.jpg`, and `Misty Forest.jpg` is replaced by `Woman with White Eyeliner and Yellow Jacket.jpg`. The other twelve presets keep their positions, the collection remains fourteen images, and repetition three still renders 42 physically curved Flow cards.
- Source/reference checked: The two user-provided screenshots identifying the salt-pond and misty-forest cards; the two user-provided AVIF sources and their decoded dimensions/content; the v6 asset records and local JPGs; Toolcraft default-media hydration/reset behavior; targeted media/persistence acceptance; and the native local-browser output after a real reload.
- Reference inputs: `/Users/kusnizza/Desktop/White SUV Desert Photo.avif`; `/Users/kusnizza/Desktop/Woman with White Eyeliner and Yellow Jacket (1).avif`; `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-82807fae-3893-42de-8388-aa9af7e04823.png`; `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-d2cefd27-f0e7-4231-894a-46d95805600f.png`.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` with the prior explicit-user-request evidence. Both user images use the same wheel, drag, arrow-key, inertial movement, press response, and physical card-bend paths.
- Interaction ownership: The existing Gallery `fileDrop` remains the only collection-edit surface and canvas remains the transient browsing surface. No preset picker, source label, or duplicate canvas collection UI is added.
- Decision: Map the screenshots to the supplied files in request order, convert both AVIF sources to quality-90 JPG files at a 3000 px long edge, preserve the remaining sequence positions, remove the superseded public assets while retaining a temporary recoverable backup, and bump persistence to v7 so existing v6 media cannot retain the two replaced cards.
- Alternatives rejected: Keeping AVIF would not fulfill the established JPG preset workflow; overwriting the old files without renaming would leave misleading names; replacing more than the two marked cards would exceed the request; hard-coding images in the renderer would bypass runtime media transforms/order/reset/export. A repeated verification failure isolated dnd-kit keyboard collision coordinates as nondeterministic in the four-column thumbnail grid, so media acceptance now performs the same reorder with a real pointer drag between measured card centers rather than weakening the order assertion or patching immutable runtime code.
- State/output mapping: Ordered v7 `media.defaultAssets` hydrate `source.images`; runtime media order and transforms pass through `selectSpiralGalleryImages` into the retained WebGL preview and still-export path. Fourteen sources multiplied by repetition three produce 42 Flow cards; Reset restores the exact v7 sequence and reload persists it.
- Files changed: two local JPG assets, product schema, product Vitest, gallery Playwright acceptance, implementation plan, and this worklog. The superseded JPGs were moved from `public/gallery-presets` to `/private/tmp/gallery-v7-review.Pm7Khm/replaced-assets`.
- Performance intent: ordinary-product-work. Fourteen sources remain below the enforced 24-source boundary; pass cost, invalidation, resource lifecycle, shader work, animation settling, export stages, adapters, and bounded fixtures are unchanged.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload"` is the exact delivery command for this batch.
- Skipped checks: Renderer performance scenarios and full performance certification are not run because no renderer pass, shader, workload boundary, animation lifecycle, fixture adapter, or export algorithm changed.
- Risks: Persistence v7 intentionally supersedes v6 once. The replacement JPG pair is about 2.8 MB versus about 3.8 MB for the removed pair, reducing the static preset payload by roughly 1 MB.

### Iteration 5 — Replace Two Rejected Presets

- Request: Эти замени.
- Task type: Tier 3 — two default media assets, persistence identity, acceptance expectations, and initial WebGL output change; renderer passes, physics, controls, workload limits, and export algorithms remain unchanged.
- User-visible result: `Spectrum Gradient.jpg` is replaced by the photographic `Salt Ponds Aerial.jpg`, and `Neon City Reflection.jpg` is replaced by the sharp `Blue Ice Cave.jpg`. The other twelve presets keep their shuffled positions, the default collection remains fourteen images, and repetition three still renders 42 physically curved Flow cards.
- Source/reference checked: The two user-provided screenshots identifying the rejected rainbow-gradient and blurred-neon cards; their matching v5 asset records and local JPGs; candidate Unsplash photo pages; downloaded JPEG dimensions and visual content; the v6 schema/reset flow; targeted media/persistence acceptance; and the native local-browser output after a real reload.
- Reference inputs: `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-3326243c-51d2-481d-a807-91830f05fee6.png`; `/var/folders/59/s44c8y816ss64d4trvkqybsm0000gn/T/codex-clipboard-e7ebb86c-90c5-43b1-b044-f1a3baecf9bd.png`; `https://unsplash.com/photos/aerial-view-of-colorful-salt-evaporation-ponds-Tt4is-cWMR8`; `https://unsplash.com/photos/a-close-up-of-a-blue-ice-cave-TaGDiQk3jN4`.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` with the prior explicit-user-request evidence. Both replacements use the same wheel, drag, arrow-key, inertial movement, press response, and physical card-bend paths.
- Interaction ownership: The existing Gallery `fileDrop` remains the only collection-edit surface and canvas remains the transient browsing surface. No preset picker, source label, attribution layer, or duplicate canvas collection UI is added.
- Decision: Use a crisp aerial salt-pond photograph and a blue ice-cave photograph as stylistically stronger replacements; localize both as quality-90 JPG files with a 3000 px long edge; preserve the two existing sequence positions; remove the superseded assets from the public build while retaining a temporary recoverable backup; and bump persistence to v6 so existing v5 media cannot keep the rejected images.
- Alternatives rejected: Overwriting the old files without renaming would leave misleading preset names; remote hotlinks would add availability, caching, and CORS risk; replacing more than the two marked cards would exceed the request; runtime randomization would make Reset and acceptance nondeterministic; a custom picker would duplicate the built-in media collection. During targeted proof, the dnd-kit keyboard reorder test was found to send direction before `KeyboardSensor` activation; the test now waits for actual drag state before moving instead of adding arbitrary retries or patching the immutable runtime.
- State/output mapping: Ordered v6 `media.defaultAssets` hydrate `source.images`; runtime media order and transforms pass through `selectSpiralGalleryImages` into the retained WebGL preview and still-export path. Fourteen sources multiplied by repetition three produce 42 Flow cards; Reset restores the exact v6 sequence and reload persists it.
- Files changed: two local JPG assets, product schema, product Vitest, gallery Playwright acceptance, implementation plan, and this worklog. The two superseded JPGs were moved from `public/gallery-presets` to `/private/tmp/gallery-replacement-review.PRy68f/replaced-assets`.
- Performance intent: ordinary-product-work. Fourteen sources remain below the enforced 24-source boundary; pass cost, invalidation, resource lifecycle, shader work, animation settling, export stages, adapters, and bounded fixtures are unchanged.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload"` is the exact delivery command for this batch.
- Skipped checks: Renderer performance scenarios and full performance certification are not run because no renderer pass, shader, workload boundary, animation lifecycle, fixture adapter, or export algorithm changed.
- Risks: Persistence v6 intentionally supersedes v5 once. The two new local JPGs total about 2.8 MB versus about 1.2 MB for the removed pair; both source pages identify the photos as free to use under the Unsplash License.

### Iteration 4 — V5 Full Shuffle And Stale-Profile Recovery

- Request: Не вижу новых изображений в превью; все изображения должны быть в галерее, и весь набор нужно перемешать.
- Task type: Tier 3 — the default media order and persistence identity change the initial visible WebGL output; renderer passes, physics, controls, workload limits, and export algorithms remain unchanged.
- User-visible result: The existing browser profile now opens with all fourteen local JPGs in one fully reshuffled deterministic sequence. Default Flow repetition three renders 42 physically curved cards, and a real page reload preserves the same fourteen-image collection.
- Source/reference checked: All three previously open local-browser tabs; their persisted v4 media state; the v4 schema and fourteen local presets; the native user tab's 732 px viewport; Toolcraft default-media hydration/reset behavior; the media lifecycle and persistence recipes; and the live WebGL output after reload.
- Reference inputs: The current local app at `http://127.0.0.1:3016/` and the existing fourteen JPG files under `public/gallery-presets`. No new external assets were added.
- Docs/contracts read: `workflow.md`; Plan routes `decision-contract.md`, `core/runtime-boundary.md`, `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `component-rules.md`, `renderer-technique.md`, and `schema-reference.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`. Wheel, drag, arrow-key navigation, inertia, press response, and physical card bending continue to operate on the complete shuffled collection.
- Interaction ownership: The existing Gallery file-drop remains the only collection-edit surface and the canvas remains the transient browsing surface. No duplicate picker, reset control, or canvas collection UI is introduced.
- Decision: Reorder all fourteen defaults into a new deterministic mixed sequence and bump the persistence identity to v5. Before applying the identity change, temporarily navigate the native user tab away from the hot-reloading app so its stale eight-image v4 state cannot be copied into the new key; then return that same tab to the app and verify the v5 state after reload.
- Alternatives rejected: The visible Gallery Reset icon was 24 px outside the narrow native viewport and could not be reached by pointer automation; direct localStorage mutation would be fragile and bypass the product workflow; runtime randomization would make Reset and acceptance nondeterministic; hard-coding renderer images would bypass runtime media ordering, transforms, persistence, and export; patching `src/toolcraft` is forbidden. The media proof uses one asserted keyboard reorder step because consecutive reorder keystrokes can replace the focused handle during a runtime rerender and make a second immediate key nondeterministic.
- State/output mapping: Ordered v5 `media.defaultAssets` hydrate `source.images`; runtime media order and transforms pass through `selectSpiralGalleryImages` into the retained WebGL resource and still-export path. Fourteen sources multiplied by repetition three produce 42 Flow cards.
- Files changed: product schema, product Vitest, gallery Playwright acceptance data, implementation plan, and this worklog.
- Performance intent: ordinary-product-work. Fourteen sources remain below the enforced 24-source boundary; pass cost, invalidation, resource lifecycle, shader work, animation settling, export stages, adapters, and bounded fixtures are unchanged.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload"` is the exact delivery command for this batch.
- Skipped checks: Renderer performance scenarios and full performance certification are not run because no renderer pass, shader, workload boundary, animation lifecycle, fixture adapter, or export algorithm changed.
- Risks: Persistence v5 intentionally supersedes v4 so stale eight-image state cannot conceal the complete collection. At a 732 px viewport the host panel's section-reset icon can sit outside the visible right edge; this is a runtime layout limitation rather than an app-owned control, and v5 removes the need to use it for this recovery.

### Iteration 3 — Mixed Fourteen-Image Preset Set

- Request: Перемешать изображения и добавить ещё шесть разных изображений примерно в том же стиле, скачав подходящие материалы с Midjourney или Unsplash.
- Task type: Tier 3 — the built-in media order, asset collection, persistence identity, acceptance expectations, and initial WebGL output change; renderer passes, physics, controls, and export algorithms remain unchanged.
- User-visible result: The gallery starts with fourteen local JPG images in a fixed mixed sequence that alternates nature, architecture, and abstract/color subjects. The six additions are turquoise ocean, misty forest, desert dune, neon city reflection, modern architecture, and spectrum-gradient imagery; all fourteen remain selectable, reorderable, transformable, removable, replaceable, exportable, persistent, and Reset-restorable.
- Source/reference checked: The existing eight-preset collection; six free Unsplash photo pages and their downloaded image assets; decoded JPEG dimensions/types; Toolcraft default-media hydration/reset behavior; the media lifecycle and persistence recipes; the real local UI and WebGL state.
- Reference inputs: `https://unsplash.com/photos/a-person-walks-across-a-desert-dune-Co1PdGUlNxk`; `https://unsplash.com/photos/misty-forest-with-sunbeams-filtering-through-trees-8xkImX3so8U`; `https://unsplash.com/photos/aerial-view-of-turquoise-ocean-waves-crashing-on-shore-c-QI_fNurHw`; `https://unsplash.com/photos/blurry-neon-signs-illuminate-a-dark-city-street-at-night-NbrtaJF3teo`; `https://unsplash.com/photos/modern-architecture-with-geometric-shapes-and-clean-lines-VvpN157u7ks`; `https://unsplash.com/photos/abstract-colorful-background-with-smooth-gradients-fMxEx0WrKCw`.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera` with the prior explicit-user-request evidence. The mixed assets enter the same wheel, drag, arrow-key, inertial movement, and physical card-bend paths.
- Interaction ownership: The existing Gallery `fileDrop` remains the only collection-edit surface. The canvas continues to own transient browsing; no preset picker, source text, attribution layer, or duplicate canvas collection UI is added.
- Decision: Select six stylistically compatible but subject-diverse free Unsplash images, download them as local quality-90 JPEGs bounded to a 3000 px long edge, mix them deterministically among the eight existing presets, and bump persistence to v4 so the requested defaults are visible instead of being shadowed by v3 media state.
- Alternatives rejected: Runtime randomization would make Reset and acceptance nondeterministic; remote hotlinks would add availability, cache, and CORS risk; Midjourney browsing was unnecessary after suitable licensed Unsplash images were found; a new picker would duplicate the built-in file-drop collection; changing renderer placement would alter the requested physical gallery rather than only its content.
- State/output mapping: Ordered `media.defaultAssets` hydrate `source.images`; runtime media order and transforms pass through `selectSpiralGalleryImages` to the retained WebGL resource and still-export path. Default repetition three maps fourteen sources to 42 visible Flow cards. Section Reset restores the exact fourteen-item sequence through the runtime command bus.
- Files changed: six JPG assets in `public/gallery-presets`, product schema, readiness/acceptance inventory, product Vitest, gallery Playwright acceptance, implementation plan, and this worklog.
- Performance intent: ordinary-product-work. Fourteen sources remain below the enforced 24-source renderer boundary; the only schema-backed workload dimension remains repetition count. Pass cost, invalidation, resource lifecycle, animation settling, export stages, performance adapters, and bounded fixtures are unchanged.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload"` is the exact delivery command for this batch.
- Skipped checks: Renderer performance scenarios and full performance certification are not run because no pass implementation, shader, workload boundary, invalidation path, animation lifecycle, fixture adapter, or export algorithm changed.
- Risks: The fourteen-preset static payload is about 16 MB. Persistence v4 intentionally supersedes v3 so the mixed default collection appears; an already-open hot-reload profile may require the visible Gallery Reset once, which was verified in the local browser.

### Iteration 2 — Built-in JPG Gallery Presets

- Request: Сделать восемь предоставленных AVIF-изображений JPG-файлами и пресетами галереи.
- Task type: Tier 3 — the media defaults and persistence identity change the initial visible WebGL output while retaining the existing renderer, controls, export, and interaction model.
- User-visible result: A fresh gallery opens with eight full-resolution JPG images in the requested order. Each image is a normal Gallery item that can be selected, reordered, transformed, removed, or replaced; Reset restores the exact eight-image preset collection.
- Source/reference checked: The eight user-provided AVIF files, their decoded dimensions, the existing `source.images` multi-file control, Toolcraft default-media hydration/reset behavior, gallery settings reader, WebGL renderer, persistence configuration, media acceptance recipe, and the real local UI.
- Reference inputs: `/Users/kusnizza/Desktop/Aerial Mountain Range Photo.avif`; `/Users/kusnizza/Desktop/Snow-capped Mountain Photo.avif`; `/Users/kusnizza/Desktop/Modern High-Rise Buildings.avif`; `/Users/kusnizza/Desktop/Geometric Facade London.avif`; `/Users/kusnizza/Desktop/Abstract Red Orange Background.avif`; `/Users/kusnizza/Desktop/Blue Pink Background Wallpaper.avif`; `/Users/kusnizza/Desktop/Blue Pink Light Illustration.avif`; `/Users/kusnizza/Desktop/Pink White Background Image.avif`.
- Docs/contracts read: `workflow.md`; Plan routes `core/control-selection.md`, `core/layout.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md` and `component-rules.md`; Verification routes `acceptance-testing.md` and `performance.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `interaction-surface-ownership`, `controls-product-coverage`, `controls-section-inventory-required`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: Unchanged `fixed-camera`. The built-in images enter the same wheel, drag, arrow-key, and inertial browsing path as uploaded images.
- Interaction ownership: The existing Gallery file-drop collection remains the only surface for source selection, order, transforms, removal, and replacement. No duplicate preset picker or canvas operation is added.
- Decision: Convert each source to a quality-90 JPEG at its original dimensions, publish the files under `public/gallery-presets`, declare them as ordered `media.defaultAssets` for `source.images`, and bump the persistence key/version so an older persisted empty collection cannot conceal the new product defaults.
- Alternatives rejected: A custom image picker would duplicate the built-in media collection; hard-coding URLs in the renderer would bypass runtime transforms, ordering, reset, and export; downscaling the sources would unnecessarily reduce available export detail; migrating v2 persistence would retain an old empty collection instead of presenting the requested presets.
- State/output mapping: `media.defaultAssets` hydrates the runtime media collection; `source.images` order and transforms flow through `selectSpiralGalleryImages` into the existing WebGL preview and PNG/JPG export. Reset restores the schema defaults through the runtime command bus.
- Files changed: eight JPG assets, product schema, acceptance inventory, product Vitest, gallery Playwright acceptance/helpers, implementation plan, and this worklog.
- Performance intent: ordinary-product-work. The renderer pipeline, per-frame work, workload dimensions, limits, and invalidation model are unchanged. The fixed eight-image startup remains below the existing 24-source boundary; fixture-driven tests explicitly clear defaults before importing their bounded sources.
- Verification: `npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload"` is the exact delivery command for this batch.
- Skipped checks: Full performance certification and targeted renderer performance scenarios are not needed because no renderer pass, shader, workload boundary, animation lifecycle, or export algorithm changed.
- Risks: The preset payload adds about 9.1 MB of static assets. The persistence v3 boundary intentionally resets prior v2 local settings once so the requested defaults are visible.

### Iteration 1 — Image-only Gallery

- Request: Убрать весь декоративный дизайн, оставить только изображения, удалить сведения о прежнем источнике и переименовать продуктовые сущности.
- Task type: Tier 4 — broad product redesign touching schema, renderer output, still export, persistence identity, acceptance metadata, product tests, and browser proof.
- User-visible result: The product is named `Image Gallery`; the visible layouts are `Flow` and `Deck`; preview and export contain only uploaded image cards plus an optional flat background. Product titles, file-name captions, kicker text, counters, hints, pagination dots, decorative shadows, vignette, and gradient styling are removed.
- Source/reference checked: The current app schema, WebGL resource, DOM canvas composition, Canvas 2D export path, acceptance inventory, performance matrix, persisted product identity, and real local UI.
- Reference inputs: None. This delivery is an explicitly requested product-authored redesign.
- Docs/contracts read: `workflow.md`; Plan routes `core/reference-study.md`, `core/runtime-boundary.md`, `assembly-workflow.md`, `core/control-selection.md`, `core/layout.md`, `core/performance.md`, `core/setup-export.md`, and `core/media-upload.md`; Implementation routes `schema-reference.md`, `decision-contract.md`, `component-rules.md`, `renderer-technique.md`, and `performance.md`; Verification route `acceptance-testing.md`.
- Contract rules applied: `runtime-shell-required`, `canvas-no-app-ui`, `canvas-surface-preserved`, `interaction-surface-ownership`, `controls-product-coverage`, `output-export-required`, `controls-section-inventory-required`, `controls-layout-heuristics`, `renderer-technique-inventory`, `renderer-view-interaction`, `acceptance-product-observable`, `performance-coverage-levels`, `persistence-policy-explicit`, and `workflow-required`.
- View interaction intent: `fixed-camera` with `explicit-user-request` evidence. The user asked to retain current gallery behavior while removing its presentation layer; wheel, drag, and arrow keys continue to browse the cards rather than rotate the camera.
- Interaction ownership: Canvas owns transient image browsing and physical press feedback. The panel owns the source collection, layout choice, precise geometry/motion/view parameters, optional flat background, and still delivery. No panel control mirrors the current gallery offset.
- Decision: Keep the retained WebGL renderer, two placement branches, card shader, and physics untouched; delete all DOM and Canvas 2D presentation chrome; default the product background to excluded; retain the mandatory Background section as an optional flat fill; reclassify transfer metadata as `new-toolcraft-app`; rename visible product, layout, export, and persistence identities; reuse unchanged GPU textures during media additions/reordering; reuse card records, sort storage, and matrix buffers across animation frames; and split high-resolution export resize, render, asynchronous WebGL readback, composition, preview restoration, and encoding across responsive phases.
- Alternatives rejected: Removing only the large title would leave file captions, counters, hints, dots, shadow, vignette, and gradient; replacing WebGL with CSS would lose per-vertex physical curvature; renaming every internal geometry target would add persistence and verification churn without changing any visible product behavior.
- State/output mapping: Runtime media order and geometry/physics/view controls flow through `readSpiralGallerySettings` into one retained WebGL resource. The canvas mounts only the optional flat background element and WebGL canvas. Still delivery calls `createToolcraftPngExportCanvas`, optionally fills one flat color, rerenders the same card scene, and downloads `image-gallery.png` or `image-gallery.jpg`.
- Files changed: product schema, settings reader, canvas composition, CSS module, WebGL resource/export composition and resource types, stack placement comments/tests, acceptance/readiness metadata, renderer technique matrix, product Vitest, Playwright acceptance and performance adapters, product plans, and this worklog.
- Performance intent: ordinary-product-work. A delivery-trace failure isolated test-harness locator discovery inside the measured radius drag; the adapter now prepares and verifies that locator before timing begins, leaving only the pointer interaction and resulting render in the measured interval.
- Verification: `npm run verify:delivery -- --tier=4 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22media-import%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22initial-render%22%2C%22initial-render%22%2C%5B%22spiral.preview%22%2C%22spiral.webgl-resource%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22animation-frame%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-discrete%22%2C%22control-change%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D"` is the exact Tier 4 delivery command for this batch.
- Skipped checks: Full performance certification was not requested. No workload dimension, shader equation, pass invalidation, or resource lifecycle expanded; this redesign removes DOM and Canvas 2D work.
- Risks: Existing localStorage from the old identity intentionally does not migrate into the renamed product. Extremely large 8K exports remain subject to browser/GPU framebuffer limits and fail instead of silently reducing quality.

## Decisions

### Renderer

- Decision: Keep one retained WebGL resource with shared plane geometry, shader program, image textures, and one canonical renderer pipeline for Flow and Deck.
- Reason: Per-vertex cylindrical curvature and velocity-signed flex require GPU geometry deformation; this change only removes presentation layers around the cards.
- Evidence: `spiral.webgl-resource`, `spiral.preview`, and `spiral.export` remain the executable pass owners; the DOM text layer is removed from the renderer technique inventory.

### View Interaction

- Decision: Fixed perspective camera; no orbit target or orientation gizmo.
- Reason: The user explicitly asked to preserve current browsing behavior while stripping presentation.
- Evidence: Canvas wheel, captured vertical drag, and arrow handlers update image offset and physical flex; camera orientation is not user-authored.

### Interaction Ownership

- Decision: Canvas owns browsing; panel owns media collection and precise settings.
- Reason: Direct input preserves velocity and direction feedback, while ordering, transforms, exact values, background, and export need discoverable controls.
- Evidence: `source.images` has typed panel ownership and the product exposes no offset control that duplicates canvas navigation.

### Timeline

- Decision: No Toolcraft timeline and no Video Export.
- Reason: Motion exists only while user input and inertia settle; it is not an authored time sequence.
- Evidence: `animationIntent.mode` is `none` and `panels.timeline` is omitted.

### Layers

- Decision: No Layers panel.
- Reason: The ordered image sequence renders into one composited gallery scene rather than independently editable output layers.
- Evidence: `panels.layers` is omitted and media ordering remains in the Gallery file-drop collection.

### Controls

- Decision: Keep built-in sections for Gallery, Layout, Flow, Deck, Cards, Shadow, Depth, Physics, Interaction, View, Background, and Image Export; expose adjacent sticky `Export PNG` and `Export Code` actions; remove Overlay and Vignette; initialize Gallery with fourteen mixed JPG defaults and the imported Iteration 14 settings preset.
- Reason: Every remaining control changes image-card geometry, motion, view, media, optional background, or delivery.
- Evidence: `appControlSectionInventory` covers every remaining product target exactly once, conditional branches use `visibleWhen`, and the existing `source.images` file-drop owns the default presets and all subsequent media operations.

### Export

- Decision: Sticky `Export PNG` honors PNG/JPG format, 2K/4K/8K resolution, media order/transforms, current card position, selected layout, authored Shadow controls, bends, and optional flat background; adjacent `Export Code` packages the same configured section and assets for agent-assisted integration.
- Reason: The required still output must match preview, while the additional code artifact must preserve editable behavior instead of flattening the gallery into pixels.
- Evidence: Raster export uses `createToolcraftPngExportCanvas` and the shared WebGL scene; code export includes guide, config, portable React/CSS/renderer/physics modules, and current assets. Browser acceptance decodes raster formats/dimensions/background and unzips/inspects the code package.

### Performance

- Decision: Keep `repetition-count` as the only schema-backed workload dimension, capped at eight, with a maximum of 24 source images; lazy-load code packaging; cap projection-correct raster WebGL tiles at 2048 px per axis; and exercise the maximum repetition value through a bounded 960×540 development preview fixture with one stable source for initial/import/export paths.
- Reason: Interactive Flow work remains proportional to source count × repetitions; Deck remains a bounded subset. The development fixture isolates the declared repetition boundary while functional acceptance separately proves multi-image lifecycle and exact 2K/4K PNG/JPG output.
- Evidence: Unchanged source textures survive media additions/reordering, measured actions reuse prepared control/file-input locators, per-frame cards and matrix buffers are allocation-stable, code-only dependencies are emitted as a separate lazy production chunk, raster export yields between bounded tile rendering, `ImageBitmap` readback, Canvas 2D composition, preview restoration, and encoding, the exact 4K export path and all six impact-derived production paths pass, `app-performance-impact.json` preserves exact pass ownership, and fixtures clear built-in defaults before applying bounded workloads.

## Evidence

- Product files reviewed: schema, composition, settings, canvas, resource, export, CSS, placement math, acceptance data, performance matrix/impact inventory, product tests, browser tests, and adapters.
- Product-output evidence: both layouts mount one WebGL canvas with no `data-toolcraft-product-text` node or decorative output elements.
- Preset evidence: the local browser shows fourteen ordered JPG thumbnails, 42 Flow cards at repetition three, ready textures, settled physics, and no visible errors.
- Iteration 4 live evidence: the native user tab was reloaded with persistence v5 and reported the exact fourteen-item shuffled sequence, 42 Flow cards, ready textures, settled physics, and no visible errors.
- Iteration 5 live evidence: the native user tab was reloaded with persistence v6 and reported fourteen images, 42 Flow cards, ready textures, settled physics, both replacement names present, both superseded names absent, and no visible errors.
- Iteration 6 live evidence: the native user tab was reloaded with persistence v7 and reported fourteen images, 42 Flow cards, ready textures, settled physics, both user-image names present, both superseded names absent, and no visible errors.
- Iteration 14 live evidence: after one standard Reset to clear hot-reload carryover, the native tab reloaded from persistence v8 with Flow active, radius 2.8, twist 20.4°, vertical gap 0.35, card width 2.7, height 1.5, curve radius 10, 15% shadow at blur 0.31 and offset 0.03/0.12, the supplied depth/physics/interaction/view values, included `#171717` background, render scale 2, and no console errors.
- Iteration 15 live evidence: the native tab used the standard Background Reset, displayed `#EDEDED`, retained that value after a real reload from persistence v9, and reported no console errors.
- Delivery evidence: recorded after the protected delivery gate and local browser check.

## Verification

- Run: pnpm verify:delivery

## Historical Verification

- Run: `npm run typecheck` (Iteration 15 passed)
- Run: `npx vitest run src/app/spiral-gallery-product.test.ts --reporter=default` (Iteration 15: 1 passed)
- Browser: Iteration 15 Background Reset and reload confirmed `#EDEDED` with no console errors; no heavy delivery or performance gates were run.

- Run: `npm run typecheck` (Iteration 14 passed)
- Run: `npx vitest run src/app/spiral-gallery-product.test.ts --reporter=default` (Iteration 14: 1 passed)
- Browser: Iteration 14 standard Reset plus real reload confirmed the imported v8 defaults and no console errors; heavy aggregate and performance gates were intentionally skipped.

- Run: `npm run typecheck` (Iteration 13 passed)
- Run: `npx vitest run src/app/spiral-gallery/spiral-gallery-code-export.test.ts src/app/spiral-gallery-product.test.ts src/app/app-performance.gates.test.ts --reporter=default` (Iteration 13: 14 passed)
- Run: combined targeted Flow and Deck export browser scenarios (Iteration 13: 2 passed; PNG/JPG/background/resolution plus both code packages)
- Run: exact canonical `spiral.export` Playwright performance path for `repetition-count` (Iteration 13 passed after projection-correct tiled rendering)
- Run: protected Tier 3 `npm run verify:delivery` with two unit selectors, two functional browser selectors, and all six impact-derived canonical performance selectors (Iteration 13 passed; production build, 2 browser tests, 6 performance tests)

- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 12)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs && node scripts/check-toolcraft-code-health.mjs && node scripts/toolcraft-product-boundary.mjs --allow-missing-compiler (Iteration 12)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-discrete%22%2C%22control-change%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" (exact protected delivery command for the shadow-controls batch; pending on the development machine)
- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 11)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs && node scripts/check-toolcraft-code-health.mjs && node scripts/toolcraft-product-boundary.mjs --allow-missing-compiler (Iteration 11)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22animation-frame%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" (exact protected delivery command for the card-shadow batch; pending on the development machine)
- Run: repository-wide reference sweep for the removed Blinds mode (Iteration 10): no production, schema, acceptance, pipeline, impact, or browser-spec references remain; only the deliberate legacy-fallback product-test guard mentions the old value (passed)
- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 10)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs && node scripts/check-toolcraft-code-health.mjs && node scripts/toolcraft-product-boundary.mjs --allow-missing-compiler (Iteration 10)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --unit-test=src/app/spiral-gallery/spiral-gallery-stack.test.ts --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" (exact protected delivery command for the Blinds removal batch; pending on the development machine)
- Run: ffprobe/ffmpeg frame-by-frame study of the Iteration 9 reference recording (1 fps overview, 6 fps dense, label-strip crops): static wall confirmed, open/closed width ratio ≈ rotation ratio, focus clamps at ends (passed)
- Run: node numeric validation of the Iteration 9 stationary-wall placement (slots independent of focus, 2.21x width gain at defaults, 14-panel wall spans ~11 of ~12.3 visible units, local 2-panel envelope, edge stops, whole-panel settle at weights 0.55 and 1) (passed)
- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 9)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs && node scripts/check-toolcraft-code-health.mjs && node scripts/toolcraft-product-boundary.mjs --allow-missing-compiler (Iteration 9)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-blinds.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: blinds layout controls update rendered output" --browser-test="browser: blinds navigation and image-only export stay in sync" --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" (exact protected delivery command for the Blinds rework batch; pending on the development machine)
- Run: node numeric validation of the Iteration 8 Blinds placement math (openness envelope, tilt interpolation, edge fade, paint order, wrap) and of the integrated wall state machine (nudge → weighted glide → snap → whole-panel settle) (passed)
- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 8)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs && node scripts/check-toolcraft-code-health.mjs && node scripts/toolcraft-product-boundary.mjs --allow-missing-compiler (Iteration 8)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-blinds.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: blinds layout controls update rendered output" --browser-test="browser: blinds navigation and image-only export stay in sync" --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" (exact protected delivery command for the Blinds layout batch; pending on the development machine)
- Run: node numeric validation of the Iteration 7 weighted-scroll spring (`resolveStackScrollSpring`/`advanceStackScroll`): zero overshoot at weights 0/0.25/0.55/0.8/1, monotonic settle times 82→171 frames, stable at frame scales 0.001/0.5/2 (passed)
- Run: node simulation of the Iteration 7 integrated deck state machine (nudge → spring glide → snap → settled): settles on a whole card at every weight, reduced-motion path preserves the legacy first-order step, mid-glide input keeps converging (passed)
- Run: node_modules/.bin/tsc -p tsconfig.json --noEmit (Iteration 7)
- Run: node scripts/check-toolcraft-docs.mjs && node scripts/check-toolcraft-integrity.mjs (Iteration 7)
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery/spiral-gallery-stack.test.ts --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: deck layout controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" (exact protected delivery command for the weighted deck scroll batch; pending on the development machine)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts src/app/spiral-gallery/spiral-gallery-stack.test.ts
- Run: pnpm typecheck
- Run: npx playwright test e2e/app-performance.spec.ts --grep 'control-drag'
- Run: npm run build
- Run: production-preview impacted performance paths (six selected paths passed; one accidentally over-selected targetless framework path failed and is outside the declared impact set)
- Run: production-preview image gallery controls, deck navigation/export, and background/export browser scenarios (3 passed)
- Run: npm run ai:check (passed)
- Run: pnpm typecheck (passed)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts (passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery media lifecycle controls the rendered sequence" (passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery settings and media persist after reload" (passed)
- Browser: local in-app inspection confirmed eight ordered JPG thumbnails, 24 rendered cards, settled physics, ready textures, and no visible errors.
- Run: npm run verify:delivery -- --tier=4 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery controls update rendered output" --browser-test="browser: deck navigation and image-only export stay in sync" --browser-test="browser: image gallery background and export preserve output semantics" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22export%22%2C%5B%22spiral.export%22%5D%2C%5B%22export-only%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22batch-responsive%22%2C%22media-import%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22initial-render%22%2C%22initial-render%22%2C%5B%22spiral.preview%22%2C%22spiral.webgl-resource%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22animation-frame%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-continuous%22%2C%22control-drag%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D" --performance-test="browser perf: toolcraft path performance-path:%5B%22interactive-discrete%22%2C%22control-change%22%2C%5B%22spiral.preview%22%5D%2C%5B%22gpu%22%5D%2C%5B%22repetition-count%22%5D%5D"
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload" (exact protected delivery command for the JPG preset batch)
- Run: npm run ai:check (Iteration 3 passed)
- Run: pnpm typecheck (Iteration 3 passed)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts (Iteration 3 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery media lifecycle controls the rendered sequence" (Iteration 3 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery settings and media persist after reload" (Iteration 3 passed)
- Browser: Iteration 3 local inspection confirmed the exact fourteen-image order, 42 rendered cards, ready textures, settled physics, and no visible errors; the current open profile was updated through the visible Gallery Reset control.
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload" (exact protected delivery command for the mixed fourteen-image batch)
- Run: npm run ai:check (Iteration 4 passed)
- Run: pnpm typecheck (Iteration 4 passed)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts (Iteration 4 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery media lifecycle controls the rendered sequence" (Iteration 4 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery settings and media persist after reload" (Iteration 4 passed)
- Browser: Iteration 4 native-tab reload confirmed the exact fourteen-image v5 order, 42 rendered cards, ready textures, settled physics, and no visible errors.
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload" (exact protected delivery command for the v5 full-shuffle recovery batch)
- Run: npm run ai:check (Iteration 5 passed)
- Run: pnpm typecheck (Iteration 5 passed)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts (Iteration 5 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery settings and media persist after reload" (Iteration 5 passed as part of the targeted two-test run)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery media lifecycle controls the rendered sequence" (initial Iteration 5 run exposed pre-existing keyboard-drag activation timing; targeted fix passed)
- Browser: Iteration 5 native-tab reload confirmed fourteen v6 images, 42 rendered cards, ready textures, settled physics, both replacement names present, both superseded names absent, and no visible errors.
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload" (exact protected delivery command for the two-preset replacement batch)
- Run: npm run ai:check (Iteration 6 passed)
- Run: pnpm typecheck (Iteration 6 passed)
- Run: pnpm exec vitest run src/app/spiral-gallery-product.test.ts (Iteration 6 passed)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery settings and media persist after reload" (Iteration 6 passed as part of the targeted two-test run)
- Run: pnpm exec playwright test e2e/spiral-gallery.spec.ts --grep "browser: image gallery media lifecycle controls the rendered sequence" (initial Iteration 6 run exposed keyboard collision nondeterminism; pointer-drag fix passed)
- Browser: Iteration 6 native-tab reload confirmed fourteen v7 images, 42 rendered cards, ready textures, settled physics, both user-image names present, both superseded names absent, and no visible errors.
- Run: npm run verify:delivery -- --tier=3 --unit-test=src/app/spiral-gallery-product.test.ts --browser-test="browser: image gallery media lifecycle controls the rendered sequence" --browser-test="browser: image gallery settings and media persist after reload" (exact protected delivery command for the user-AVIF replacement batch)
- Verification: the Tier 4 command records Iteration 1; the first four Tier 3 entries record Iterations 2–5; the latest Tier 3 entry records the exact protected delivery gate selected for Iteration 6.
- Browser: targeted image-only output, Flow/Deck navigation, background, export, media, preset reset, and persistence checks are selected across the recorded batches.

## Risks

- Risk: Unsupported or corrupted uploaded images remain governed by Toolcraft media feedback and browser decoder capability.
- Risk: The current preset enables the `#EDEDED` product background by default; users can still turn Include off for transparent PNG output.
- Risk: Browser/GPU maximum texture and framebuffer sizes can make an explicitly requested 8K export fail; the app does not silently downgrade output.
- Risk: The fourteen local preset JPEGs add about 16 MB to the app's initial static media payload.
- Risk: The v5 persistence boundary intentionally supersedes v4 once so stale eight-image state cannot shadow the full shuffled default collection.
- Risk: The v6 persistence boundary intentionally supersedes v5 once so the two rejected presets cannot persist after their replacement.
- Risk: The v7 persistence boundary intentionally supersedes v6 once so the misty-forest and salt-pond presets cannot persist after replacement by the two user-supplied images.
- Risk: The v8 persistence boundary intentionally supersedes v7 once so older local values cannot shadow the imported default preset.
- Risk: The v9 persistence boundary intentionally supersedes v8 once so the previous `#171717` value cannot shadow the new `#EDEDED` default.


## 2026-08-05 — Canonical product identity and deployment path

- User-visible result: Renamed the standalone product to `Spatial Gallery` and aligned its repository package plus public demo base to `spatial-gallery`. Product rendering, controls, defaults, and export behavior remain unchanged.
- Request: Apply the approved complete rename across code, folders, gallery identity, and deployment wiring without preserving old route aliases.
- Source/reference checked: The approved complete-app-renaming design and implementation plan, the current standalone package metadata, Vite/router base handling, `vercel.json`, identity metadata, and active acceptance/deployment assertions.
- Contract rules applied: Broad identity/deployment migration because the directory and public deployment identity change across the generated app boundary. Existing product-domain modules remain semantically named; the external Vercel stage must retain the current Project ID.
- State/output mapping: Package name, HTML title, control/acceptance identity, persistence/settings-transfer namespace where present, Vite base, public asset prefix, and Vercel rewrites now use `spatial-gallery`. Changed persistence namespaces intentionally reset prior browser-local settings.
- Verification: Canonical package/title/base audit and every available standalone `demo-deployment.test.mjs` passed for this migration batch.
- Risks: Old demo paths are intentionally absent; no compatibility redirect is retained.
