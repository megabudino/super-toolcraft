# Local: file database. Production: libsql://<db>-<org>.turso.io
DATABASE_URL=file:local.db
DATABASE_AUTH_TOKEN=
# Required to open /setup the first time (the database has no users yet).
SETUP_TOKEN=change-me
# Name shown in the UI (logo, titles, invitations).
NEXT_PUBLIC_PLATFORM_NAME=Studio
